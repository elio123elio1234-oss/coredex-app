/* App version — rendered in the visible badge (web CLAUDE.md §8 convention). */

export const APP_VERSION = '0.7.0';
export const APP_BUILD_LABEL = 'all 15 pastel illustrations; exam waits for the real rotation before laying out';

// v0.7.0 — Flicker and photo-shape were one bug: the route laid itself out at
//          portrait size while the OS was still rotating. Profile art ported.
