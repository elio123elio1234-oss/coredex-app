/* App version — rendered in the visible badge (web CLAUDE.md §8 convention). */

export const APP_VERSION = '0.10.1';
export const APP_BUILD_LABEL =
  'prep photo uncropped again (absoluteFill); report on the web white/blue paper';

// v0.10.1 — Two regressions of mine: `StyleSheet.absoluteFill` on the prep
//           Image cropped the photograph to a corner AGAIN (it is banned in
//           that file now), and the report strips were painted on invented
//           pink paper instead of the web's white sheet with a blue grid.
