/* Shared prop shape for the pastel section illustrations. Each concrete
   illustration is its own file supplying the paths (CLAUDE.md §3.1). */

export interface IllustrationProps {
  /** Rendered square, in dp. */
  size?: number;
}

// v1.0.0 — Illustration prop shape.
