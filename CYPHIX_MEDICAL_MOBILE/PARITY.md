# PARITY.md — Web ↔ iOS ↔ Android feature ledger

**This file is mandatory bookkeeping for the Cross-Platform Rule (root
`CLAUDE.md` §1).** Every feature that exists on any platform has a row here.
A feature shipped web-only gets a row marked `pending` with a reason —
"we forgot" must be structurally impossible.

Legend: `✅ done` · `🟡 partial` · `⏳ pending` · `🔬 needs-iOS-verify` (built
but never run on a physical iPhone — Windows dev machine, see root §5) · `—` n/a.

## Modules (the five nav entries)

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| Floating glass bottom dock | ✅ | 🔬 | 🔬 | Ported: blur pill, springing highlight, filling icons, emphasized centre Home. Order History·Insights·Home·Chat·Profile per `dockConfig.tsx` on BOTH (web v1.57.0 — slot 2 was Tests there until then, keeping the Tests glyph exactly as mobile does). Metrics in `navigation/dockMetrics.ts`. **Two deliberate departures** (documented there): Home icon 26px not `clamp(28,7.6vw,33)`, padding 6 not 7/8 → 67px tall instead of 77, because on the web that Home rule is a desktop rule leaking into the phone breakpoint on specificity. **Not ported:** the History-only ✕ collapse (the drag-the-pill gesture landed in v0.24.3 — see below) |
| ↳ Dock — drag the pill across the tabs | ✅ | 🔬 | 🔬 | **v0.24.3, closing a note open since v0.2.** One `Pan` on the bar: the pill follows the finger continuously through all five tabs, ticks with `selectionAsync` (the picker-wheel event — this is scrubbing, not impact) as it passes each, stays swollen for the whole drag, and commits on release clamped to the ends. ★ Before this the highlight moved only on TOUCH-DOWN, because a `Pressable` owns its touch from the moment it starts and never re-targets — so the only reachable tab was the one the finger landed on, and sliding did nothing for every tab equally. Taps are unchanged: the pan needs 6 pt of travel first, which also keeps each tab a real a11y button rather than a region of a gesture surface. `runOnJS(true)` so ordering against the pressable's cancellation is deterministic, not a race |
| ↳ Dock — the material answers the finger | — CSS has no equivalent | 🔬 | 🔬 | **Mobile-only by nature, v0.24.0.** (a) The highlight travels to the tab under the finger on touch-down, commits on release, springs home after 140 ms on a cancel. (b) Hold past 220 ms and the pill swells 13 % + heavier haptic, icon and label 7 %. **Both platforms both times** — it is Reanimated, not the material. A web `backdrop-filter` has no finger to answer, so there is nothing to port back. ⚠️ v0.24.1: Apple's own `UIGlassEffect.isInteractive` was tried on the bar and **removed** — reported as "no glass effect at all", and it is meant for a button-sized control inside a glass container, not a whole bar |
| ↳ Dock — the pop when selection lands | — | 🔬 | 🔬 | **v0.24.2.** The landing tab's icon + label rise 10 % (110 ms timing) and spring back. Selection had only STATES before — the pill slid, the icon filled — so committing a tab read as the bar catching up rather than as the tap doing something. On the CONTENT not the pill: the pill's scale already carries the press swell, and 1.13 × 1.10 on one transform is wide enough to be cut by the bar's rounded cap on the outermost tabs. Not fired on first paint. **Not** done as a persistent selected-tab scale — at 68.9 pt of item width that truncates the label in every language, Hebrew first |
| ↳ Dock — the selection indicator | ✅ | 🔬 | 🔬 | **v0.24.1: the pill is SOLID, and is the same constant the icon cut-outs use.** It was translucent, i.e. visible only by being *brighter than the bar* — so glassing the bar in v0.24.0 erased the current tab completely. One value now feeds both the pill and the cut-outs, which were always required to match and had only ever been *similar*. Own hairline + small shadow, like a system segmented control's puck |
| ↳ Which frosted material resolved (Settings › About) | — | ✅ | ✅ | **Diagnostic, v0.24.1.** `GLASS_MATERIAL` names what actually loaded: `Apple Liquid Glass (iOS 26+)` / `UIBlurEffect — no Liquid Glass on this client` / `BlurView (dimezisBlurView)`. "It doesn't look like glass" has three indistinguishable causes — no iOS 26, no `expo-glass-effect` in the running client, or a design problem — and from a Windows machine only the phone can say which. Value stays English, like the build label, so a bug report quotes the changelog's string |
| ↳ Dock — why the pill is not a second `GlassView` | — | ✅ decided | ✅ decided | **Deliberate divergence from the obvious implementation.** `UIGlassContainerEffect` MERGES nearby glass elements into one shape — that is what the container is for — so a glass pill inside a glass bar dissolves into the bar and the selection indicator stops existing. Apple's own tab bar is one glass bar with a solid-ish capsule riding on it, which is what this is. Written down because the wrong version would have passed typecheck, both bundles and `expo-doctor` |
| ↳ Dock — hold is not `onLongPress` | — | 🔬 | 🔬 | React Native does **not** fire `onPress` after `onLongPress` fires, so wiring the swell to it gives a hold that grows the glass and then navigates nowhere. The dock times the hold itself (`HOLD_MS` 220, not `delayLongPress`'s 500 — that default is the threshold for long-press *menus* and is too slow to read as the surface reacting). Releasing selects the tab however long it was held |
| **App icon** (home screen / launcher) | ➖ n/a (a web app has a favicon, not an app icon) | 🔬 needs-device-verify | 🔬 needs-device-verify | **v0.67.0 — the ECG heart.** Five files from one square artwork via `npm run icons` (`scripts/make-icons.ps1`). `icon.png` is full-bleed and carries **no alpha channel** — the App Store rejects one on an app icon. Android gets the three adaptive layers; the **monochrome** one (Android 13+ themed icons) is **DRAWN, not derived** — the launcher tints that layer flat and discards its colour, so a threshold of the artwork is a ragged blob. It is a filled heart with the pulse trace punched out, and the script **measures** the result: it reads the icon's centre column and fails if the R spike has reached the heart's own notch, because the two gaps merging makes the lobes read as two blobs. Two attempts failed exactly that way before the check existed. `adaptiveIcon.backgroundColor` #FFFFFF → #9971D8 (the artwork's mean field). **iOS has no themed-icon layer**, so the tinted/dark home screen there shows the full-colour icon — a platform difference, not a gap. ⚠️ An icon is compiled into the binary, so this is a REBUILD and app.json moved 0.36.0 → 0.37.0. Rendered and inspected under both launcher masks on Windows; **nobody has seen it on a home screen** |
| ↳ **App icon — now the SIX-LEAD ECG (v0.72.0)** | ➖ n/a | 🔬 needs-device-verify | 🔬 needs-device-verify | Replaces the v0.67.0 ECG heart in the row above. Source committed at `assets/brand/app-icon-source.png`. ⚠️ **Flagged and overruled, recorded so the density is not later read as an oversight:** rendered at the REAL home-screen sizes first (180 px and 60 px, magnified x3 nearest-neighbour so nothing was flattered), the six thin traces and the I/II/III/aVR/aVL/aVF labels collapse into a blue-grey smudge at 60 px. Four alternatives from the same artwork and palette (3 / 2 / 1 lead on the original gradient) were offered with previews; the user chose the full artwork. It is their brand. ★ **Two things that were NOT taste.** (1) **The Android safe zone is a CIRCLE, not a square.** Full-bleed, every launcher mask sliced the lead labels mid-glyph (`aVR` → `R`); fitting to the 72dp SQUARE still clipped under Pixel’s round mask, because a 683 px box has a 914 px diagonal. New `scripts/make-adaptive-foreground.js` rebuilds that layer: the artwork’s own gradient with its own six leads added back at **50 %** of the frame, fitted by the DIAGONAL, the subject added as GLOW (artwork minus its own FINE local background) rather than `max()` — which left a bright rectangle where the cyan corner outran the coarse field — and feathered over the outer 5 % so the content box leaves no step. **iOS is untouched and full-bleed**: it does not crop an icon, only rounds its corners. This is a real iOS/Android DIVERGENCE in the asset, and it is the platform’s, not ours. (2) **The monochrome layer was a heart and could not stay one** — Android 13+ tints it flat, so a themed home screen would have shown a DIFFERENT MARK from the app drawer, and nothing on Windows would have surfaced it. It is now three bold ECG traces (three, not six: six leaves ~5 dp of pitch at 48 dp — the same smudge, in the one layer we draw ourselves). Its block is SOLVED from the safe radius, not chosen, and the script FAILS THE BUILD on wrong run count, thin strokes, or reach past the circle. It did fail once, at 406 px against a 342 px radius. `adaptiveIcon.backgroundColor` #9971D8 → #10307F, sampled. ⚠️ REBUILD: app.json 0.37.0 → 0.38.0, and v0.68.0-v0.71.0 ride along in the binary because those OTAs targeted 0.37.0. All five files rendered under circle, rounded-square and squircle masks and LOOKED AT; **nobody has seen it on a home screen** |
| ↳ **App icon — now the ECG PAPER STRIP (v0.75.0)** | ➖ n/a | 🔬 needs-device-verify | 🔬 needs-device-verify | Replaces the six-lead artwork above, which was flagged at the time as collapsing into a smudge at 60 px and shipped anyway at the user’s choice. **This one does not** — rendered at a real 60 px the trace and the grid are both legible. Source is a clean 1024 square (no backdrop, no pre-applied corner mask). ★ **Android is FULL-BLEED again, and that is a judgement, not a regression.** v0.72.0 needed `make-adaptive-foreground.js` because its lead labels sat in a column hard against the left edge and every mask sliced them mid-glyph; THIS artwork carries paper TEXTURE at its edges (part numbers already cropped in the source), so a circular crop reads as a window onto the strip rather than as damage. Rendered under circle, rounded-square and squircle masks and looked at before deciding. The **monochrome** layer stays THREE traces though the artwork is one strip: both were rendered at a real 48 dp and compared, and a lone trace is swallowed by the circle while three read as "ECG paper", which is what the artwork is. Same subject, different density — not the heart-vs-ECG contradiction v0.72.0 had to fix. `adaptiveIcon.backgroundColor` #10307F → #D1C9CB, sampled. Two landmines removed on the way: the foreground script CLAMPED the subject difference at zero (assuming subject-brighter-than-background — true of neon on navy, false of a dark trace on white paper; it would have shipped a blank gradient), and the monochrome’s safe-circle check compared two floats the solver makes EQUAL and rejected its own answer at `$rows = 2`. ⚠️ REBUILD: app.json 0.38.0 → 0.39.0 |
| ↳ **App icon — now the ECG CARD, HR 72 / 25mm/s (v0.77.0)** | ➖ n/a | 🔬 needs-device-verify | 🔬 needs-device-verify | ★ **The artwork arrived PRE-MASKED** — a rounded card on a white page, which is how an icon is PRESENTED and the opposite of what the platforms want. `icon.png` must be full bleed because iOS applies its own squircle on top, and this artwork’s radius measured **25.2 % against iOS’s ~22.4 %**, so shipping it as-is gives white crescents bitten out of every corner. New `scripts/unmask-icon.js` (third pre-masked source in a row, so a script): edges measured from the CENTRE lines (the corner arcs and the drop shadow cannot move them), corner profile measured ROW BY ROW (no curve assumed). ★ **What I missed for three attempts: the OS masks the corners anyway.** The first three tries mapped the whole bounding square and tried to INVENT the corners back — ~420 px of made-up pixels each — and every method left a visible streak. Modelling the corner as a circular ARC was wrong twice over: a modern corner is a SQUIRCLE, so a circle cuts INSIDE the real shape along part of the curve (page white showed through) and OUTSIDE it along the rest (the rim smeared into an outline). Zooming to 88 % drops the worst uncovered corner from 257 px to **21 px**, which sits under the mask; the 12 % given up is all margin, and "HR 72" keeps 47 px of clearance. **Android is full-bleed and DOES crop** — the 1.5x launcher zoom takes "HR 72", "25mm/s" and the swoosh, leaving one bold beat. The safe-circle alternative (47 % of the frame) was BUILT AND RENDERED SIDE BY SIDE and is worse: a small busy card on a pale field. At 48 dp those annotations are unreadable decoration and the trace is the mark. `make-adaptive-foreground.js` stays for artwork whose edges carry meaning, as v0.72.0’s did. `adaptiveIcon.backgroundColor` #D1C9CB → #DBE5F5. Monochrome unchanged (three traces). ⚠️ REBUILD: app.json 0.39.0 → 0.40.0 |
| ↳ **App icon — now the 6-LEAD REPORT CARD (v0.78.0)** | ➖ n/a | 🔬 needs-device-verify | 🔬 needs-device-verify | Chosen by the user from FIVE candidates, all rendered at the real home-screen size on a dark AND a light wallpaper and ranked. Two of my claims died in that render: this artwork is MORE legible at 60 px than I predicted, and the neon heart was the MOST legible of the five (it lost on identity, not eyesight). ★ Six leads at home is the product’s actual differentiator, and the "6" is the one thing no other health app could put on its icon. ⚠️ **THE PIPELINE BUG THIS EXPOSED:** v0.77.0 shipped with "HR 72" SLICED by iOS’s squircle — 7,178 ink px outside the mask, worst by 31 px — **reported from the phone, not caught here.** One-sided optimisation: `unmask-icon.js` chose the frame so the card nearly filled it (no white corners) and never asked the opposite question, pushing content INTO the corners the OS cuts. Two constraints pull against each other; I checked one. The script now measures both and re-checks. Three things had to be right, each wrong first: **(1) LOCAL CONTRAST**, not luminance, decides what "content" is — "darker than 140" finds NOTHING on a pale-on-pale artwork (it would have passed the icon most likely to clip), and "darker than the median" flags the card’s own RIM, which reaches the edge by definition. **(2) The re-check IGNORES THE PADDING BAND** — it repeats the card’s edge, so a trace running to that edge is smeared into a bar reaching the corner; counting it never converged (two rounds of "STILL CLIPPED" against the fix’s own artefact). **(3) `make-adaptive-foreground.js` had four HARDCODED crop numbers** from an era before `unmask-icon.js` — a stale crop that mangled this artwork on the first run. **Regression-tested** against v0.77.0’s artwork: correctly detects and shrinks to 86.1 %. **Android uses the rebuilt SAFE-CIRCLE foreground here**, unlike v0.75.0/v0.77.0: full-bleed was rendered first and cut the "6" and the lead labels, because on this artwork the edges carry the CONCEPT, not texture. ⚠️ Honest cost: at 48 dp it is FAINT — a pale card on a pale field at 47 % of the frame. If Android becomes a real target the fix is a deeper background field, not a bigger subject. `adaptiveIcon.backgroundColor` #DBE5F5 → #DBE8FA. ⚠️ REBUILD: app.json 0.40.0 → 0.41.0 |
| ↳ **App icon — the 6-lead card ON BRAND BLUE (v0.79.0)** | ➖ n/a | 🔬 needs-device-verify | 🔬 needs-device-verify | Same concept as the row above, new artwork. ★ **It fixes the one thing v0.78.0 was filed with a warning about:** that icon "floats" on a light wallpaper (low internal AND external contrast at once). Rendered side by side at 60 px on a light wall, the pale version loses its own outline and this one holds a hard edge. ★ **Android flips back to FULL-BLEED** — the opposite call to v0.78.0, for the opposite reason. That artwork ran content to the card’s edges so the launcher’s 1.5x zoom cut the "6" and the lead labels; THIS one has generous margin built in, so full-bleed survives the same crop with nothing lost, and is bolder than the 47 % subject the safe-circle treatment gives. Both rendered under a circular mask and compared before choosing. The faintness warned about in v0.78.0 is RESOLVED, not inherited. **The rule, now that three artworks in a row have needed different answers: full-bleed when the edges carry margin or texture; the rebuilt safe-circle layer when they carry meaning.** A property of the artwork, not a house style. Pipeline: `unmask-icon.js` treated an already-full-bleed source as an ERROR and exited — refusing exactly the artwork that needs no un-masking, when "no corner to remove" is not "nothing to check" (the mask-fit constraint is what shipped a sliced "HR 72"). It now passes through and runs the check alone. `adaptiveIcon.backgroundColor` #DBE8FA → #6AA6E8. ⚠️ REBUILD: app.json 0.41.0 → 0.42.0 |
| ↳ **App icon — it FILLS THE FRAME now (v0.80.0)** | ➖ n/a | 🔬 needs-device-verify | 🔬 needs-device-verify | Reported: *"on iOS it’s too small."* Measured: content at 47 % x 54 % of the frame where the mask allowed 78 %. ★ **And v0.78.0’s check PASSED it** — it asked only "is anything CLIPPED?", which is half a rule: nothing was, so it had no opinion about an icon far too small. Shrink-only was never the right shape; one measurement answers both questions and I used one direction. `unmask-icon.js` now NORMALISES to `SAFE_FILL` (0.87) of the largest scale the mask permits, zooming or shrinking. Computes x1.449 of a x1.665 ceiling — the same value picked by eye from a rendered comparison BEFORE the rule was written, which is the only reason to trust it. ⚠️ Wrong for artwork whose content is a full-bleed TEXTURE (v0.75.0’s paper): there the ceiling is ≈1 and 0.87 shrinks something meant to bleed. Recorded, and the scale is printed every run. ★ **Android takes the UNZOOMED artwork**, because the launcher’s own 1.5x crop IS the Android version of this zoom — fed the unzoomed source it leaves content at ~70 % of the visible area. **iOS and Android are handed different framings of one artwork**, for one reason: each applies a different amount of its own. Measured: unzoomed content survives the square window but its furthest corner sat 377 px from centre against a 342 px safe RADIUS, so a Pixel circular mask would have clipped the "6" and the keypad; x0.90 brings it to 328 px. Both masks rendered. `adaptiveIcon.backgroundColor` #6AA6E8 → #92C2F2. ⚠️ REBUILD: app.json 0.42.0 → 0.43.0 |
| **App name — "Cyphix", not "CYPHIX Medical" (v0.81.0)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | `expo.name` is compiled into the binary (`CFBundleDisplayName` on iOS, `app_name` on Android), so a rename is a REBUILD, never an OTA. Changed in the two places it was written: `app.json`’s `name` and `BrandLogo`’s `accessibilityLabel` — those two must agree, because a screen reader announcing a different product name from the one on screen is the accessibility equivalent of a wrong label. ⚠️ **NOT renamed, and flagged rather than guessed: the WORDMARK still draws MEDICAL.** It is a separate `<Path>` (`fill={medical}`) so hiding it is one line — but `CROP_BOX` is measured around the FULL lockup (40.988 → 181.798), and dropping the subtitle leaves ~56 units of dead air on the right. Wherever the mark is CENTRED (`BootSplash`) it would sit visibly off-centre — exactly the defect `crop` was added in v1.1.0 to fix. A new box must be measured and LOOKED AT, and react-native-svg cannot be rendered from this machine. Shows on BootSplash, ProfileScreen and the report letterhead; the letterhead is its own decision regardless, since it identifies the issuer of a clinical document. **Web is `pending`**: its own name/wordmark is untouched by this change and needs the same decision. The App Store listing name lives in App Store Connect, not in the repo |
| ↳ Splash screen mark | ✅ | ⏳ unchanged | ⏳ unchanged | **Deliberately NOT changed with the icon, v0.67.0.** `assets/splash-icon.png` still carries the old mark. The request was the app icon; a launch screen is the other half of a brand moment and changing it unasked would be a second design decision smuggled in behind the first. Flagged to the user rather than done silently |
| CYPHIX wordmark | ✅ | 🔬 | 🔬 | SVG verbatim; inner drop-shadow filter dropped (no react-native-svg equivalent) |
| ↳ Floating shell wordmark (top-start) | ✅ (in the sidebar) | ⏳ hidden | ⏳ hidden | **Deliberate divergence, v0.14.0, at the user's request — temporary.** Behind `SHOW_SHELL_WORDMARK` in `config/featureFlags.ts`, currently `false`; flip it and the mark returns on every patient screen at once. The flag sits **outside** the mirrored `FEATURE_FLAGS` table on purpose — that table maps the web's nav modules one for one, and this is a mobile-only presentation switch (the web's mark lives in a sidebar mobile does not have). The 70 pt of top padding that existed only to clear the mark follows the same switch, so nothing reserves space for something undrawn; patient screens are vertically centred, so content settles up ~29 pt. Rendered from **two** sites — `PatientShell` and `ProfileScreen`, which scrolls and so does not use the shell — both reading the one constant |
| ↳ Report letterhead wordmark | ✅ | 🔬 | 🔬 | **Not** covered by the switch above, on purpose: this mark identifies a clinical document rather than decorating a screen, and a report with no issuer on it is a separate decision |
| Teal wavy shell backdrop | ✅ | 🔬 | 🔬 | 5 ribbon paths + field gradient, both themes. Alternate styles (waves/white/gray/calm) are now pickable in Settings › Appearance. **Not ported:** the web's `glass` style |
| Home (greeting + blob button) | ✅ | 🔬 | 🔬 | Orb, particles, morphing blob ported. **The tap target must stay ABOVE the Skia canvas** — wrapping the canvas in the `Pressable` let the canvas eat the touch and the button did nothing (v0.4.1). v0.19.0: the greeting is the registered account's own name, falling back to the demo card. v0.25.0: the **connected** state is a deliberate divergence — see the row below |
| ↳ ⚠️ **Landscape was legal on EVERY screen, not just the exam (v0.75.0 DID NOT fix it — fixed v0.76.0)** | ➖ n/a | 🔬 | 🔬 | Reported: *"after you leave the measurement screen, turning the phone sideways is suddenly legal in ALL the tabs."* ★ **The per-route masks were never the problem** — they read correctly in both platforms’ react-native-screens source. **On iOS nobody was asking them.** iOS decides whether the USER may rotate by asking the ROOT view controller for `supportedInterfaceOrientations`, and a bare Expo app has no root VC that knows about react-native-screens — so the question fell through to Info.plist, which `app.json`’s `"orientation": "default"` fills with every orientation. **Why it hid:** RNS does not need the root VC to ROTATE (`enforceDesiredDeviceOrientation` calls `requestGeometryUpdate` on the scene directly), so pushing the exam turned the phone correctly while nothing enforced the mask BETWEEN navigations. The exam looked right, which is what stopped anyone looking further. Fix: `expo-screen-orientation` is a dependency **and is never imported** — its ReactDelegateHandler installs a root VC that DEFERS to react-native-screens when a trait is set. ⚠️ Installing it makes the declarative masks take effect; CALLING it is what caused three rotations per navigation and got `lockAsync` banned. **The ban stands** — an `import` of that package in `src/` is the bug coming back, and RootNavigator’s header says so. Android was already correct (the activity’s requestedOrientation is set per screen); this is an iOS-on| ↳ ★ **Orientation — ONE authority, three writers removed (v0.76.0)** | ➖ n/a | 🔬 | 🔬 | **v0.75.0 shipped a whole binary claiming this was fixed and it was not** — reported back as "the icon changed but the rotation still isn’t fixed". It installed `expo-screen-orientation` WITHOUT calling it, on the reading that its root VC defers to react-native-screens’ masks. It does not: `ScreenOrientationViewController` is a plain `UIViewController`, and `guard !shouldUseRNScreenOrientation() else { return super.supportedInterfaceOrientations }` returns **UIKit’s default** (`allButUpsideDown`), not RNS’s mask. The method name READS as "defer to RNS" and MEANS "defer to UIKit". ★ **So the declarations were never neutral — they WERE the bug:** any RNS orientation trait fires that guard and switches the package off for the whole app. And there were **THREE** writers, two of them dynamic and invisible in the navigator: (1) `RootNavigator`’s stack + exam `orientation`; (2) `LimbMeasureScreen`’s `nav.setOptions({ orientation })` per phase; (3) `StudyViewerScreen`’s `setOptions({ orientation })` on full screen — under a comment claiming it kept RNS "the single owner of that API". Any ONE of them unlocked rotation everywhere, so fixing the navigator alone would have shipped another no-op binary. Fix: no route declares `orientation`, nothing calls `setOptions` with it, and `expo-screen-orientation` is the only writer — baseline `PORTRAIT_UP` at start, the exam takes `LANDSCAPE` on focus and returns it on blur, the viewer takes it for full screen and **restores portrait on blur** (without that, backing out of a full-screen study leaves the whole app LANDSCAPE-locked — the mirror image of the bug). The `lockAsync` ban is LIFTED rather than worked around: the flicker’s diagnosis (two writers of one native API) was right, but the cure removed the only writer iOS listens to and left the declarations. **New invariant, in RootNavigator’s header: NO ROUTE MAY DECLARE `orientation`.** Cost: the exam rotates just AFTER its push, not as part of it. Android was never broken — its per-screen requestedOrientation really is applied — and now takes the same path for one behaviour. ⚠️ Needs build 12 (runtime 0.39.0); published to 0.39.0 only, since build 11 has no such native module |
ly gap, fixed for both |
| ↳ Screen entrance — title leads, content follows (v0.74.0) | ➖ CSS has its own | 🔬 | 🔬 | Asked for as *"make the elements rise with a subtle, professional animation"*. `PageTitle` rises 8 pt over 380 ms on mount; the body follows 90 ms behind (`FadeUpView`, 10 pt / 420 ms — the SAME numbers History’s rows have used since v1.4.0, so the app keeps one motion vocabulary instead of inventing one per screen). Applies to History (skeleton, error card, empty card, rows) and Insights (all four states). ⚠️ Three things deliberately not got wrong: the title’s entrance is MULTIPLIED INTO its scroll fade inside one animated style (two nested animated views both writing `opacity` is how a screen entered mid-scroll ends up half-lit); the body wrapper is KEYED BY PHASE (otherwise React keeps it mounted across a state change and the real content appears instantly under a wrapper that already finished animating for the spinner); and that wrapper restates `gap: 14`, because the body is now ONE child of the content container instead of many |
| ↳ Home — the `homeSubPatient` subtitle is REMOVED | ✅ still shows it | 🔬 | 🔬 | **Deliberate divergence from web, v0.68.0, at the user's instruction.** The web MeasurePage prints "Performing a Home ECG Test" under the greeting; mobile no longer does. It narrated the screen to somebody already standing on it, one line above a button reading *Start Test* — and on a phone that line is competing for the vertical room the patient-screen rule (fits without scrolling) is trying to protect. The greeting stays. `homeSubPatient` is deleted from both locales, not orphaned. **Not ported back to web** — the web hero is a desktop composition with room the phone does not have, same reasoning as the connected-state row below | ⚠️ **v0.73.0 follow-up — the SPACE is kept.** Reported immediately: *"now Hello Elio is really close to the button."* A second-order effect, not a taste miss: `PatientShell` centres vertically, so deleting ~30 pt from this block both left only `inner`’s 12 pt gap under a 38 pt heading AND dropped the greeting half the lost height down the screen. `greet` now carries `marginBottom: 32`, which is `greetSub`’s old footprint, so the composition sits exactly where it did. **The rule this is an instance of:** on a vertically-centred screen, deleting an element moves everything that is left — "remove the text" is not the same instruction as "remove the space".
| ↳ Home orb — the connected state is a button, not a picture | ✅ navy blob + white morphing core + caption under it | 🔬 | 🔬 | **Deliberate divergence from web, v0.25.0, at the user's request** ("it isn't pretty, and it isn't clear it's a button"). The web puts the action's NAME under the shape and decoration — the white morphing core, which reads as a heart — in the middle; that is a caption under a picture, and nothing in it says *press*. On mobile the middle carries the **action**: play glyph + the button's own words in white on the brand navy, arriving at **45 %** of the 1.2 s fill (colour first, words second); the white core is **removed**, with the grey idle core expanding ~55 % as it dissolves so the dot reads as opening into the label; a **navy drop shadow** lifts the shape off the page; the caption below **collapses** in step, so the action is never named twice. A green "ready" fill/ring was offered and **declined** — the brand is navy and the connection is already stated in words. The **disconnected** state is untouched on purpose. Not ported back to web: the web hero is a desktop composition with room the phone does not have. ⚠️ The Skia canvas is `ORB + 2·PAD` and the orb box carries `marginVertical: -PAD` — a shadow is drawn into the canvas's own pixels, so a canvas cut to the blob's size clips the lift away silently |
| ↳ Home orb — the morph starts where the eye already is | ✅ n/a (CSS starts the animation with the class) | 🔬 | 🔬 | **Mobile-only bug, fixed v0.25.1, kept in v0.25.2.** Mobile holds the idle blob at the 0 % keyframe while the morph clock **free-ran from mount** — so connecting jumped the outline from the rest shape to wherever the clock had drifted, which if the timing fell badly meant landing straight on the 75 % frame's tight top-left corner. Reported from the phone as a vertex going out of proportion "right at the start". The clock now starts **with the connect, at zero**: the cycle is walked in order and the eye is led into that corner ~6 s later. ⚠️ **v0.25.1 also rounded the keyframes off (`excursion` 0.5) and v0.25.2 REVERTED that** — it removed one of the shapes the design is for in order to fix a moment. The 75 % frame really is the extreme (measured: 7.6 px outside a circle at the top-left, 7.8 px inside at the top-right, on a 150 px blob) and it stays. **An extreme that is arrived at wrongly is a timing bug** — sanding the extreme down hides the symptom and takes the design with it |
| **Limb (6-lead) exam** | ✅ | 🔬 | 🔬 | Full pipeline: LimbPrep → auto-armed monitor → 10 s capture → report, a **layout port of the web** (`.prep-stage` and `.limb-stage`), with the web's own guide photographs (`assets/guides/`) and copy verbatim from `en.ts`. Traces are the web's `2 × 3` `.lead-grid` with the web's drawing (3 s window, 12/60 px paper, 0.38 mV scale, window-mean centring). **Mobile-only:** the route is LANDSCAPE — declared on the `Stack.Screen`, never locked imperatively (v0.8.0) — and below a 500 pt stage every size is compacted so the content, not the chrome, gets the phone's height (v0.9.0; see the Phone-scale table below). **Not ported:** the 12-lead chain into the chest protocol, and saving to Scan History |
| **End-of-exam report** | ✅ | 🔬 | 🔬 | Same content and tokens as the web `EcgReport`, **deliberately restructured for the phone** (v0.11.0 — see the row below for why). Vector strips on 1 mm / 5 mm ECG paper at the clinical 25 mm/s · 10 mm/mV with the 1 mV calibration pulse and lead-II R-peak ticks; the measurement sheet (rate tiles · hexaxial axis dial · interval bars vs reference bands · amplitude table · quality) behind the second tab. Colours are the web's `--ecg-*` tokens **verbatim** — white paper, blue grid, navy trace (light) / `#0D1424` paper, green trace (dark). **Rotates back to PORTRAIT** — a document is read top to bottom, and sideways halves every strip. **Not ported:** print / Save-PDF (no browser print dialog on a phone; needs `expo-print`) |
| ↳ **Report layout — deliberate divergence from web** | 2 × A4 sheets, letterhead each | 🔬 | 🔬 | The web report is built for a printer, and the literal port read as a fax: the brand mark + 4 provenance fields **twice** in one scroll, and six **51 pt** slivers of paper (182 mm squeezed into 361 pt = 1.9 pt/mm, so a 1 mV R wave stood 19 pt tall). Per root `CLAUDE.md` §3.3 the brand is identical and the layout follows the platform, so: **one** letterhead · a summary card carrying the rate at 46 px + rhythm + provenance · full-width **sliding segmented tabs** (Waveform \| Measurements) instead of stacked pages · a **100 mm window** that scrolls horizontally through the full 10 s, giving **108 pt** bands and a **36 pt** millivolt (2×). All six leads share ONE scroll so they always show the same instant; lead labels are pinned outside it. The 25 mm/s · 10 mm/mV scale is untouched — rescaling the time axis to fit the screen is banned (`ecgPath.ts`), and truncating would have discarded 6 s of a clinical recording |
| ↳ Report — one continuous sheet | 6 separate `.ecg-svg-strip` cards | 🔬 | 🔬 | v0.12.0: the leads are drawn edge to edge with **no gap** inside one surface panel (`EcgStripSvg variant="channel"`), so the mm grid runs unbroken lead I → aVF, as a six-channel printout does. ⚠️ The band height must stay a **multiple of 5 mm** (it is 30) — otherwise band N's last grid line and band N+1's first do not coincide and every seam shows as mismatched squares |
| ↳ Report — glass action bar | ✅ n/a (web has a print button) | 🔬 | 🔬 | v0.12.0: the pinned bar is a `GlassSurface` and the document scrolls **under** it. Liquid Glass on iOS 26+, `dimezisBlurView` on Android. Deliberately **not** on the tab switcher: that sits on a flat background, and a material that refracts what is behind it has nothing to refract there |
| ↳ Report — axis dial | fixed 190 pt | 🔬 | 🔬 | v0.12.0: self-sizing, fills its container up to 340 pt (**1.74×**), with strokes/labels/radii scaled by **√** so it does not read as coarse. It is the only figure on the sheet that is *read* rather than looked up |
| Real frosted glass | ✅ (`backdrop-filter`) | 🔬 | 🔬 | `GlassSurface` atom. iOS 26+ uses Apple **Liquid Glass** (`expo-glass-effect`), which the web has no equivalent of; older iOS uses UIBlurEffect; Android needs `experimentalBlurMethod="dimezisBlurView"` or `expo-blur` does not blur **at all**. ⚠️ v0.19.2: the tint must be passed to BOTH materials — untinted Liquid Glass over a light page is clear, and every surface built from this atom was invisible on iOS 26 while correct on Android (see the trap table). The material also takes `colorScheme` from the app's theme, not the phone's. v0.24.0: optional `interactive` → Apple's `isInteractive`, **default off** — a surface you look *past* that lights up because a finger crossed it is noise, so only a surface that IS a control asks for it (today: the dock alone) |
| ↳ Confirmation dialogs | ✅ | 🔬 | 🔬 | **Deliberately NOT glass** (v0.19.2). A material is for a surface you look past; a confirmation is the one surface you must look at, right before something irreversible, so it is a solid card over the dimmed page on both platforms |
| ECG signal chain (DSP, Pan-Tompkins, report filter, analysis) | ✅ | ✅ | ✅ | **Now shared** in `CYPHIX_SHARED/src/ecg/`, consumed by mobile. ⚠️ The web still imports its own copy under `src/services/ecg/` — migrate it, and until then edit both |
| Live Scan (camera + ONNX pose) | ✅ | ⏳ | ⏳ | Needs camera + `onnxruntime-react-native`; geometry math must be copied verbatim from web `services/scan/` |
| Guided chest protocol (V1→V6 FSM) | ✅ | ⏳ | ⏳ | Depends on the scan engine landing first |
| **Tests (the test picker)** | ✅ | 🔬 | 🔬 | **v0.30.0 — was `⏳ shell only`.** The web `TestsPage` ported: image-led circles, the "Scheduled" badge, first-time explainer highlighting (read from the same bounded `useListRecordingsQuery` page History already has cached, so it costs no extra request), and the connect hint / demo link whose meaning follows device state exactly as `startLimb` does on the web. Copy is the web's `en.ts` verbatim; the circle artwork is the web's own `ecg-tests-limb.jpg` / `ecg-tests-chest.jpg`, copied into `assets/guides/` so a patient is never shown one illustration on the phone and another on the web |
| ↳ **Tests — ONE circle per screen, not a grid** | ✅ 3-up grid | 🔬 | 🔬 | **Deliberate divergence, v0.30.0, at the user's request.** The web's `.choice-grid` is `repeat(3, 1fr)`; on a 390 pt screen that makes each photograph a thumbnail, and the photograph IS the interface here — an elderly patient recognises "the watch on the wrist" long before they read "6 Limb Leads". So one circle owns the width (`min(58 vw, 34 vh, 264)`) and pages by **swipe or arrow**, with dots below. Two ways in on purpose: a patient who does not know to swipe never finds the second test, and unsteady hands cannot swipe reliably. ⚠️ **Nothing in the carousel mirrors under Hebrew** — RN only reverses a horizontal `ScrollView` when `I18nManager.isRTL` is set, which this app deliberately does not use (direction is per-component so the language can change without an app restart), so pages, arrows and dots are all physically left-to-right in every language. The arrows are absolutely positioned outside the scroller and are **told** the circle's diameter rather than measuring it — which is why pages are top-aligned |
| ↳ **Tests — two tests offered, not the web's three** | ✅ limb · chest · 12-lead | 🔬 | 🔬 | **Deliberate divergence, v0.30.0, at the user's request:** the phone offers **6 limb leads** or **the full 12**. Chest-only is dropped — it is not really a third way to measure so much as half of the 12-lead test, and three near-identical circles make the choice slower, not richer. Not a gap to be closed later unless asked |
| ↳ **Tests — 12-lead is shown but cannot be started** | ✅ starts (routes to `/measure/limb`) | ⏳ | ⏳ | Badged "Coming soon" with a line saying the chest part is on the web for now. **Deliberately NOT wired to the limb exam the way the web route is:** recording six leads under a label that says twelve is the one outcome worse than not offering it (root `CLAUDE.md` §2.3). Unblocks when the guided chest protocol lands — which needs the camera + ONNX scan engine, the two rows above this table's Live Scan entry. Its "Watch how" **stays enabled**: reading what a test involves is exactly what someone does while waiting to be able to do it |
| ↳ **Tests — explainer clips** | ✅ full-screen `<video>` modal | 🔬 | 🔬 | v0.30.0. `ExplainerVideoSheet` = `expo-video` inside the app's own `BottomSheet`, because a phone presents a thing you opened from the page you were reading — and will dismiss back to — as a sheet. The 6-limb clip is the web's own `6limb-tutorial.mp4`, **bundled** (2.7 MB); a test with no clip yet shows its still behind a "coming soon" badge, as the web does, and plays with no other change the moment a file is listed in `MEASUREMENT_GUIDE_VIDEO`. ⚠️ **`allowsFullscreen` is OFF on purpose** — iOS fullscreen video is presented by AVPlayerViewController, which manages its **own** orientation, and every route but the exam is declared `portrait_up` in `RootNavigator`; handing a second party the orientation API is the exact shape of the bug post-mortem'd at the top of that file. ⚠️ `expo-video` is a **native** module, so v0.30.0 is a REBUILD, not an OTA (mobile `CLAUDE.md` §5A.1) |
| **History (doctor)** | ✅ | 🔬 | 🔬 | v0.15.0. The whole web module: a LIST screen (`HistoryScreen`) and a STUDY screen (`StudyViewerScreen`), because choosing what to read and reading it are two jobs and a phone has one column. Data comes from `recordingApi` — endpoints mirrored 1:1 with web — over an **on-device store** until a server exists (see the row below). Every viewer tool is behind the same `viewerFeatures` → RBAC permission the web checks, and every read / annotate / export / delete is audit-logged |
| ↳ History — where the recordings come from | ✅ server (`httpBaseQuery`) | 🟡 | 🟡 | `services/db/recordingStore.ts` (AsyncStorage) behind `services/api/localBaseQuery.ts`, selected by the same line web's `baseApi` carries: `ENV.hasBackend ? httpBaseQuery : localBaseQuery`. ⚠️ **v0.20.0: setting the API URL flips this line too** — the whole data layer, not just sign-in. The endpoints are 1:1 with web's (paths, tags, page size, base64 channel encoding), and a capture now files itself under the signed-in patient's real `linkedPatientId` instead of `Patient/MOCK-SELF`, which the server correctly answers 403 for. **Unverified on a device against the live server: 🔬 — this is the row to check first if History looks empty after connecting.** **Deliberate divergence in the storage SHAPE:** web keeps one localStorage array; mobile keeps an INDEX plus one payload key per recording, so `list()` never touches a waveform. `RecordingRepository.list()` promises exactly that, and on a ~6 MB AsyncStorage budget rewriting ~1.4 MB to save one note would make the promise decorative. Cap is 40 recordings, same as web |
| ↳ History — a capture files itself | ✅ `useSaveRecording` | 🔬 | 🔬 | Ported. Fires once the report exists, idempotent on the capture's timestamp (a ref, not state — the report route also rotates, which re-renders), and the outcome is **shown on the report**: saving / saved / a named failure. A recording that did not persist must never look like one that did |
| ↳ History — the trace | ✅ `EcgReviewStrip` (one SVG per lead) | 🔬 | 🔬 | **TILED**: each lead is a row of 65 mm `<Svg>` panes (`EcgReviewStrip`). react-native-svg gives each `<Svg>` ONE native texture and past ~4 096 px draws **nothing at all** — a blank lead, not a clipped one — so a single-SVG port would cap the zoom (that is what `MAX_STRIP_PX` costs the report). ⚠️ `TILE_MM` must stay a **multiple of 5**: at 64 the major grid resets at every seam. Each tile's path starts ONE SAMPLE early so the trace crosses seams unbroken. Scale frozen at 25 mm/s · 10 mm/mV; all leads in ONE scroll so they always show the same instant |
| ↳ History — calipers | ✅ hover → click → click → click | 🔬 | 🔬 | **Deliberate interaction divergence.** A finger cannot hover, and its ~9 mm contact patch is 360 ms of paper — wider than a QRS — so the web's model measures what you cannot see. Two crosshairs are placed into the visible window the moment the tool opens and are DRAGGED, each by the finger's *delta* (never to its absolute position), so the point stays visible beside the hand. Same Δt / implied-BPM / ΔmV readout — in the CHROME as of v0.16.0, not floating on the trace, where it covered the deflections whose distance it reports. Precision comes from zooming in; the scale is never rescaled |
| ↳ History — mark / cursor / compare | ✅ | 🔬 | 🔬 | Ported as MODES (one tool at a time; a finger has no modifier keys). Marks: tap to label, tap to edit, drag to move — committed once on release, in-place PATCH. Cursors: tap to drop across every lead, drag anywhere along the line to move it, tap it to remove. Compare: all three alignments (beat / fiducial warp / manual), `alignByFiducials` shared. **Divergence:** the ghost drag is its own mode, because a drag that both pans the paper and moves the ghost cannot be either. ⚠️ v0.15.0 shipped ALL of this non-functional — see the gesture trap below |
| ↳ History — R-peak ticks | ✅ every lead | 🔬 | 🔬 | v0.16.0. Was lead II only, copied from the REPORT — where it is right, because a printed sheet marks the one rhythm strip the rate was computed from and marking six would imply six independent detections. A *tool* is the opposite case: the web marks every lead so a reader sees which beats the rate came from wherever they happen to be looking |
| ↳ History — zoom | ✅ Ctrl+wheel + − / + | 🔬 | 🔬 | **− / + and Fit only; no pinch.** Zoom re-renders six tiled vector sheets, which is not a 60 fps operation on the JS thread, and a pinch that stutters reads as a broken control. The web's own buttons do the same job in the same discrete steps. Pinch with a deferred re-render is the obvious follow-up. **Fit is a HEIGHT calculation** (`fitWindowMm`): fitting the whole recording to the WIDTH gives 99 pt bands in a 353 pt sheet, so four of six leads sit below the fold |
| ↳ History — **full screen** | ✅ (a button; the page is already landscape-ish) | 🔬 | 🔬 | v0.16.0, **and it ROTATES** — the part a phone adds. A six-lead ECG is 259 × 180 mm, a landscape shape, so portrait can never give it 90 % of the display however hard the chrome is compacted. Opens fitted to all six leads. Orientation goes through `navigation.setOptions`, never `lockAsync` (see the RootNavigator post-mortem). **v0.17.0 fixed three faults found on device:** the bar floated OVER the paper (now in flow), the screen was full-bleed so the landscape Dynamic Island cut the first ~50 pt of every lead (now inset on all four edges), and there was no labelled way out (now the first control in the bar) |
| ↳ History — sheets & dialogs | ✅ CSS popovers | 🔬 | 🔬 | One shared `BottomSheet`: a scrim that **blurs** the page rather than dimming it, a `GlassSurface` panel (Liquid Glass on iOS 26+, `dimezisBlurView` on Android), 28 pt corners, grabber, hairline, lift. Fields and chips inside are a translucent wash of the panel, not solid blocks on it. `ConfirmDialog` matches — the study being deleted stays recognisable behind it. ⚠️ **v0.17.0 shipped every line of that and none of it rendered**: the sheets were in a `Modal`, which is its own window, so the blur had nothing to sample and fell back to the flat grey rectangle it was written to replace. v0.18.0 presents them **in tree** through `OverlayLayer`, which also had to take over the Android back button, the keyboard and the mount lifecycle — see the trap below |
| ↳ **Sheets are DRAGGABLE — the grabber was a picture (v0.83.0)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | Reported as two things — “כל הסליידרים … עולים בריצוד ואין לי יכולת להחזיק את הפס … זה או נפתח או נסגר” — and ★ **the second half is the diagnosis of the first: there was no gesture.** The grabber was a 36×5 rounded `View`, a PICTURE of a handle drawn since `BottomSheet` v2.0.0 announcing an affordance nothing implemented (`ActionSheet`’s header has promised “dismissed by … dragging down” since v1 — fiction). Two states and a 240 ms timeline between them; an animation you cannot interrupt is the only thing on screen, so every dropped frame in it IS the experience — which is why v0.18.1’s duration cut changed nothing (“it’s not the speed, it just isn’t smooth”). A real pan now drives the same value the animation does, on the UI thread: tracks the finger, falls below 62 %, goes with a >750 px/s flick, returns on a <−420 px/s one, springs back **inheriting the release velocity**, and can be caught mid-rise. The scrim dims with it. Three structural fixes ride along: **Reanimated replaces `Animated`** (a finger cannot take a JS round trip per frame); ★ **the panel travels its OWN height, not the window’s** — a 380 pt sheet was flung 844 pt in 240 ms, compositing a full-width Liquid Glass surface off screen for most of them, and the window height was CORRECT when written until v1.2.0’s layout gate silently expired its reason; ★ **the layout gate is a ref, not state** — as state it rendered the layer, re-published into `OverlayPortal` and re-rendered the host on the exact frame the rise began. ⚠️ `IN_MS` 240→330 is NOT a smoothness fix — same perceived speed, half the distance. **Drag is on the HANDLE, not the panel**: most callers put a `ScrollView` inside it, and arbitration needs wiring at both ends (twelve callers) for a gesture the user named by its handle. Grabber + title are one ~45 pt full-width band; rendered spacing unchanged. ⚠️ **A context was tried first and was wrong in a way that compiles**: the provider lives inside `OverlayLayer`, so `BottomSheet` would have read it from ABOVE its own provider and got `null` — a handle that silently does not drag, i.e. the bug being fixed. Render prop instead. One file, all **twelve** sheets. **Web is `pending`**: its popovers have no drag either and need the same treatment |
| ↳ **Entrances replay on EVERY tab visit (v0.84.0)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | ★ The animation played once per session because it was keyed on MOUNT, and a bottom-tab screen mounts once and stays mounted for the life of the session — leaving a tab does not unmount it, which is what makes coming back instant. Focus, not mount, is the event that means “I am looking at this now”. New `hooks/useReplayOnFocus.ts`. ⚠️ **NOT `useIsFocused()`**: it calls `useNavigation()`, which THROWS where there is no navigator — the AUTH FLOW (`AuthGate` is in front of the navigator; `SuccessStep` uses `FadeUpView`), every SHEET (portalled to the app root, outside the navigator by design) and anything mounted above the navigator later. It would have crashed the sign-in screen on launch. Plain `useContext`; no navigator ⇒ mount-only, i.e. exactly the old behaviour. ★ **NOT a `focused` boolean in state**: that puts the reset ONE COMMIT after the screen is already visible, so the content is drawn in its finished state and then blanks — a flash, and this app has had exactly that reported (“the tab is glitchy, it appears for a split second”). The shared value is written straight from the navigation listener: same JS tick as the commit that made the screen visible, so Reanimated flushes it in the same frame, and no re-render per row (`FadeUpView` is per-row in History). History’s CASCADE had the same bug one level up (`Date.now() - mountedAt < 900`, a ref set at mount → stagger 0 on every later visit, so rows would replay all at once). Now state, because `renderCard` is memoised and FlatList re-renders no rows unless a dependency changes. ⚠️ **Home and Chat have NO entrance to replay** — they never had one; flagged, not invented, since Home is the patient-first one-big-button screen and animating it is a design decision. Replaying: Insights, History, Profile, Settings. **Web is `pending`**: its screens unmount on route change, so it has the opposite problem and needs its own look |
| ↳ **Boot splash — thinking-orb while the server wakes (v0.85.0)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | ★ **The `thinking-orbs` package’s own component is a WEB component and THROWS in RN** — `canvas.getContext('2d')`, `matchMedia` ×6, `document.visibilityState`, `devicePixelRatio`; its README says “Chrome, Safari and Firefox” in the first sentence. But the author split the geometry out for exactly this case and says so in `engine/registry.d.ts` (“the React Native port imports exactly these functions”), so `thinking-orbs/engine` is imported — **never the root entry** — and a new atom `ThinkingOrb` paints the finished frame with **Skia** (already a dep; 200 `drawCircle` in ONE native node rather than 200 RN/SVG nodes per frame). It derives nothing: frames arrive z-sorted and radius-clamped, array order is draw order — which keeps an upgrade a version bump, not a re-port. ✅ **Verified by BUNDLING, not by reading docs**: this is the first screen the app shows, so an unresolvable module is not a missing animation but an app that does not start. `expo export` output inspected — engine present (`rubik`, `connecting→web`), `getContext("2d")` **0**, `prefers-color-scheme` **0**; the lone `matchMedia` is Reanimated’s reduced-motion check and both `visibilityState` hits are RTK Query. Metro resolves the subpath via `unstable_enablePackageExports` (checked against the running config). ★ **Raised only after 1.5 s, and that is the design**: BootSplash’s own header argues the quiet ring is right because “a disk read is not an occasion” — kept, with the orb as what “earned” looks like. `AuthGate` holds this screen 900 ms on a healthy launch and up to **60 s** waiting on the server (`RECOVERY_TIMEOUT_MS`; a cold Render instance is ~a minute), so a healthy launch never sees it. It also makes the orb affordable: the geometry runs on the JS thread because workletising an IMPORTED function is impossible (Babel only transforms our own source), and by 1.5 s that thread is blocked on a socket. ⚠️ Bounded work — a fresh `SkPicture` per frame is native memory freed by GC; fine under a 60 s ceiling, not fine as ambient chrome. State `connecting`, painted in brand navy on white by mapping the package’s ink-depth language onto two colours. **Web is `pending`**: it can use the package’s real component as shipped, which is the one platform where that is true |
| ↳ **Boot orb is BOUNDED + SOUP register (v0.85.1)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | Egress question answered by measurement, not assertion: zero runtime deps, zero install scripts, and the 19 KB engine references exactly five globals — `Math` ×155, `Object` ×2, `Set`, `Map`, `Array`, and nothing else. No network, storage, dynamic-code, env or clock primitives; compiled into the bundle, so no CDN/licence check/telemetry. Structurally incapable of exposing patient data — it is never given any. ★ **But egress was not the real risk.** React has no partial failure and **the app had NO error boundary at all**, so on `BootSplash` — the FIRST screen — an ornament was as load-bearing as the ECG: a throw would be an app that does not start. True of v0.85.0 as shipped. New atom `FailSoft` wraps the orb, falling back to the `ActivityIndicator` that preceded it, so the worst case is now exactly the previous release. ⚠️ Scoped to DECORATION — clinical content must never go behind it, because content that silently fails to draw cannot be told apart from content that was never there. `SOUP.md` (new) identifies all 41 direct dependencies per IEC 62304 §8.1.2 and fully evaluates `thinking-orbs` per §7.1.2. ⚠️ Blunt about the gap it exposes: the other 40 have had no risk evaluation, several of them (`expo-secure-store`, `expo-local-authentication`, `expo-updates`, Skia, Reanimated) far closer to patient safety than the ornament that prompted it — **pre-existing, now visible**. Not a regulatory submission. **Web is `pending`**: it has no error boundary either, and shares the SOUP obligation |
| ↳ **Boot orb actually shows (v0.86.0)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | Reported as “I only see the old circle under Cyphix” — not a rendering bug, **my threshold**. v0.85.0 raised the orb only after 1.5 s, reasoning (from BootSplash’s own header) that a disk read “is not an occasion”; `AuthGate`’s minimum splash is **`SPLASH_MS = 900`**, so the screen was gone 600 ms before the timer fired and the orb **was never mounted on a healthy launch** — nor does a signed-in user with a stored principal reach the long `recovering` branch. A feature shipped in a state where it could hardly ever run, with three paragraphs defending the number that did it. Threshold deleted; entrance 520 → 260 ms, because it was still fading in when a 900 ms splash ended. ★ **Second bug: the two failures were indistinguishable.** v0.85.1 used the OLD RING as `FailSoft`’s fallback, so a crashed orb and an unmounted one looked identical — the v0.82.0 lesson (a tool that records one of two outcomes cannot distinguish them) repeated one release later. Now the ring is not a legitimate state, so seeing one IS the failure signal; and `FailSoft` records what it caught, shown in **Settings › About → “Render fallback”**, a row that exists only when something fell back. **Web is `pending`** |
| ↳ **Boot orb — the one actually asked for (v0.87.0)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | ★ **I picked the state by MEANING and never asked.** The package ships NINE animations; v0.85.0 used `connecting` because the name matched what the app does (waiting on a server). Nobody requested it — the ask came with a link to a live demo showing one specific animation that had been SEEN. The result was the opposite end of the set, not a near miss: `connecting` → `web` is **48 dots + 82 LINES**, a sparse wireframe; `composing` → `ribbon` is **566 dots, no lines**, a dense undulating sash. **Settled by rendering, not by reading names**: the state names describe an AGENT’S ACTIVITY, not a shape, so re-reading them answers nothing. The engine is pure `Math` with zero deps, so it runs in plain Node — all nine rasterised from the same `MODE_FRAMES` the app calls and compared against frames cut from the user’s screen recording (HEVC; the local ffmpeg is a stub that cannot decode it, so a real decoder was fetched). `ribbon` is the only match. ⚠️ **Do not re-pick from the state names** — that IS the mistake; reproduce the nine renders instead. **Web is `pending`** |
| ↳ **History pull-to-refresh spins the orb (v0.88.0)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | The hard part was already solved in v0.58.x: a `RefreshControl`’s spinner sits at the top of the SCROLL VIEW (here, behind a ~180 pt frosted header, spinning invisibly) and `progressViewOffset` is **not dependable on iOS** — RN rewrites `UIRefreshControl`’s frame from `layoutSubviews` and its own source warns that breaks `ContentInset`. So the control keeps only the pull gesture and a **36 pt badge this screen draws itself** is the visible part; this release swaps that badge’s `ActivityIndicator` for the orb, native control untouched. ★ **`design={20}` is the craft here.** The package ships TWO designs, not one scalable one — 64 is 566 dots at radius ×0.395 and scaled into a 26 pt badge every dot lands at a fraction of a pixel (a grey smudge); 20 is 208 dots at ×1.011, sparser and fatter so it survives being small. **Both rendered at 26 pt and LOOKED AT** (the engine runs in plain Node), not reasoned about. `ThinkingOrb` takes `design` now; callers pick by FOOTPRINT — 20 up to ~40 pt, 64 above — never by preference. Bounded in `FailSoft` with the same `ActivityIndicator` fallback: a refresh spinner must not be able to take down the History tab. ⚠️ **`ProfileScreen` still uses the plain native `RefreshControl`** — no self-drawn badge to swap, so matching it is the v0.58.x exercise again. Flagged, not half-done. **Web is `pending`** |
| ↳ **Profile refreshes with the orb — and its ring was under the notch (v0.89.0)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | ★ **Not only cosmetic.** Profile still used the plain native `RefreshControl`, whose indicator sits at the top of the SCROLL VIEW — and this page’s content starts at `insets.top + 12`, so the ring was landing in the **status-bar strip, under the notch**. The same defect as History from the opposite cause: there a frosted header covered it, here there is no header and the scroll view begins above the safe area. `progressViewOffset` is not dependable on iOS (post-mortem in History’s refresh block; it cost a release). Native indicator made transparent — `tintColor` for iOS, `colors` for Android — keeping only the pull gesture. ★ **The slot already existed**: the identity header row ends in a status position that was showing an `ActivityIndicator` on `isLoading`. The orb goes there, INSIDE the layout, so nothing is positioned absolutely against a header of unknown height — the work History had to do. Gated on `isFetching` (a superset of `isLoading`), so ONE indicator covers the first load and every refresh instead of a native ring for one and a spinner for the other; `isLoading` is no longer destructured. `design={20}` + `FailSoft` with the `ActivityIndicator` fallback, as History. **Web is `pending`** |
| ↳ **Profile orb sits ABOVE the identity (v0.89.1)** | ⏳ pending | 🔬 needs-device-verify | 🔬 needs-device-verify | v0.89.0 used the header row’s trailing status slot **because it already existed** — a reason to notice a place, not to use it. On the same line as the name it read as attached to the PERSON rather than as the state of the PAGE. Reusing existing furniture is only tidy when the furniture means the right thing. It now has its own centred strip directly above the identity block. ★ **The strip is reserved whether or not anything is in it**: rendering it only while fetching would shove the page down ~32 pt the moment a refresh began, and `isFetching` goes true on EVERY arrival at the tab — exactly when someone is looking. A fixed strip costs a little air on a page that scrolls anyway and can never jump. `headerBlock` wraps the strip and the identity as ONE child so `page`’s 18 pt gap applies to the block instead of opening a second gap under the indicator. Outside `FadeUpView` on purpose — that entrance is for content arriving, not for a status indicator that would fade up on every tab visit. **Web is `pending`** |
| ↳ ★ **ONE loading screen at launch (v0.94.0)** | ⏳ pending | 🔬 needs-iOS-verify | 🔬 needs-iOS-verify | **Reported with screenshots: “small glitches, but they make my app look unprofessional”.** A cold start showed FOUR loading states in a row — the splash, then the app, then a “connecting” line under the status bar while the server woke, then a spinner on History, then a gap on Profile. Each was individually defensible and together they read as an app that could not tell whether it had finished opening. `useBootWarmup` now holds the splash until (a) `auth.sessionMode` is `live` — evidence from the transport, not a guess — and (b) the first `runSync` delta has landed, so History opens on the list it would have shown a second later anyway. **★ 15 s ceiling, then it opens OFFLINE on purpose**, in the user’s own words. A Render free-tier container takes ~50 s to wake, so that ceiling **will** be hit on the first launch of the day: the app comes up on the device’s mirror, `ConnectionStrip` says offline, and `AuthGate`’s backoff keeps knocking. Deliberately NOT the 60 s `RECOVERY_TIMEOUT_MS` beside it — that minute buys not showing a sign-in door to somebody already signed in, this one buys a nicer first paint, and the fallback here is the working app. Adds `auth.revalidatedOnce`, because `revalidating === false` is true both before the question is asked and after it is answered, so a phone in airplane mode would otherwise burn the whole 15 s on a question already answered. Also adds SyncProvider trigger ② (session goes live after being offline): the reconnection is now the normal case and **nothing used to act on it**. Not armed behind the app lock — the revalidation is deliberately not sent from behind a screen the patient has not passed, so it would stall until the ceiling; and after an unlock a splash would be a second door. ⚠️ Typechecks and bundles; the 15 s path is timing behaviour on a real cold container and nobody has watched it on a phone |
| ↳ ★ **The pages that moved on their own — History + Profile (v0.94.0)** | ➕ web has no `RefreshControl`, so no equivalent defect | 🔬 needs-iOS-verify | 🔬 needs-iOS-verify | **One root cause, two screens, three symptoms in the report.** Both handed a BACKGROUND fetch to a `RefreshControl` — History `sync.phase === 'syncing' || list.isFetching`, Profile `isFetching`. On iOS `refreshing` is not “draw a spinner”, it is **“enter the refreshing state”**: `UIRefreshControl` grows the scroll view’s top inset ~80 pt and the content goes down with it. So (1) History’s list dropped away from its title with nobody touching it and did not reliably come back, (2) Profile opened from halfway down the page “out of nowhere”, and (3) the native ring drew under the notch — which is why it was **only ever caught in a screenshot**. Both controls are driven by the GESTURE now (`pulling`, set in `onRefresh`, cleared when the work’s promise settles — `usePatientCard.refetch` returns one for that reason). Background work is silent on both screens; it is reported by the boot splash and the connection strip instead. History’s native ring is suppressed on BOTH platforms (`tintColor` + `colors`, since iOS and Android read different props) and its orb is drawn only while pulling, moved from the title row to the top of the screen — the pull has cleared room there, and centred on the title row it landed on the word “History” itself. ⚠️ **A stale comment cost real time here**: History’s `RefreshControl` block claimed the native ring “spun invisibly behind a frosted header ~180 pt tall”. True in v0.58.0; false from v0.70.0, when that header stopped being a bar. The note outlived the thing it described by a dozen releases and is corrected in place. ⚠️ Typechecks and bundles; scroll-inset behaviour is exactly the class of bug those prove nothing about |
| ↳ **History — compare, as its own sheet** | ✅ inline panel | 🔬 | 🔬 | v0.18.0, after being reported unusable **twice**. Comparison was three rows in the middle of the filters sheet — a list of settings for a feature nothing had explained. It is now its own toolbar tool and its own `CompareSheet`, which opens with **what the grey trace is** plus a two-swatch legend (painted from the colour the ghost is actually drawn in), then the study picker, then the alignment modes as a segmented control with the selected mode's **consequence printed underneath** ("never measure off a warped trace" is not a footnote). Moving the ghost happens **on the paper**: it is draggable everywhere in ghost mode, with the labelled handle over it saying so, and the offset reads out on the status line while dragging. ⚠️ v0.18.0 briefly replaced that drag with an arrow pad in this sheet and was told to put it back the same day — see the trap below. The sheet's last action closes it and hands the trace over draggable |
| ↳ History — sticky measurements header | ✅ (page scrolls, sidebar is fixed) | 🔬 | 🔬 | v0.17.0. A glass bar the analysis genuinely passes **under**, earning a hairline only after 6 pt of scroll. An always-drawn edge is a boundary between nothing and nothing; an opaque static header just eats the trace |
| ↳ History — toolbar | ✅ labelled text chips | 🔬 | 🔬 | **Icons**, 44 × 38 with a 44 pt `hitSlop`. v0.15.0's labelled 44 pt chips plus a headline, a meta line, tabs and a hint took ~a third of the screen on the module whose whole subject is a trace. The two costs are paid explicitly: every icon carries an `accessibilityLabel`, and anything without an honest pictogram — the filter stages — went into a **labelled ⋯ sheet** (`ActionSheet` rows can now be toggles) rather than becoming a guessed symbol. v0.18.0 promotes **Compare** to its own icon: it is a tool, and being three rows down someone else's sheet was most of why it read as unusable. Seven tools now, computing to 364 pt — an eighth means shrinking `ToolToggle`, not the gap |
| ↳ History — export | ✅ CSV · EDF+ · `window.print()` | 🔬 | 🔬 | Builders (`buildRecordingCsv`, `buildRecordingEdf`, `parseEcgCsv`) moved into `CYPHIX_SHARED`, so a file exported on either platform is the same bytes. Delivery differs and must: web clicks a hidden `<a download>`, mobile writes to the cache and opens the **OS share sheet** — a phone has no downloads folder. iOS UTIs are set explicitly or a CSV offers no destinations |
| ↳ History — PDF | ✅ `window.print()` | 🔬 | 🔬 | **New work, not a port.** There is no print dialog and no rendered page to snapshot on a phone, so `services/export/recordingPdf.ts` BUILDS the A4 sheet from the same shared `buildEcgPath` / `buildEcgGrid` the screen uses — a ruler on the printout agrees with the web print. ~~Paginates a 10 s capture across consecutive six-lead sheets~~ **(stale since v0.49: ONE sheet, window stated against the total — see the dedicated pagination row below).** Always the LIGHT paper tokens: the dark theme's green-on-navy is a screen affordance and prints as a navy rectangle |
| ↳ **PDF — the patient is finally ON the report (v0.56.0)** | ✅ (web prints the page it shows) | 🔬 | 🔬 | **Bug, not polish: `patientName` + `ScreeningContext` existed in the builder since v0.48 and were never passed.** The letterhead named nobody, the ID grid printed "Patient —", and the PDF screened without sex/age — so the paper could DISAGREE with the Findings tab it was exported from. `useReportContext` attaches both under the same "provably theirs" guard as the screen (`patientContext.ts`); a clinician exporting someone else's study gets an anonymous, conservatively-thresholded report, never a mislabelled one |
| ↳ **PDF — the colour pass (v0.56.0)** | — (web report is its own design) | 🔬 | HW | *"Not colourful enough, no added value."* Full-bleed navy letterhead band with the WHITE wordmark on every page (negative margins + matching padding: flow height unchanged, `assertFits` untouched), blue section rules + footer keylines, BRAND header rows + soft blue zebra on the ruled tables, blue panels under every figure, the verdict statement on its level's tint, the ID grid as a tinted band. **v0.49 and v0.50 rulings stand:** green stays reference-band-only, no pills, no rounded cards — a lab report in the issuer's colour, not the app on paper. HW: how the band and tints render on an actual printer is only knowable on paper |
| ↳ PDF — content cut + the quality table (v0.56.0) | — | 🔬 | 🔬 | The layperson "how to read" tutorial is GONE (wrong reader, and its 4th sentence had been false since v0.49 — it promised continuation sheets). In its place the SIGNAL QUALITY table finally prints: SQI, analysed window, beats, RR range, ectopy burden — declared in v0.48's labels and never rendered. A SIMULATED report now carries the ID grid on its statistics page (it used to have none anywhere) |
| ↳ **PDF — in-app PREVIEW (v0.56.0)** ⚠️ native dep | ✅ (the browser page IS the preview) | 🔬 | 🔬 | `ReportPreviewScreen` renders the EXACT HTML `printToFileAsync` receives in a `react-native-webview` (13.15.0) with an A4 viewport + pinch zoom — one source of truth, previewed and printed; a third hand-kept page layout was rejected. ⋯ menu leads with "View report"; Share PDF is the preview's one action. **⚠️ Requires the 0.35.0 binary — BUILT AND SUBMITTED:** build **8** (`69dbf640`, runtime 0.35.0, commit `284acab`) uploaded to App Store Connect 14 Aug 2026; TestFlight after Apple's processing. `OptionalWebView` resolves behind a require guard (the cyphix-ble pattern), so a 0.34.0 binary falls back to the direct share — no dead menu entries, no OTA crash. ⚠️ Every future `eas update` must now target runtime **0.35.0**; the v0.53–0.55 OTA sits on 0.34.0 and will not reach the new binary (it carries that code compiled in) |
| ↳ PDF — export progress overlay (v0.56.0) | — (browser print dialog is its own feedback) | 🔬 | 🔬 | `ExportOverlay`: the DSP + 43 rules + print engine run seconds on the JS thread and used to be fire-and-forget — the sheet closed and NOTHING happened until the share sheet appeared, which reads as a dead tap. A blocking scrim now says "Preparing the report…" (blocking is honest: the thread genuinely is) |
| ↳ PDF — `scripts/verify-pdf.ts` (v0.56.0) | — | ✅ | ✅ | The v0.48 "nine cases" Node harness, committed and repeatable (`npx tsx scripts/verify-pdf.ts`): build never throws, footer page counts match rendered pages, letterhead per page, ID grid everywhere incl. simulated, quality table present, patient name present iff passed, no unsized SVGs / NaN / placeholders. ✅ because it runs in Node — the printed sheet itself stays HW |
| ↳ History — import CSV | ✅ (in the study's Actions menu) | 🔬 | 🔬 | Same shared parser, so both platforms accept and reject exactly the same files. **Placed on the LIST, not inside a study** — it CREATES a study, and an action that adds a row belongs where the rows are. ⚠️ The importer's `problem`/`remedy` text is untranslated English on BOTH platforms on purpose: it names column headers and units, and a localised paraphrase of "rename the columns to I and II" says less |
| ↳ History — RBAC + audit | ✅ | 🔬 | 🔬 | `types/rbac.ts` and `features/history/viewerFeatures.ts` ported whole, rationale comments included. ⚠️ The permission matrix now exists in THREE places (web, mobile, server `policy/permissions.ts`) — a change to one is a change to all, or a tool opens on one platform and not another. `features/auth/useCurrentUser.ts` is the single stand-in principal (`clinician`, so the doctor tools are demoable); auth lands as a swap of that one file |
| ↳ **Settings — Preview as role** | ✅ | 🔬 | 🔬 | **v0.28.0 — was listed as NOT ported**, correctly, because until connected mode there was no real role to switch away from. `auth.debugRole` is applied in ONE place — `useCurrentUser`, where the principal resolves — so every `can()` and gate follows it without knowing it exists. ★ **It grants nothing, and the row says so on screen:** the server authorises against the session's REAL role, so previewing `admin` on a patient account draws the admin buttons and each request behind them returns 403. That is the demonstration, not a defect — and the reason it ships visible rather than behind a build flag. Deliberate limits: `id` / `linkedPatientId` are NEVER swapped (that would read and write a different patient's record — not a preview, a data-integrity bug); the override clears on sign-out so it cannot apply to whoever signs in next; `guest` is not offered (the signed-out principal, previewed from a screen that only exists behind sign-in, is a shell with no way out); tapping the active role clears it, and the real role is printed beneath the picker while a preview is on |
| ↳ Preview as role — **the default is now `admin`, not `null`** | ➖ web still starts on the real role | 🔬 | 🔬 | **Deliberate divergence from web, v0.69.0, at the user's instruction** ("make the default user admin, instead of me going into Settings to change it myself every time"). `authSlice` boots `debugRole` at `DEFAULT_PREVIEW_ROLE` (`config/featureFlags.ts`), and sign-out / a server-rejected session reset it to that default rather than to `null`. **Grants nothing** — unchanged from the row above: the server authorises the session's real role, which for a registered account is `patient` (`CYPHIX_SERVER/src/routes/auth.ts:118`). ★ The consequence that had to be checked: an admin holds `history:read`, so History/Insights stop sending a `patientId` and call `GET /recordings` — **not** a 403 for a patient, because the server's `listFor` falls through to `assertCanOrSelf(…, {history:read:self}, req.user.patientId)` and then scopes with `allowedPatientIds()`. Same studies, different URL. An empty History by default would have passed typecheck, both bundles and expo-doctor. Set the constant to `null` to restore the old behaviour. **Not ported to web**, which has no such per-launch chore |
| ↳ History — **not** ported | — | — | — | Full-screen mode (the study screen already is full screen — the web's button exists to escape a sidebar mobile does not have) · double-click-to-magnify (replaced by tap-to-focus, which the web also has) · `Preview as role`, so the patient-role gating cannot be demonstrated on device yet |
| **Study viewer → Interpretation (screening)** | ⏳ **pending** | 🔬 | 🔬 | **v0.46.0 — MOBILE-FIRST, at the user's request; the web has no interpretation tab and this row is the Cross-Platform Rule's IOU** (root `CLAUDE.md` §1). A third tab beside Waveform \| Measurements, resolving 43 rules to one of four answers — *no abnormal finding / worth showing a doctor / get medical help now / could not be read* — with the action underneath and a breathing mark in the level's colour. **All the maths is in `CYPHIX_SHARED`** (`types/ecgScreening.ts` + `ecg/ecgScreening.ts`) with zero platform imports, so the web port is a UI job and the server can run the identical function later. The web already carries `ecgAnalysis.ts`, which is everything the engine reads, so the port needs no new signal code |
| ↳ Screening — ★ `margin`: how far past the threshold, and the borderline demotion | ⏳ | 🔬 | 🔬 | **v0.47.0, from a user report that was a BUG report, not a design one.** A healthy person saw `Largest QRS +0.48 mV / Threshold 0.50 mV` — 4 % over — draw the same amber verdict as a genuine finding, and read it as a statement about their heart. The engine had no way to express DEGREE. Every rule now returns `margin` (0 = on the line, 1 = unambiguous, computed as `(value − threshold) / (decisive − threshold)`); below `BORDERLINE_MARGIN` (0.15) a finding is `borderline` — still listed, still explained, still in the report, and it **does not raise the verdict**. ⚠️ **The fix alone shipped a worse bug than the one it fixed:** validation caught a QTc of **515 ms** demoted to borderline and returning a **green** verdict. Silencing an urgent finding is the OPPOSITE error to over-calling a benign one, not a milder version of it, and the two do not cost the same. The demotion is now asymmetric — `attention` can be demoted, `urgent` never can (`runner.ts`) |
| ↳ Screening — one file per disease (43 files) | ⏳ | 🔬 | 🔬 | **v0.47.0, at the user's instruction.** Was one 900-line function with 43 inline rule calls. Now `shared/ecg/screening/<category>/<disease>.ts`, each a declarative `ScreeningRule`: id, level, confidence, **published citation**, thresholds as named constants, and an `evaluate` returning `null` (could not run) / `false` (ran, did not fire) / `RuleHit`. Three-way return rather than a boolean because "41 of 43 checks ran" is what makes a clear result mean anything, and a boolean cannot tell a clean recording from an unreadable one. `registry.ts` is the only place a rule is registered and `RULE_COUNT` is derived from the array, so the denominator cannot go stale. **The split changed zero results:** 20/20 regression cases still pass and the healthy-adult clear rate went 87.0 % → 90.4 % purely from the borderline demotion |
| ↳ Screening — the "Why?" sheet | ⏳ | 🔬 | 🔬 | **v0.47.0.** *"I have no idea what you are talking about… maybe a button that explains it like to a small child, with drawings and proof from my own measurement."* Every finding card opens `WhyFindingSheet`: (1) the patient's **own representative beat** with the segment the rule measured shaded — the window comes from the rule's declared `focus`, never from a switch on finding id, so a UI can't drift from what the rule actually looked at; a `rhythm` finding gets a 5 s strip with R ticks instead, because a pause is invisible inside one complex; (2) `RangeBar` — their number against the typical band, which says "barely" without the word; (3) the cause in plain language (43 × 2 `scrCause_*` strings, ordinary explanation before the serious one); (4) the consequence and the **published criterion**. A stock heart diagram was rejected: it explains the concept and proves nothing about this recording |
| ↳ Screening — patient-scale redesign + the truncated tabs | ⏳ | 🔬 | 🔬 | **v0.47.0.** Tabs: three segments on a 390 pt screen give ~120 pt each and both labels exceed 100 pt at 14 pt bold, so "Measurem… Interpretat…". `SegmentedTabs` now sets `adjustsFontSizeToFit` above two options — per label and per LANGUAGE, so Hebrew's shorter words are not shrunk to match English's. Statistics moved off `MetricTile` (the REPORT's dense bordered table atom, six to a screen — reads as a spreadsheet) onto `StatCard`: 30 pt value, inset card, progress track where the number is a fraction. Section headings 19 pt sentence case. Raw evidence figures moved from the card into the Why sheet — a doctor still has them, a frightened person no longer meets them first |
| ↳ Screening — ★ history row VERDICT (was: "dot not built") | ⏳ | 🔬 | 🔬 | **v0.53.0 — built, as a device-side digest cache.** The two constraints this row used to document still hold and shaped the build: the 6-rule summary shortcut stays **rejected** (it would disagree with the detail screen — the pill runs the FULL `screenLimbEcg`), and per-visit re-derivation stays banned — the cost is paid ONCE per immutable study by `services/db/studyDigestCache.ts` (the `templateCache` design: one heavy entry, `DIGEST_VERSION` gate, PINNED `DIGEST_FILTERS`, staged writes, pruned on delete), backfilled off the render path by `useStudyDigests` (the `useEcgIdentity` pattern: after interactions, one study at a time, macrotask yields, `subscribe: false`, visible progress). Honesty rules bind unchanged: simulated → no verdict, SIMULATION chip in the pill slot; sex/age only when the study is provably the active patient's — that rule now lives ONCE in `features/history/patientContext.ts`, imported by `useScreening` and the backfill, with each digest recording its `ctxKey` so a late-loading card invalidates exactly once. **The server-column version remains the right end state for WEB parity** (web has no digest cache); when it lands this cache becomes a read-through |
| ↳ History — row waveform PREVIEW (4 s, Kardia-style) | ⏳ | 🔬 | 🔬 | **v0.53.0 — mobile-first; the web list is a dropdown of one-line rows and has nothing like it.** `EcgMiniPreview`: ~4 s of digest-cached filtered lead II at a FIXED time scale filling the card width, 44 pt, second-tick hairlines only. **Deliberately not ECG paper** — no grid, no calibration pulse, no mm truth — so recognition and measurement stay different things; `EcgStripSvg` remains the only component allowed to look like paper. This ANSWERS StudyCard v1's argued no-thumbnail decision rather than ignoring it: 10 s in 40 pt is indeed unreadable, a 4 s window is not, and the clinical statement is the verdict beside it. Placeholders reserve both slots so cards never reflow as digests arrive |
| ↳ ⚠️ **History — pull-to-refresh spinner hidden BY the glass header (v0.58.3 → fixed v0.58.4)** | ✅ | 🔬 | 🔬 | A regression v0.58.0 introduced and no local check could catch: a refresh indicator sits at the top of the SCROLL VIEW, which is now behind a ~180 pt frosted bar, so it span there invisibly and the pull read as "stuck at a height then released". ⚠️ **v0.58.3's `progressViewOffset` did NOT fix it, and the lesson is about how I checked**: I confirmed the prop reaches the iOS native view and stopped — without reading that RN implements it by REWRITING the UIRefreshControl's frame from `layoutSubviews` through a converging coordinate conversion, in a file that warns "setting the frame breaks integration with ContentInset". Reaching the native layer is not the same as working. **v0.58.4:** the `RefreshControl` keeps only the pull gesture and the `refreshing` state, its indicator stays behind the glass (invisible, harmless), and the screen draws its OWN badge at a position it owns — one indicator, both platforms, no native quirk in the path. **Rule for the next floating header: anything the OS positions relative to the scroll view — refresh indicators, scroll bars, keyboard avoidance — is now in the wrong place, and the fix is usually to own the placement rather than to offset theirs** |
| ↳ ⚠️ **ECG ID — the builder's touch handler does not exist while the tab is hidden (v0.58.7)** | — | 🔬 | 🔬 | **Fifth report of the same route (drag it, leave for Studies, return, dead), and the first fix that is not a new candidate.** v0.58.6's own note already contained the answer and then acted on the weaker half of it: *"the caliper already had this property for free — its detector unmounts entirely when `measurable` goes false"*. `BeatSignature` crosses the **identical** boundary, in the **same pane**, behind the **same `active` prop**, and has never once been reported dead, because `if (!gesture) return sheet` means it has **no `GestureDetector` in the tree at all** while Insights is hidden. The builder was the ONLY control keeping a live detector inside a `display: none` subtree — a native view History hides, Fabric marks hidden and Yoga lays out at zero — and every fix since v0.58.2 was an attempt to repair that handler after the fact (new gesture object → width back → stop queueing samples → remount on return). It is now mounted BY `enabled` exactly as the caliper's is: absent off show, and constructed fresh in the **same commit that reveals the pane**, over a freshly-mounted track whose `onLayout` reports a real visible width. `visitId` is removed — it rebuilt one effect-tick after the return, racing RNGH's re-attach, and left the handler live for the whole hidden period. ⚠️ "Built once" still holds: `gesture` memoises on `settle` alone and `enabled` changes only on a tab tap. **Rule: a gesture handler must not be left mounted inside a pane hidden with `display: none` — mount it WITH the pane, not across it.** ⚠️ Superseded v0.58.6 before it ever shipped |
| ↳ ⚠️ **ECG ID — the builder is remounted on every return (v0.58.6, superseded by v0.58.7)** | — | 🔬 | 🔬 | **Three reports, three real causes, three correct fixes, and it came back after each one** — always on the same route: drag it, leave the tab, return. v0.58.2 (gesture object rebuilt mid-drag), v0.58.4 (`onLayout` width of zero), v0.58.5 (pointer samples queued into a saturated JS thread). What they share is not a mechanism but a **shape**: something crosses the hide/show boundary in a state nothing on a Windows machine can observe, and naming candidates one at a time — with the phone as the only adjudicator — is a bad loop. So the invariant replaces the diagnosis: coming back on show **remounts the `GestureDetector` with a gesture object of its own** (fresh native handler, fresh `onLayout`, no half-finished interaction, no inherited width). ⚠️ This does NOT weaken "built once" — that rule forbids reconfiguring *during* a drag, and `enabled` only changes on a tab tap, which cannot happen with a finger on the track. The caliper already had this property for free (its detector unmounts when `measurable` goes false); the builder now has it on purpose rather than by accident. **Rule: a control kept mounted behind a hidden pane should be rebuilt when it comes back, not trusted to have survived.** |
| ↳ ⚠️ **ECG ID — the haptics outlived the tab (v0.58.5)** | — | 🔬 | 🔬 | Reported as *"I go back to Studies and I STILL FEEL the vibration from Insights, and then Insights doesn't work again."* ★ **The obvious theory was wrong and I checked it instead of shipping it:** a hidden pane is not stealing touches — RN sets `self.hidden` for `display: none` (`UIView+ComponentViewProtocol.mm`) and a hidden `UIView` is never returned by `hitTest:`. **It was a QUEUE, and the buzz was the tail of a drag already finished.** Two compounding causes: (1) `useEcgIdentity` returned a **bare object literal**, so `view` was new every render — and `EcgIdentityPanel` lists it in five `useMemo` deps, one of them `buildBaselineSequence` over the whole history, so the panel re-fused the entire baseline on every render *including the drag's own*; (2) `BeatBuilder` marshalled **every pointer sample** through `runOnJS` (60–120/s) and only discovered *in JS* that the notch had not changed — so with JS saturated the calls queued, the buzz ran behind the finger, kept firing after the tab changed, and the next drag started behind a thread still catching up. The crossing test now runs **in the gesture worklet** against shared values (JS entered once per notch, ~11 per sweep), and the hook's result is memoised. **Plus a mute, because a queue can never be proved empty**: the panel takes `active`, the builder takes `enabled`, the caliper is gated with `measurable={active}`, and a crossing retired after the reader has left is dropped rather than buzzed. ⚠️ Splitting the redraw guard across two threads created a NEW hazard: copying `value` back into the worklet mid-drag *rewinds* it (JS is a notch behind by construction) and the next sample re-reports it — a double thump; the sync ignores an echo of the control's own commit. **Rule: `runOnJS` per pointer event is a queue, not a callback — decide on the UI thread and cross once per meaningful change.** |
| ↳ ⚠️ **ECG ID — `display: none` blanks a measured width (v0.58.4)** | — | 🔬 | 🔬 | **The second cause of "the builder drag is dead", and my own v0.58.1 armed it.** `BeatBuilder` measures its track in `onLayout` and refuses to act without a width (it cannot compute a ratio) — and since History keeps both tabs mounted and hides the inactive one with `display: none`, **Yoga lays the hidden subtree out at zero**, so every trip to Studies blanked the width and coming back left the drag dead until a later layout pass. A zero is never a measurement; the track's width does not change while the panel lives, so the last real one is kept. **Rule: any `onLayout` measurement inside a pane that can be hidden must reject zero** |
| ↳ ⚠️ **History — the tabs must HIDE each other, not replace each other (v0.58.1)** | — | 🔬 | 🔬 | Reported as "a little flicker every time I come back to Studies". `tab === 'insights' ? <Insights/> : <list/>` **unmounts** a pane on every switch, and a remount replays every entrance animation, every visible trace's sweep, the cell window and the scroll position — the flicker was the screen being rebuilt on purpose. Both panes stay mounted and hide with `display: none` (Yoga drops it from layout: not measured, not drawn, state kept). Insights is still mounted LAZILY on first visit — it runs the identity backfill over the whole history. ⚠️ `activeTab` must derive from `tab` AND `showTabs`, or a stale `insights` while the list is loading/empty hides both panes. ⚠️ **The ledger of what keeping a pane alive costs is now two entries long, and neither was obvious:** Yoga lays a hidden pane out at **zero** (v0.58.4), and every control in it **still exists** and can still be reached by work already in flight (v0.58.5) — so the pane is told whether it is on show (`active`), and anything that vibrates must honour it. What it does NOT do is leak touches: `display: none` sets `hidden` on the native view, which cannot be hit-tested. Also v0.58.1: the first-frame header-height constant was ~35 pt off on a notched phone, so the list dropped into place on entry — it is now estimated from the same blocks the bar is built from; and `CONTENT_TOP_GAP` gives the newest card air under the glass at rest |
| ↳ **History — frosted header, page scrolls under it (v0.58.0)** | ✅ (sticky CSS header) | 🔬 | 🔬 | Asked for: *"can the top bar get the glass effect like the tab bar at the bottom, so you see the waves behind it?"* Same `GlassSurface` as the dock and the study viewer's header (Liquid Glass where it exists, `dimezisBlurView` elsewhere). ⚠️ **A floating bar is not part of the layout**, so its height moves onto every scroller's CONTENT inset — the third axis of `scrollsUnderDock`, shipped as `PatientShell.bleedTop`; without it the shell's padding pushes the list down and the glass has only bare page to refract. Height is **measured** (`onLayout` + the glass's own padding added back), because the bar grows a count line, a backfill-progress clause, a tab row and an error banner. ⚠️ Anything that would sit *between* header and list must live INSIDE the glass — as a sibling it is pushed down by the clearance and the list then pads for the header again beneath it. Hairline earned after ~6 pt of scroll; both tabs report scroll so the rule holds on Insights too. Tint between the dock's and the viewer header's, stated why in both directions |
| ↳ **History + Insights — THE TOP BAR IS GONE (v0.70.0)** | ➕ web keeps its sticky header | 🔬 | 🔬 | **Deliberate divergence from web, at the user’s instruction:** *"in Insights and History there is no need for a top bar; it can be part of the page and fade out as you scroll down."* The row above is now HISTORY — the frosted bar it describes was removed here. Title, count and Import are the list’s `ListHeaderComponent` (and `EcgIdentityPanel`’s new `header` first child on Insights), shared via the new `PageTitle` molecule so the two tabs cannot drift apart by a font weight. ★ **Rendered INSIDE the scroller, not animated over it.** The obvious version keeps the absolute bar and animates `translateY: -scrollY`; that makes the title’s POSITION a 60 Hz animation driven by a throttled JS `onScroll`, and a position lagging the content it belongs to reads as the title sliding on its own. As real content it travels with the page for free at the scroller’s frame rate, leaving OPACITY as the only animated property — a lagging opacity is invisible, a lagging position is not. **Deletes** the measured header height, `estimateHeaderH()` (the first-frame estimate, once wrong by ~35 pt on a notched phone), the `onLayout` that added the bar’s padding back by hand, the `scrolled` hairline state, `HEADER_PAD_BOTTOM`/`HEADER_SHADOW_AT` and the Liquid-Glass tint pair. `PatientShell.bleedTop` STAYS — the title owns the safe area now. ⚠️ Two non-obvious bits: a faded-out Import button must stop being a TARGET (pointer events are not animatable, so each screen flips one boolean at the exported `TITLE_FADE_DISTANCE`, which is also what the fade uses); and History’s refresh badge, which hung off the measured header height, now sits level with the title row and centred — a fixed position, because `refreshing` is also true during a background sync when nothing has been pulled down. **Not ported to web**, whose sticky header sits in a desktop two-column reading view. 🔬 Nobody has scrolled it on a phone yet |
| ↳ ⚠️ **Insights — the early-return states missed the whole frame (v0.70.0 regression, fixed v0.74.0)** | ➖ n/a | 🔬 | 🔬 | Reported as *"the Insights tab glitches, it shows for a split second and it looks unstable"*, with a screenshot of **"Building your ECG ID" under the status-bar clock**, against the left edge, no title. ★ **One bug, not two.** `EcgIdentityPanel` has three early returns (error / building / no-identity) and each rendered `<Empty>` BARE, outside the ScrollView — so all three missed `header`, `paddingTop` AND `paddingHorizontal`. Survivable before v0.70.0, because the absolutely-positioned glass bar was drawn in every state and took the safe area regardless. v0.70.0 moved the title INTO the content and handed the safe area to it; the early returns never got the memo. The loading state drew at y=0 with no title and then the ENTIRE PAGE jumped down and inward when the identity resolved. **That jump is the glitch.** Easy to miss because the building state lasts a frame or two on a warm cache — the slower the device, the longer the wrong layout shows, which is the opposite of how a bug should surface. Fixed by routing all four states through one `frame()`: the ScrollView is defined ONCE and they cannot disagree about where the page begins. **Rule for the next screen that owns its own top inset:** an early return is a second layout, and it has to carry the same frame as the first one |
| ↳ **History — the trace SWEEPS ON when the row is reached (v0.57.0)** | ⏳ | 🔬 | 🔬 | Asked for: *"an animation as if the wave is being created live — and the ones further down should only run when you scroll to them."* Constant speed (`Easing.linear` — a stylus does not accelerate; easing reads as a UI wipe), ~1.1 s rather than the recording's own 4 s, pen dot at the writing edge. Fired from **FlatList viewability**, so rows below the fold draw as they are scrolled to; a mounted row draws once and holds still. ⚠️ `onViewableItemsChanged` + its config must be **ref-stable** (RN throws otherwise), and the seen-ids live in a ref with a counter — a state `Set` rebuilt per viewability event re-renders the list mid-flick |
| ↳ ⚠️ **History — the sweep that made the tab unscrollable (v0.57.1)** | — | 🔬 | 🔬 | **Reported as "drastically slow, you can't scroll at all". Three faults, all mine, kept here because each is a reusable rule.** ① `strokeDasharray` + animated `strokeDashoffset` is **not a cheap effect, it is a per-frame geometry rebuild** — the renderer measures the path and constructs dash segments every time the offset moves, O(points) × every visible row. The UI thread does not save you; it only decides where the frames drop. **The SVG is static now and the reveal is one `translateX` on a plain view** in the card's colour. ② A **re-render storm**: viewability `setState` → every mounted row re-rendered, with `StudyCard` unmemoised and fed inline `{samples,…}` objects and per-render closures — *the exact trap this file already records from `StudyViewerScreen`, walked into twice*. Now `memo` + primitives/stable refs only. ③ The **"reveal anyway" timer was itself the bug**: FlatList mounts rows a screen ahead, so it drew them off-screen and reaching them blanked and re-drew the strip. No timer; only visibility sweeps, a latch stops restarts, threshold 30 % so a peeking row still qualifies |
| ↳ History — the verdict is NOT in a capsule (v0.57.0) | ⏳ | 🔬 | 🔬 | Reported: *"I don't want the finding sitting inside a coloured capsule — it looks cheap."* Correct, and the codebase had already made the argument at page scale: v0.50.0 replaced the PDF's verdict CARD with a ruled statement block because "a rounded box with a coloured fill is an app component photographed onto paper". The row was still doing it. Now a dot in the level's colour + the words in its ink, 14 pt, nothing behind them. **SIMULATION keeps its chip on purpose** — not a finding but a warning that the trace did not come from a heart (§4); a safety label may shout where a conclusion may not. The trace itself moved from `accentLive` (a generic product blue meaning "live UI") to **`brandNavy` #0D2041, the wordmark's own lettering**, which carries its own dark-theme value |
| ↳ Study viewer — Findings FIRST (order + patient's initial tab) | ⏳ | 🔬 | 🔬 | **v0.53.0, at the user's request — reverses v5.0.0's trailing-edge decision.** Segments now `Findings \| ECG \| Values`; a patient lands on Findings, a clinician lands on ECG. The ORDER is identical for every role (a control whose segments move between roles cannot be learned); only the landing differs. Web has no interpretation tab yet — this row extends the v0.46.0 IOU |
| **Printed report (PDF)** | ✅ `window.print()` | 🔬 | HW | **v0.48.0 - rebuilt.** A phone has no print dialog and no page, so the document is BUILT: `services/export/pdf/` (theme / figures / pages / document) plus `recordingPdf` for the print-and-share half. Strips come from `buildEcgPath`/`buildEcgGrid` in `CYPHIX_SHARED`, so a ruler laid on this printout agrees with one laid on the web sheet. **Pages 1..n are the six-lead ECG at full page** (40 mm per lead = 240 of the 256 mm body); then Interpretation, Statistics, Reference |
| ↳ **PDF** — ★ the clinical-document idiom (v0.50.0) | ⏳ | 🔬 | 🔬 | Reported: *"it does not look like a professional medical report."* The diagnosis is that pages 2–4 were the APP photographed onto A4 — rounded cards, soft fills, chips, six 30 pt stat tiles. What makes a sheet read as a report is boring and specific: **an identification grid** at the top (whose / when / on what / beats / quality), **the verdict as a ruled STATEMENT BLOCK** with a heavy left edge rather than a card (the shape a pathology report puts its impression in), **two ruled measurement tables with reference ranges and H/L flags** instead of tiles (a clinician reads a column, and the eye runs down the flag column first — blank when in range, because a column of ticks hides the exceptions), and **section headers in letterspaced caps on a full-column rule**. Radii, soft fills and chip pills removed; `tabular-nums` on the body so figures align on the decimal |
| ↳ Viewer tabs — `ECG / Values / Findings` | — | 🔬 | 🔬 | **v0.50.0.** A segmented control divides its width EVENLY, so three labels of different lengths always look wrong however the type is tuned — "Waveform / Measurements / Interpretation" is 72 pt then ~105 pt twice on ~120 pt segments, and `adjustsFontSizeToFit` was papering over it: the labels fitted and TOUCHED, because type squeezed to the edge of its box has no breathing room left. Short labels + a 10 pt gutter (was 4). They are also the better words — what a clinical report calls those sections. ⚠️ The end-of-exam report's TWO-tab control deliberately keeps `Waveform / Measurements`: it has the room, and changing it would rename a section of a different screen for no reason |
| ↳ **PDF** — ⚠️ the BLANK PAGE after every sheet, and the one millimetre that caused it | ⏳ | 🔬 | 🔬 | **v0.49.0.** `.pg` was `height: 297mm` inside a 297 mm page. WebKit lays print out in CSS pixels: 297 mm is **1122.52 px**, rounded **up** to 1123 — so the box was half a pixel taller than the page holding it, the engine honoured `page-break-after: always` on a box that had ALREADY overflowed, and half a pixel of nothing became a sheet of paper after every single page. `PAGE_BOX_H` is **296** now and `:last-child` breaks with `avoid`, not `auto`. ⚠️ Never declare a print box at exactly the paper height |
| ↳ **PDF** — the recording no longer paginates | ⏳ | 🔬 | 🔬 | **v0.49.0, at the user's instruction.** 186 mm at 25 mm/s holds 7.1 s, so a 10 s capture became two sheets and the second was six leads stopping a third of the way across — "half a page of ECG for the remaining leads". One sheet now, window stated against the total. **The alternative was rejected:** compressing 10 s into 186 mm means 18.6 mm/s, and rescaling the time axis is banned (`ecgPath.ts`) because every interval measured off the paper would then be wrong by a quarter. CSV/EDF export still carry every sample |
| ↳ **PDF** — the wordmark, the ruler, and the brand's own navy | ⏳ | 🔬 | 🔬 | **v0.49.0.** `pdf/logo.ts` draws the real lockup as plain SVG (path data verbatim from `components/atoms/BrandLogo`, which cannot be imported — it renders through react-native-svg into native views), 34 mm per letterhead. Axis labels are now ON the ECG paper: one per second along time, ±0.5 / ±1 mV against the baseline. And `clear` is **#0D2041**, not green — reported as "green is not my brand colour", which it is not. Green survives only as the reference band on an interval bar and the normal sector on the dial, where it is chart convention rather than identity |
| ↳ **PDF** — ★ the 43-check AUDIT, and why an empty page was the wrong problem | ⏳ | 🔬 | 🔬 | **v0.49.0.** Reported: "a whole page for that one line — are you serious?" The emptiness was the symptom; the disease is that **"no abnormal finding" is a claim with no content unless the reader knows what was looked for**. `screenLimbEcg` now returns `checks: ScreeningCheck[]` — every rule with `found` / `notPresent` / `notEvaluated` — and the page prints all 43 in three columns grouped by category. `notEvaluated` is NEVER collapsed into `notPresent`: one says the pattern is absent, the other says nothing at all. A suppressed finding is demoted to `notPresent` on the audit so one fact is not reported twice |
| ↳ **PDF** — the representative beat panel (shared with ECG ID) | ⏳ | 🔬 | 🔬 | **v0.49.0, asked for.** All six leads' median beats, from `buildBeatTemplates` in `CYPHIX_SHARED` — the SAME function the ECG ID tab uses, so the beat on paper and the beat on that screen are one computation rather than two derivations that will eventually disagree. Real ECG machines print this panel beside the rhythm strip for a clinical reason: a median beat is what a reader inspects for a Q wave or an ST segment, and a ten-second strip shows rhythm, not morphology |
| ↳ **PDF** — why it used to tear across pages, and the two fixes | ⏳ | 🔬 | HW | **Reported: "the graphs stretch across two pages".** Not styling - a MISSING CONSTRAINT. Strips were `<svg width="100%">` with a viewBox and **no height**, so height was inferred from an aspect ratio against whatever column the engine chose, and `.page` had no height ceiling. Any growth above (a long `deviceLabel` wrapping the letterhead suffices) pushed lead aVF past 297 mm. **Fix 1:** every box is a number in mm and `assertFits()` THROWS while building; `.pg` is `overflow:hidden` as a second line of defence. **Fix 2, found while fixing the first:** `printToFileAsync`'s default paper size **follows the device locale** - a US-English phone gets Letter, 6 mm narrower and 18 mm shorter than the geometry, which shears the sheet invisibly. A4 is now passed explicitly in points with zero margins |
| ↳ **PDF** — the figures, and why each one | ⏳ | 🔬 | HW | **v0.48.0.** Verdict **donut** whose fill is the fraction of checks that ran (a fraction is a shape before it is a number). **Hexaxial dial** - the frontal axis is an angle, and a compass is the only representation that needs no sentence. **Poincare plot** with the SD1/SD2 ellipse - the cloud's SHAPE separates a regular rhythm, respiratory variation and AF faster than any summary statistic. **RR tachogram** - where in the ten seconds the variation happened; a single early beat and a drifting rate give the same SD and look nothing alike. **Reference bars** per interval against its normal band. **Signed amplitude bars** - a Q wave being negative is the point of it. **Einthoven's triangle** - makes "inferior" mean something, and shows the blind spots by having no arrow at the front wall |
| ↳ **PDF** — verified in Node, because a PDF cannot be diffed | ⏳ | 🔬 | HW | The document builder was split away from `expo-print` into `pdf/document.ts` so it imports nothing native and runs under `tsx`. Nine cases (normal / simulated / 3 s / 30 s / brady / tachy / low-voltage / left-axis / irregular), all with deliberately over-long labels and a 78-char device name: **0** unsized SVGs, **0** percentage dimensions, **0** unresolved `{n}`/`{total}`, **0** inconsistent page numbers (checked against the real `class="pg"` count), **0** NaN/undefined, and `assertFits` confirmed to fire on an over-tall page. Still needs-device-verify: this proves the ARITHMETIC, not that the printed sheet is beautiful - that needs a human looking at paper |
| ↳ **PDF** — a SIMULATED study gets no interpretation page | ⏳ | 🔬 | HW | Same rule as `useScreening`, and it binds harder here: a document leaves the phone and is read by someone with no way to know the trace came from a bench generator. The interpretation page is omitted entirely and a floating SIMULATED banner sits on sheet 1 - floating rather than a block, so a demo export has exactly the same pagination as a real one (a layout that changes shape with the data has only been checked with one kind of data) |
| ↳ Screening — ★ why it is a SEPARATE module from `ecgAnalysis` | ⏳ | 🔬 | 🔬 | The codebase states twice that it does not interpret (`ecgAnalysis.ts` header; the `attention` token in `tokens.ts`). Neither was edited. `ecgScreening` **imports** the measurements and is never imported by them — `ecgAnalysis` "PR is 236 ms" (a fact, falsifiable) vs `ecgScreening` "236 ms is a first-degree AV block" (a reading, arguable). Deleting the screening file leaves the measurement layer intact, which is the property that keeps the numbers auditable by someone who disagrees with the reading. **The only change to `ecgAnalysis.ts` is the word `export` on `delineateBeat` and its type** — so screening can find a J point rather than forking the slope-collapse delineation. No maths, no constant. The web's copy under `src/services/ecg/` was **regenerated from shared** in the same change set so the two remain identical (root `CLAUDE.md` §2.1) |
| ↳ Screening — ⚠️ THE THREE DEFECTS FOUND BY RUNNING IT | ⏳ | 🔬 | 🔬 | The first version returned "no abnormal finding" on **1 of 40** healthy subjects. None was findable by reading. **(1) Bazett:** `qtLongSevere` — an *urgent* finding — fired on **3.6 % of 3 000 synthetic healthy adults**, because QT/√RR over-corrects above ~90 bpm and turns an ordinary 390 ms QT at 98 bpm into a QTc of 500. The correction is now chosen **by rate** (Bazett inside 60–100 bpm, Fridericia outside) and the urgent finding needs both to agree; reports still print both. **(2) Electrical alternans** fired on 1 in 7 — it was measuring noise, since ten beats split into even/odd groups differing by 15 % often. It now needs the alternation to exceed the scatter *within* each group. **(3) Lead reversal** fired on ordinary marked **right axis deviation**: a vector at +120° inverts lead I on its own. The discriminator is aVR, whose P wave is negative at every physiological axis (−0.09 mV at +45°) and flips **positive** when the arm electrodes are swapped (+0.73 mV). **After: 87.0 % clear, 0.00 % urgent**, with the survivors at their published population rates (LVH voltage ~5 %, PR > 200 ms ~2 %) |
| ↳ Screening — the three honesty invariants | ⏳ | 🔬 | 🔬 | Enforced by the type shapes, not by copy discipline. **(a)** Every finding carries the arithmetic that fired it (`evidence`), so it can be argued with. **(b)** Every screen carries its `blindSpots` — what six limb leads structurally cannot observe, the anterior wall above all — and renders them on a **clear** result too, most importantly there: a green mark with nothing beside it is read as "my heart is fine" when it says "nothing these leads can see is wrong". **(c)** A rule whose inputs were missing is **counted, not skipped**, so "no abnormal finding" always arrives with "41 of 43 checks ran" — 6 of 43 is an unreadable recording, and without the denominator both draw the same green mark |
| ↳ Screening — a SIMULATED study gets no verdict at all | ⏳ | 🔬 | 🔬 | `useScreening` returns **null**, and the tab says what the recording is instead — not a caveat under a verdict (mobile `CLAUDE.md` §4). ⚠️ **And this is measured, not precautionary:** the bench simulator's T wave sits at a *fixed offset* from the QRS, so its QT does not shorten with rate and every simulated strip measures a QTc near **280 ms** — screened, ~90 % would report a short QT. The engine is right and the signal is not a heart. **Known simulator defect, deliberately NOT fixed here:** `ecgSimulator.ts` is shared frozen code and re-timing its T wave changes the web's demo signal too. Worth doing on its own — a demo reading "43 of 43 checks ran, no abnormal finding" demonstrates more than one flagging a short QT |
| ↳ Screening — locale keys are compile-checked against the rule set | ⏳ | 🔬 | 🔬 | `FindingCard` **annotates** rather than casts: ``const nameKey: TranslationKey = `scrF_${finding.id}` ``. TypeScript checks the template-literal type against the locale key union, so adding a rule to the engine is a **compile error** until both `en.ts` and `he.ts` carry its two strings. Verified by temporarily adding a probe id — 3 errors, in `FindingCard` and in the engine's own `CATEGORY_OF`. `as TranslationKey` would type-check identically and print `scrF_myNewRule` to a patient, which is the one failure a translated medical screen must not have |
| ↳ Screening — patient sex is passed only when it is provably theirs | ⏳ | 🔬 | 🔬 | Sex moves the long-QT limit by 10 ms (450 male / 460 female). `usePatientCard` returns the **active** patient, who is not necessarily the subject of the study being viewed, so `useScreening` compares the recording's `subject` reference first and passes **no context** on a mismatch — the engine then falls back to the higher (more conservative) limit. Under-calling a borderline QT is recoverable; screening a woman against a man's threshold because of a UI coincidence is not |
| **Insights: ECG ID** | ✅ **web v1.57.0 — IOU PAID** | 🔬 | 🔬 | **v0.31.0 — MOBILE-FIRST, at the user's request; the web port landed 2026-09-21 in web v1.57.0, so this row is the Cross-Platform Rule's IOU PAID** (root `CLAUDE.md` §1). On the web it is a ROUTE (`/insights`) rather than a second tab inside History, because the web dock's second slot was free once Tests left it; the panel, the maths and every string are the same. The claim that the maths is platform-neutral held up exactly as intended — the port was a UI job, and the seven shared modules were copied with only their import specifiers rewritten (verified by a normalised diff). ⚠️ The web CANNOT import `@cyphix/shared` (Vercel builds the web checkout alone), so they are COPIES there, each carrying a footer saying so: **if they change here, change them there.** A second tab on History (`SegmentedTabs`, Studies \| Insights) holding the patient's own cardiac signature: every eligible study is reduced to a **representative (median) beat**, those are fused into a weighted **baseline**, and every study is then scored against it. The maths is entirely in `CYPHIX_SHARED` (`ecg/beatTemplate.ts`, `ecg/ecgIdentity.ts`, `ecg/measurementStats.ts`) with **zero** platform imports, precisely so the web port is a UI job and the server can adopt the same functions unchanged — no second implementation may ever be written. Tabs rather than a sixth dock item: both views are about the same records, and the reader moves between them constantly (flag → open study → back) |
| ↳ ECG ID — ⚠️ **the empty-history hang: the SAME BUG is still here** | ✅ fixed web v1.57.0 | ⏳ **open** | ⏳ **open** | **Found by opening the web page with the history cleared; a typecheck and a production build both pass it without a word.** `useEcgIdentity` keys its backfill effect on `ids = (list.data ?? []).map(…).join(',')`, which answers `''` **both** before the list query resolves **and** for a patient with no studies. Same value ⇒ the effect never re-runs when the empty list arrives ⇒ `templates` stays `null` ⇒ `isBuilding` stays true ⇒ a NEW USER sits on "Building your ECG ID · Reading your studies…" **for ever**, with nothing to read and no way to find out. Invisible the moment there is one recording, which is why it survived. The web now uses `null` until the list arrives; **mobile’s line is unchanged** because that change set was web-only by instruction — fix it here and the row closes. **Rule: a derived dependency must distinguish "not loaded" from "loaded and empty".** |
| ↳ ECG ID — web has NO HAPTICS on the caliper or the builder | ➖ deliberate | ✅ | ✅ | **Deliberate divergence, web v1.57.0.** The caliper’s Heavy tick per small square and the builder’s tick per study crossed are most of what makes either control worth touching on a phone, and the web has no equivalent: `navigator.vibrate` is absent on iOS Safari entirely, a blunt duration elsewhere, and fires whether or not the pointer is a finger — a mouse cannot feel anything at all. Emulating it would be a worse lie than omitting it. The builder gained a `role="slider"` with arrow/Home/End keys instead, which the phone has no need of |
| ↳ ECG ID — web shows no weekly goal | ➖ no reminder scheduler on web | ✅ | ✅ | `GoalWeek` is ported and wired on the web, and is handed `goal = 0` because the goal is the number of reminder times the patient set and the web has no reminder feature yet — so the "set reminder times to see a weekly goal" line shows instead. **Not a stub:** inventing a default target would be the app deciding how often someone should measure their own heart, which is advice. One expression changes the day web reminders exist |
| ↳ ECG ID — web template cache is IndexedDB, not localStorage | ➖ web-only concern | ➖ | ➖ | The phone stages templates into AsyncStorage. The web’s obvious counterpart is `localStorage` (the rest of that app’s client storage), and it is the wrong answer twice: the table is ~570 kB against a ~5 MB ORIGIN quota the mock database already sits in — so a long history would evict the recordings it is derived from — and `localStorage` is SYNCHRONOUS, so a 570 kB `JSON.parse` on first paint is precisely the freeze the cache exists to prevent. IndexedDB, and every operation resolves rather than rejects so a private window degrades to "nothing was cached" |
| ↳ ECG ID — ⚠️ the sheet’s labels are LTR and PHYSICALLY placed | ✅ fixed web v1.57.0 | 🔬 | 🔬 | In Hebrew the web sheet printed "mm/s · 10 mm/mV 50": a run that STARTS with a number is reordered by the bidi algorithm, and logical (`inset-inline-*`) placement also put the lead name at the END of its own trace. **Time runs left to right on an instrument in every language** — the lead label marks where the trace BEGINS and the scale sits at the end of the sweep — so both are `left`/`right` with `direction: ltr`. The phone uses `left`/`right` already and `Text` does not reorder a whole run the same way, but nobody has read that sheet in Hebrew on a device |
| ↳ ECG ID — what makes a study eligible | ⏳ | 🔬 | 🔬 | Simulator output (**never** — synthetic data may not define a person), fewer than 3 clean beats, or SQI < 50 are barred, each with a reason shown on screen. `ExclusionReason` is a typed union so a bar can never be silent |
| ↳ ECG ID — ★ TWO baselines: enrollment anchor + time-weighted tracker | ⏳ | 🔬 | 🔬 | **v0.41.0. The brief contains a contradiction and this is the resolution.** "The first studies define you" and "the baseline must follow slow change" cannot both be true of one number: weight the past and the reference can never learn; weight the present and there is nothing left to deviate from. So there are two — an **anchor** built from the enrollment cohort (target **5 → 10**) with no time decay, and a **tracker** decayed toward now (180-day half-life) which is what the screen draws and what a new study is scored against — with **drift** between them reported as a per-year rate. This is Phase I / Phase II from statistical process control, and it is what makes an *alert* and a *trend* separable events. ⚠️ Before this there was **no time in the model at all**, so a slowly drifting heart was *guaranteed* to fall below the agreement floor and be labelled an outlier: the baseline locked onto the past and called the present noise. The feature's whole purpose was structurally unreachable |
| ↳ ECG ID — the early studies weigh more, and are watched hardest | ⏳ | 🔬 | 🔬 | The first **10** eligible studies own the anchor; later ones keep a 0.1 tail so it can still be corrected, slowly — never zero, or the anchor would freeze permanently at ten recordings and become *more* fragile over time, since nothing could dilute an enrollment study that turned out poor. **Because** they weigh most, an early study that disagrees with its own cohort is `flaggedAtEnrollment` and drawn in the attention colour — one loose electrode on day one would otherwise bend the reference permanently and invisibly, and every later study would then score as "deviant" against a bad baseline |
| ↳ ECG ID — ★ no study may own the identity (`weightCap`, `nEff`) | ⏳ | 🔬 | 🔬 | **v0.41.0, from a user report: "one measurement captured most of the volume of the patient's ID".** The agreement weight was `clamp01((r − 0.8) / 0.2)` — a linear ramp off a fixed constant with a hard zero, which turns a 0.05 difference in correlation into a **10×** difference in weight and deletes everything below 0.80 outright. The enrollment boost was never the culprit; at 2:1 it could not have been. Now a **one-sided Tukey biweight** on a robust z against the cohort's *own* spread (so where down-weighting begins is measured, not chosen — one-sided because a two-sided version penalises a study for agreeing *better* than the median, i.e. treats the patient's cleanest recording as suspicious), plus a **hard cap**: no study exceeds `max(1/3, 2/n)` of the total, enforced by redistribution. The cap is the structural guarantee — whatever the arithmetic concludes, an identity may not rest on one recording. `nEff` (Kish, `(Σw)²/Σw²`) is now computed, drives `maturity`/`confidence` instead of a contributor count, and is shown when it materially disagrees with it: **it is the only field that can reveal concentration** — a count cannot, and a mean agreement cannot |
| ↳ ECG ID — ★ where the pads were is not what the heart did | ⏳ | 🔬 | 🔬 | **v0.41.0. `CYPHIX_SHARED/src/ecg/leadCalibration.ts`.** The screen was reporting `Shape · 3 leads` while the two **measured** leads sat silent, plus an amplitude change on exactly those two — a signature, not a finding. III/aVR/aVL/aVF are *linear combinations* of I and II, and correlation is gain-invariant, so pads a couple of cm off leave I and II at ~0.99 while `III = II − I` — a **difference** of two channels whose gains moved apart — genuinely changes shape. Those studies were being struck as outliers and the baseline was becoming "the sessions where the pads happened to match". The linear (I, II) remap is now fitted out **before** agreement is judged. ⚠️ **THE SAFETY ARGUMENT IS THE WHOLE FILE:** a placement change and a real frontal-axis change are **not separable** from the waveform, so the remap is used ONLY for "is this the same beat shape" — the question that decides whether a study may shape the baseline, where placement is a nuisance — and **never** touches what the deviations report. Axis is still measured independently by the DSP in degrees, amplitude in mV, both from the untouched trace. Removing the nuisance from the *weighting* while leaving it in the *reporting* is the design; applying it to the deviations would hide a real axis change, which is the most dangerous kind of defect — one that makes the screen quieter. Self-limiting: an implausible (rotation > 35°, gain outside 0.6–1.7, reflection, anisotropy > 2) or unhelpful (< 0.01 r) fit is refused and the identity behaves as if the file did not exist. The panel states on any study where it fired what was corrected **and what was not** — a corrected number handed over silently is worse than an uncorrected one |
| ↳ ECG ID — ★ compare a strip against YOUR OWN typical beat | ⏳ | 🔬 | 🔬 | **v0.43.0, at the user's request.** The viewer could ghost one STUDY behind another; it could not compare a strip against the PATIENT. That is the better comparison — one prior study brings its own noise, the ECG ID is the signal that survived every clean recording — and the reference was one screen away, unreachable from where people actually look at waveforms. The identity's representative beat is **stamped at every R peak of the strip**, so alignment is exact by construction: no beat-shift to accumulate error, no fiducial warp to distort intervals, and the three alignment modes are **not offered** because they exist to reconcile two independent timelines and this ghost has none of its own. ⚠️ **Which means its rhythm IS the strip's**, so the sheet says compare the SHAPE and never measure an interval off the grey trace. Above ~130 bpm the RR is shorter than the 700 ms template, so each stamp is necessarily truncated at the T wave — stated on screen, because measured agreement is 0.04 mV across 45–100 bpm and 0.30 mV at 140, all of it in the truncated region, and a reader would otherwise read the drawing's truncation as a T-wave change. Both hooks return an `OverlayView` so the screen keeps ONE ghost concept with two sources rather than two comparison features. Built only when selected (`useEcgIdentity({ enabled })`) — a cold pass re-analyses the whole history. ★ Two bugs found by measurement, not reasoning: the stamp must remove the TEMPLATE's own isoelectric before adding the strip's (0.13 mV float otherwise — already ~0 for templates this app builds, which is exactly why it must be explicit rather than left true by luck), and the TP gaps must HOLD the neighbouring stamp's edge rather than be written at the strip's measured level (the PR window contains part of the P wave; that bias cancels inside a stamp and not in an independently-filled gap — it was a 0.13 mV staircase at every beat edge, 0.998 → 0.972 correlation) |
| ↳ ECG ID — ★ the caliper hits hard, says what it found, and gets out of the way | ⏳ | 🔬 | 🔬 | **v0.52.0, from "the line over the wave should vibrate as hard as possible, when I lift my finger it should disappear, and while it's there it should write the wave's value nicely".** ★ **Tick = `Heavy`**, the strongest SINGLE event either platform exposes (the only louder API call is `notificationAsync`, a multi-thump success/warning/error PATTERN — wrong by meaning and impossible at scrubbing rate). Replaces `selectionAsync`, the lightest event iOS defines. `MIN_TICK_MS` (45 ms) is what MAKES it possible, not a compromise: 1 mm squares on a ~40 mm sheet = ~40 crossings/s, and a Heavy impact every 25 ms is more than the engine can reproduce — they merge into one flat rumble, which is WEAKER in the hand than a slower train of distinct hits. Throttles the buzz only, never the line. ★ **The reading is on the sheet**: a paper chip at the top edge, beside the caliper, with ms-from-R, baseline mV, and the overlaid study's mV in its own colour. ⚠️ The v0.16.0 rule (a readout on the trace covers the deflections it reports) still holds and is WHY it sits at the top edge rather than under the finger — what changed is that v0.44.0 deleted the chrome strip it reported into without moving the numbers, so since then it was a line you could drag along your own ECG that told you nothing. ★ **Vanishes on release**; persisting was right only while the readout was elsewhere AND stayed up. The tap went with it (a tap fires on release — it could only flash a line that erased itself); a 180 ms HOLD replaces it, long enough that a finger heading for a scroll does not drop a caliper. The lead name hides while measuring. 🐛 **Fixed**: the panel held the reading in state nothing had drawn since v0.44.0, re-rendering the whole Insights tree at gesture rate for a discarded value — the gesture is gated on `measurable` now. 🔬 Haptic strength and hold-vs-scroll arbitration are hand-only judgements |
| ↳ ECG ID — ★ the builder is back, and you can feel it | ⏳ | 🔬 | 🔬 | **v0.51.0, from "you took away the progress bar I could play with to see how my ID gets built over time, and that's a shame because it was cool with the vibration (and the vibration needs strengthening)".** ★ **`BeatBuilder` returns**, under the trace and above the lead buttons — a control must be adjacent to the thing it changes. v0.44.0 cut it with the legend row and the explainer as "explanations nobody asked for", and that was HALF right: the legend TOLD the reader something, the builder lets them DO something, and what they do is the only demonstration in the app of the claim the feature rests on (averaging many recordings cancels what is not the heart). Does not break the one-viewport rule — that block already reserves the full remaining window height and its content sat ~130 pt short of it, so the builder lands in slack that existed anyway. ⚠️ While a PARTIAL baseline is drawn the latest-study overlay hides: one study over "the first three studies" invites reading a comparison against something that is not this person's baseline. ★ **Haptics strengthened**: Medium impact per study crossed instead of `selectionAsync` — the LIGHTEST event iOS defines, tuned for a picker wheel under a resting thumb, and easy to miss entirely through a case with the finger already moving. HEAVY at either end of the timeline as an end-stop, so the finger finds the first and last study without looking. `MIN_TICK_MS` (32 ms) stops a fast flick merging the ticks into one rumble — it throttles the BUZZ only, never the value, so the picture never lags the finger. Caption 11.5 -> 13.5 pt, track and notches +2 pt. 🔬 Haptic strength is by definition something only a hand can judge |
| ↳ ⚠️ **ECG ID — the builder "loses touch" mid-drag (v0.58.2)** | — | 🔬 | 🔬 | **Reported as intermittent, and it was deterministic.** ★ **A new gesture OBJECT was reaching `GestureDetector` during the drag**: the caller passed `onChange` inline → new every render → new `move` (`useCallback`) → new `useMemo` gesture — and crossing a notch calls `onChange`, which re-renders the caller. `GestureDetector` reconfigures the native handler on a new gesture, and reconfiguring one mid-interaction can drop it, so it failed whenever a reconfiguration landed inside a drag. `BeatBuilder` builds its gesture ONCE over a stable ref-read callback (a careless caller can no longer reach the detector) **and** the caller was made stable — one place is a fix, both is a rule. **This is v0.57.1's inline-closure trap in gesture form; that makes three.** Two more found in the same read: `failOffsetY` ±12 was too tight for a thumb starting a horizontal drag on a 28 pt track (an ordinary diagonal start failed the pan and scrolled the page) → ±16; and the per-frame redraw guard never followed the `value` prop, so an external change (reset link, lead switch, rebuilt identity) left it stale and swallowed the first drag back to that notch |
| ↳ ECG ID — ★ Insights is the ECG first, and almost nothing else | ⏳ | 🔬 | 🔬 | **v0.44.0, from "it feels like you just piled more information on me instead of minimalism, and a patient doesn't know what that 'agree' in the green circle is".** Fair — v0.42.0 answered "make it useful for a patient" by ADDING, which is how a screen gets fuller while getting no clearer. GONE: the confidence ring, the three figures, the three-line explainer, the caliper readout strip, the beat builder, the legend row, the standalone baseline numbers, every explanatory paragraph, and "Changes since you started". The rule now: **if a line does not change what the reader does next, it is not on the screen.** FIRST: the ECG — trace plus lead buttons sized to ONE viewport, measured from the window and the dock, so the recording is never half-visible. ★ **The sheet is PAPER again**: ground, hairline edge, low soft shadow — reported as "the rounded rectangle with no outline and no shadow doesn't look professional", and both halves were right. ⚠️ Not the white card v0.33.0 removed: that was a white sheet inside a white CARD on grey, three nested rectangles; this is the opposite failure — a grid floating with no edge reads as a texture, not a recording. Grid keeps the brand navy, not clinical pink (pink reads as a hospital printout). The shadow is on the wrapper View, never the Svg — a native SVG casts none on either platform and ignores `elevation` on Android, so it would have looked framed in the simulator and flat on a phone. ★ **Every measurement, every time** (`StudyReadout`): rate, PR, QRS, QTc, axis, each beside what that person usually holds, colour the only difference — showing only what MOVED made the screen's content depend on whether anything was wrong, so the layout jumped and an empty space was ambiguous between "everything agreed" and "nothing could be measured". ★ **A weekly goal** (`GoalWeek`): seven rings against the reminder times already set, no second setting because a goal and a schedule are one intention said twice. It never scolds — empty days are ordinary border colour, future days carry no count, and there is no reward state ("four instead of three" is not better care). The plain reading moved under the lead buttons and lost its tick: a green check is a PASS MARK and this layer does not pass anything. Type up throughout — no small text, for an older reader |
| ↳ ECG ID — ★ the patient's half of Insights | ⏳ | 🔬 | 🔬 | **v0.42.0, from "it feels dated, the colours are old, it isn't practical — add useful information for a patient who understands nothing about ECG".** Three complaints, one defect: the screen was built for a clinician and opened with letterspaced small caps, a ring reading 82, then percentages and Latin. **The order inverted** — the answer first as a SENTENCE (`PlainVerdict`), then three figures anyone can place (`PatientFacts`: resting rate, their own recording count, months tracked), then the curve, then three numbered lines saying what the curve IS (`HowItWorks`, deliberately AFTER the picture — explaining a thing before showing it is how a manual reads). Nothing deleted: every clinical figure is still below, so Insights is two screens stacked in one column. **The "dated" feeling was the section headers** — 11 px letterspaced uppercase in the FAINTEST colour, six down one grey column, quiet past the point of readable so the eye got no structure at all; now legible sentence-case in the secondary colour, gaps 10 → 14 (de-carding in v0.33.0 removed the boxes, which were doing the spacing as well as the framing). **The palette did not change — it was barely being spent**, almost everything was one of three greys, and `signalSoft`/`attentionSoft` now actually appear. ⚠️ Still no cards: a tinted borderless band reads as *the page speaking*, a white rectangle on grey reads as an object pasted on it, which is what the v0.33.0 report was about |
| ↳ ECG ID — ★ the plain verdict is measured against the PATIENT, not a threshold | ⏳ | 🔬 | 🔬 | **v0.42.0. `CYPHIX_SHARED/src/ecg/ecgIdentitySummary.ts`.** The obvious way to say "does this look like your usual ones" is to ask whether the study carries a marked deviation — **which is exactly how v0.41.0's alert banner was built, and it told a real user their heart differed on 26 studies out of 26.** The per-study thresholds are calibrated for a clinician weighing one study; promoting them to a verdict addressed to the patient is the defect. So the file contains no absolute threshold at all: a study is compared with the median and robust σ of THIS PATIENT'S OWN scores, one-sided (a better-than-usual recording is never flagged), with σ floored at 4 points so being consistent does not make the app hair-triggered, and no verdict offered under 4 studies because there is no "usual" to be unlike. By construction the quiet state is quiet — half of anyone's studies sit at or above their own median. `plainVerdictOf` exposes the same spread so the sentence under the TAPPED bar cannot disagree with the one at the top. ⚠️ Two copy rules, both already got wrong once here: it never GRADES ("looks healthy" is a diagnosis; one word of reassurance changes the product's regulatory class), and it never implies a universal yardstick ("a healthy 95 %" would describe a computation that does not happen) |
| ↳ ECG ID — ★ there is deliberately NO alert banner | ⏳ | 🔬 | 🔬 | **v0.41.0 added one; v0.41.1 removed it after a day on the user's real history, where it read "The same difference on 26 studies in a row: Shape · Amplitude."** 26 of 26. The persistence rule counted backwards while the same deviation *kind* kept appearing, and `morphology`/`amplitude` fire against the local baseline on very nearly every study — the run never terminated, so the banner had been true since the first recording. ★ **The lesson is not "tune the rule": a persistence rule cannot rescue per-study thresholds that fire constantly, it inherits their false-positive rate however many repeats it demands, and an alarm on since day one is worse than absent because it occupies the space a real one needs.** Removed the line, the `IdentityAlert` model and its copy — not left computed-but-unrendered, which is a landmine for whoever next believes it works. ⚠️ **Both tests written for the rule PASSED while it was broken** (last-two-widen → `marked`, last-one-only → `watch`): the synthetic cohorts are clean enough that only ~11 of 24 studies carry any deviation, so the run terminated there. A generated cohort can prove a rule's arithmetic and cannot prove its **base rate** — and a rule whose entire job is counting repeats lives or dies on that. Anything reintroduced must rest on a residual whose quiet state is genuinely quiet, shown on real serial data first. The per-study deviation chips stay: checkable arithmetic about one recording is a materially different claim from "something is happening to you" |
| ↳ ECG ID — ★ drift is a trend, and never an alert | ⏳ | 🔬 | 🔬 | **v0.41.0.** `IdentityDrift` carries anchor → current, the delta, and a **per-year rate**, because "+14 ms" is not a fact about a person until you know whether it happened over three weeks or three years. Drawn as data — neutral ink, no chip, no severity colour — because a living person drifts and a screen that alarms about it teaches the reader to dismiss it. ⚠️ **The threshold for "has this moved" is NOT the per-study one, and reusing it was a bug caught by measurement:** deviation thresholds are calibrated for one 10-second recording against a baseline, while drift compares two *aggregates* each averaging 10+ studies, whose noise is smaller by ~√n. On a cohort drifting at a known +7 ms/yr the drift was computed correctly as +8.9 ms/yr and then **suppressed**, because 8 ms between two 12-study means did not clear a threshold built for one noisy strip. Now scaled by `1/√min(nEff)`, floored at ⅓ so it cannot fire on delineation rounding |
| ↳ ECG ID — ★ the similarity scale and the chart axis are ONE constant | ⏳ | 🔬 | 🔬 | **v0.41.0, and this was most of what the reported screenshot actually showed.** `similarity` was stretched from r = 0.90 while `SimilarityTimeline` drew an 80–100 axis it had chosen for itself in a different file, with nobody owning the pair. Work it through: the chart's *entire visible range* was **r ∈ [0.971, 1.000]**, so a study matching at 0.96 — an excellent serial match — drew as exactly the same 6 px stub as one at 0.80, and any normal history could only render as one tall bar in a row of identical dashes. **It looked like a weighting bug in the identity; it was the chart discarding the data.** `SIMILARITY_FLOOR` (now 0.80) and `SIMILARITY_AXIS_FLOOR` (60) are exported together and the chart derives both its scale and its axis labels from them. General lesson: a chart that picks its own axis floor for a score computed elsewhere is asserting something about that score's distribution that it has no way to know |
| ↳ ECG ID — ★ the provisional baseline is a weighted MEDIAN | ⏳ | 🔬 | 🔬 | **A real failure, found by running the algorithm on synthetic cohorts before shipping, not by reasoning.** As a weighted MEAN, five consistent studies plus one taken with a displaced electrode ended with the FIVE excluded as outliers and the ONE as the baseline. Mechanism: in the leads whose polarity the bad study reversed, the mean nearly cancelled, leaving a noise-shaped residual that the good studies correlated poorly against. **An estimator an outlier can pull cannot be used to find that outlier.** Median to find the inliers, weighted mean of the inliers to combine them |
| ↳ ECG ID — no study is scored against its own reflection, or against another era | ⏳ | 🔬 | 🔬 | Every match uses a **LOCAL leave-one-out** baseline: every *other* study, weighted by how close in time it is (365-day half-life, wider than the tracker's on purpose — the tracker answers "what is normal now", scoring answers "was this normal for its own time" and needs neighbours on both sides). Leave-one-out is how a system like this avoids fooling itself: with few studies each one drags the mean toward itself and then reports an excellent match with largely its own reflection, so outliers would be the *least* likely thing to be caught. **Making it local (v0.41.0) is what stops slow drift retroactively condemning old studies** — a recording from two years ago is compared with the heart of two years ago, which is the only comparison that was ever meaningful. It also replaces the old `subtractOne` accumulator trick, at a cost of one accumulation per study (~2 M float ops over 40 studies) |
| ↳ ECG ID — a majority is required before anyone is called an outlier | ⏳ | 🔬 | 🔬 | Under **3** eligible studies the agreement pass does not run at all. Two studies have no majority: with one good and one bad there is no information saying which is which, so whichever the arithmetic happened to favour would be crowned and the other struck. Both are kept, neither is called an outlier, confidence reports the truth (≈20) |
| ↳ ECG ID — the tolerance corridor is measured, never chosen | ⏳ | 🔬 | 🔬 | Per-sample σ combining the spread BETWEEN studies with the spread WITHIN them (the beat-to-beat MAD each template carries), floored at 20 µV — the hardware's real repeatability limit. Drawn as ±2σ behind the baseline beat; an excursion counts only past 3σ **for ≥ 20 ms**, because a real change in a waveform has duration and a spike does not |
| ↳ ECG ID — deviations state their own arithmetic | ⏳ | 🔬 | 🔬 | Each chip carries value, baseline and delta ("QRS +14 ms · 98 → 112"), so a clinician can check it and disagree with it. Thresholds differ **per interval by how firmly its landmarks can be found**: QRS [10, 20] ms (slope collapse — the firmest), QTc [30, 50] (T-end by tangent), PR [35, 55] (P onset — the faintest landmark; 25 ms fired on delineation jitter between ordinary consecutive studies, and a panel that cries wolf weekly teaches the reader to ignore it). Rate is reported but capped at `watch`: it moves with the stairs and the coffee. ⚠️ **Nothing here interprets** — no "normal", no finding, no advice; `insDisclaimer` is part of the screen |
| ↳ ECG ID — the amplitude ratio needs an absolute floor | ⏳ | 🔬 | 🔬 | Second finding from the same probe: lead III and aVL are derived and often ~0.2 mV, so a 0.07 mV wobble is a 35 % change and fired `marked` on ordinary variation while the two MEASURED leads stayed silent. Now gated on a 0.3 mV baseline amplitude **and** on 3× that lead's own measured tolerance |
| ↳ ECG ID — lead-set agnostic by construction | ⏳ | 🔬 | 🔬 | **The 12-lead answer, built in now rather than retrofitted.** Nothing in the stack counts to six: `EcgLeadName` = limb ∪ precordial, and an identity holds a PARTIAL map keyed by it. A study carrying V1–V6 extends the identity into those leads on its own; limb-only studies keep contributing to the limb leads and are not penalised for what they never had. `LeadCoverageGrid` prints all twelve with the un-measured ones explicitly empty — that is how a reader sees this is a six-lead identity, and it is the seam the 12-lead hardware arrives through |
| ↳ ECG ID — the signature is drawn on real paper | ⏳ | 🔬 | 🔬 | `BeatSignature` uses the same shared `buildEcgGrid` / `buildEcgPath` as the report, at **50 mm/s** — the clinical DETAIL speed, printed on the sheet — because 700 ms at 25 mm/s is 17.5 mm wide against ~27 mm tall and a square-gridded sheet of that is an unreadable sliver. Gain drops to half-standard **only** when a complex will not fit at 10 mm/mV, and says so. A fitted scale was NOT used (`ecgPath.ts` — a QRS measured off 17 mm/s reads ~30 % narrow). No calibration pulse: 18 mm at this speed is half the sheet, and the grid at a stated scale already is the ruler |
| ↳ ECG ID — computed once, on the device, off the render path | ⏳ | 🔬 | 🔬 | One template costs a decode + six lead-filters + Pan-Tompkins + a median stack; forty of those in one synchronous burst is a frozen screen. `useEcgIdentity` runs after `runAfterInteractions`, one study at a time with a **macrotask** yield between each (an `await` inside a CPU-bound loop yields nothing), releases each waveform immediately (`subscribe: false`), and reports "Analysing study 12 of 34" rather than hiding behind a spinner of unknown length. Templates are cached in `services/db/templateCache.ts` — one heavy entry, staged in memory, flushed every 5 — and gated on `TEMPLATE_VERSION`, so two generations of the maths can never be averaged into one signature. **Only ever slow once**: a recording is immutable, so its template is too |
| ↳ ECG ID — measurement statistics | ⏳ | 🔬 | 🔬 | `summariseMeasurementHistory`: totals, per-week cadence, longest gap, weeks-in-a-row, days since last, and a 24-bar **local-hour** histogram with the busiest 4-hour block lit. It sits beside the baseline rather than in a stats corner because it QUALIFIES it — six studies all taken at 22:00 are a baseline for late evenings and a poor one for a 07:00 comparison, and the hours are the only place a reader can see that. Deliberately **no targets and no streak game**: it counts and divides, it never advises |
| ↳ ECG ID — drag the beat to MEASURE it | ⏳ | 🔬 | 🔬 | **v0.32.0.** A caliper on the signature: tap or drag and a line follows the finger, ticking `selectionAsync` once per small square (20 ms at 50 mm/s), reading out time from R · baseline mV · the width of your own range there · the overlaid study's value. Readout is in the CHROME — History's calipers learned in v0.16.0 that one floating on the trace covers the deflections whose position it reports. ⚠️ Two gesture rules, both the difference between a control and an interruption: the pan claims horizontal and **fails on vertical** (`failOffsetY`) or the card becomes a hole the page cannot be scrolled through; and it fires on `onStart`, not `onBegin`, or resting a thumb while flicking past drops a caliper and buzzes |
| ↳ ECG ID — drag the track to BUILD it | ⏳ | 🔬 | 🔬 | **v0.32.0.** One notch per contributing study; drag and the baseline assembles study by study, one haptic tick each (`buildBaselineSequence`, incremental — one pass, not one per step). The only control that EXPLAINS the feature rather than describing it. ⚠️ **It was written claiming the band TIGHTENS as studies are added; measuring it said otherwise** — 0.021 → 0.026 → 0.028 mV, then flat. The corridor is a PREDICTION interval, not a standard error, so it converges on real variability instead of shrinking. The honest story is the better one: after one study the narrow band holds nothing but that recording's own beat-to-beat noise — a single measurement wearing the costume of a range — and dragging right is the app learning how much this person varies. Both the shared function and the component carry a warning not to write the tempting version again |
| ↳ ECG ID — the rejected beats are SHOWN | ⏳ | 🔬 | 🔬 | **v0.32.0.** "3 beats were not used" asked for trust on the decision that most shapes the whole feature. Up to four are now kept per recording (`TEMPLATE_VERSION` → 2; v1 entries recompute) and drawn on the **accepted beat's own axes and gain** — one gain for all of them on purpose, since a rejected beat rescaled to its own extremes would draw the same height as the accepted one and "it is twice as tall" is what the picture exists to show — each labelled with why (came early / different shape) and its correlation. `truncated` beats stay a count: their window ran off the end, so there is no complete shape to draw |
| ↳ ECG ID — ★ no green, no red | ⏳ | 🔬 | 🔬 | **v0.32.0, from device feedback.** The green `ESTABLISHED` capsule is gone: green means "pass", a baseline existing is not a pass, and this layer may not grade anything — state is a letterspaced small-caps line, the register an instrument labels itself in. Red is gone too, and it mattered more: red on a medical device means *alarm, act now*, while a distance from your own baseline is a measurement the app has no licence to judge. Reported as exactly that failure — people tensed before reading what it referred to. New `attention` amber token carries every deviation, flag and outlier; `danger` keeps destructive actions. The chips also gained a sentence saying what a difference IS, because **a number nobody can interpret is worse than no number: it worries without informing** |
| ↳ ECG ID — the ring stopped being frozen at 5/5 | ⏳ | 🔬 | 🔬 | **v0.32.0.** Two states drawn as the two different quantities they are: SEGMENTS while enrolling (a countable target of studies) and a continuous ARC for agreement once established, with the caption naming which. Also fixes the overlap reported on device — "Confidence 48 %" printed straight *through* the ring, because a `Text` in a flex row does not wrap, it overflows, and that column had no `flexShrink` (`MetricTile` carries a comment about this exact failure) |
| ↳ ECG ID — ★ one gain, one channel height, every lead | ⏳ | 🔬 | 🔬 | **v0.33.0, from device feedback ("the window changes with the wave height and feels unstable").** Height and gain were derived PER LEAD from that lead's own amplitude, so every lead drew a different rectangle and dragging the builder resized it under the finger. Both are now chosen once from the tallest lead in the identity and passed into `BeatSignature` (`pickGain`, still only the standard 10 / 5 / 2.5 mm/mV, still printed). Not just a layout fix: **a small lead now draws as a small trace in the same box, which is true and is information — a lead scaled to fill its own box is the picture that lies about it.** This is what a real 12-lead sheet does |
| ↳ ECG ID — ★ no cards, and the ECG is full bleed | ⏳ | 🔬 | 🔬 | **v0.33.0, from device feedback ("it's drawn inside white rectangles, not on the screen").** White ECG paper inside a white card on a grey page is three nested rectangles, which is how you announce a picture pasted into a layout. Insights has no cards: a section is a small-caps label, its content and a full-bleed hairline, and the trace is drawn on the app's own background with the grid as a faint brand tint (`SCREEN_LIGHT`/`SCREEN_DARK`). ⚠️ **The REPORT keeps its white paper and that is not an inconsistency** — `EcgStripSvg` is a *document* with real edges whose grid is the ruler; this is a panel on a screen. Same geometry, same builders, same standard scales, different surface. The bleed is MEASURED (screen width − content width), never a hard-coded inset: the shell's padding is `max(safe-area, 20)` and drifts with the notch |
| ↳ ECG ID — rounded sheet corners, inset from the display | ⏳ | 🔬 | 🔬 | **v0.33.2–.3.** The full-bleed grid ended in hard 90° corners; rounded at the app's own radii (`lg` for the signature, `md` for the shorter rejected-beats sheet — radius scales with the surface). ⚠️ **As an SVG `ClipPath`, not `overflow: 'hidden'` + `borderRadius` on the wrapping View** — clipping a native SVG child to a parent's rounded corners is a place iOS and Android have historically disagreed, and round-on-one/square-on-the-other passes typecheck, both bundles and doctor alike. Radius converts points → mm through the sheet's own scale so the curve reads the same at any width; each sheet gets a `useId` clip id because Android resolves `url(#…)` per document and two sheets sharing one would clip to whichever mounted last. **v0.33.3:** the sheet also stops `SHEET_MARGIN` (10 pt) short of the display instead of running flush to it — flush was fine with square corners and wrong with round ones, because a curve ending against the screen edge reads as the grid spilling off rather than as a corner. 10 pt is half the page margin, so the sheet still visibly breaks out of the text column |
| ↳ ECG ID — ⚠️ a negative margin cannot escape a ScrollView | ⏳ | 🔬 | 🔬 | **v0.33.1, from device feedback ("the edges are cut and the I / II / aVL label gets clipped").** Not a wrong padding number: RN clips a scroll view's children at its **own frame**, so a child made wider than the scroller loses the overhang entirely. The shell was applying the side padding, making the scroller 40 pt narrower than the screen, and the signature's `marginHorizontal: -20` was cut on both sides — with the lead label (16 pt from the edge) entirely inside the cut. Fixed by making the SCROLLER full width: `PatientShell.bleedHorizontal` drops the shell's padding and `shellPaddingH()` hands the screen the same number to re-apply, so the two can never disagree. Applies to any future full-bleed element anywhere in the app |
| ↳ ECG ID — the timeline became the picker | ⏳ | 🔬 | 🔬 | **v0.33.1.** "Latest study" was its own block above the chart, repeating the last bar's date and match figure in bigger type — reported as adding nothing, correctly. But its deviation chips are the ONLY place "has anything changed" is actually answered, so deleting the block would have lost the answer. Merged instead: tapping a bar selects that study and the detail beneath the chart follows, defaulting to the newest; the beats that study left out moved in beside it. A bar tap no longer navigates (the detail row does) — the old behaviour made an old bar a one-way door, so the chart could point at an outlier and then only offer to change screens about it. Two sections fewer, nothing lost, every older study inspectable rather than only openable |
| ↳ ECG ID — the prose got out of the way | ⏳ | 🔬 | 🔬 | **v0.33.0.** One line per section; `insCoverageBody` deleted outright (the dimmed empty V1–V6 cells already say it). What survives is only what the screen cannot say without words: `insDeviationMeaning` (a number nobody can interpret is worse than no number — it worries without informing) and `insDisclaimer`. Baseline figures became a plain row rather than bordered tiles — five numbers, not five controls |
| ↳ ECG ID — the page passes behind the glass | ⏳ n/a | 🔬 | 🔬 | **v0.32.0.** `PatientShell` reserved the dock's footprint as PADDING, so a scrolling screen ended above the bar and the strip the dock floats over was bare background — reported as an ugly grey band, and it also left a frosted bar with nothing to refract. New `scrollsUnderDock` prop: the clearance moves onto each scroll container's content inset, so both History tabs travel behind the glass and still scroll clear of it |
| ↳ ECG ID — **the web port is the outstanding work** | ⏳ **pending** | — | — | Every rule above is already platform-neutral TypeScript in `CYPHIX_SHARED`; the web needs `EcgIdentityPanel`'s equivalent and a place to put it (its History is a two-column reading view, so the tab may want to be a sidebar section instead). **A second copy of the maths would be a violation** — the web must import `buildEcgIdentity` / `buildBeatTemplates` / `summariseMeasurementHistory`, not re-derive them |
| **Measurement reminders** | ⏳ **pending** | 🔬 | 🔬 | **v0.34.0 — MOBILE-FIRST, at the user's request; the web has nothing like it and this row is the Cross-Platform Rule's IOU** (root `CLAUDE.md` §1). The patient picks how many measurements a day and at what times; the phone notifies them daily. The schedule SHAPE is in `CYPHIX_SHARED` (`types/reminder.ts`) because it is a statement about someone's care rather than a handset setting — it must survive a new phone and be legible to the web. Times are **minutes of the local day, never instants**: a reminder is a time of day, and pinning it to a date and timezone would wake a patient who flew somewhere at 03:00. ⚠️ **Nothing in it recommends how often to measure** — four a day is a UI bound, not advice; no "recommended" marker, no streak language. How often to take an ECG is a clinical instruction and this app does not give those |
| ↳ Reminders — where it lives | ⏳ | 🔬 | 🔬 | Settings › Notifications › **Test reminders**, which has existed since v0.2 as a switch that stored a boolean and did nothing. The switch and the TIMES stay two settings: the switch answers "may this app remind me at all", the schedule answers "when" — folded into one, a patient silencing reminders for a fortnight would lose the times they had chosen. Switching on with nothing set opens the editor rather than leaving them to find the row again. The **Tests tab badge** said the static word "Scheduled"; it now prints the real next reminder, because that circle is where a patient looks to ask when they are meant to do this |
| ↳ Reminders — ★ a daily OS trigger, not a background task | ⏳ | 🔬 | 🔬 | `SchedulableTriggerInputTypes.DAILY`, handed to the OS, which fires it whether or not the app has run since. A self-re-arming background task would be at the mercy of iOS's background-execution budget, so **a patient who had not opened the app in a week would silently stop being reminded** — the one failure this feature cannot have. Four repeating triggers are 4 of iOS's 64 pending slots and survive a reboot. The stored schedule is the truth and the OS is a projection of it: every edit path ends in one `applySchedule`, which cancels-then-sets so the OS can never drift into being a second copy |
| ↳ Reminders — re-applied on mount, including on a language change | ⏳ | 🔬 | 🔬 | A notification's words are baked in when it is **scheduled**, so without this a patient switching the app to Hebrew would keep receiving English reminders until they next happened to edit their times — a bug with no visible symptom inside the app. Re-applying also covers permission revoked in the phone's settings and a restore onto a new device (preferences come back; the OS's schedule does not) |
| ↳ Reminders — permission is asked once, and refusal is stated | ⏳ | 🔬 | 🔬 | The prompt is raised when reminders are switched ON — the moment the patient has said what it is for — not on first launch, which is how an app gets denied permanently. Never re-asked after a settled no: on iOS a second request shows no prompt at all and would read as a silent failure. When it was denied the sheet says so in words, because **a schedule that looks armed and never fires is worse than one that is plainly off** |
| ↳ Reminders — ★ the editor is a SCREEN, not a bottom sheet | ⏳ | 🔬 | 🔬 | **v0.34.1, from device feedback ("it comes up from the bottom, it's small, it doesn't feel native").** A container problem, not a styling one: on iOS a settings row with a chevron PUSHES a panel, and a sheet is for a quick action or a single pick — a switch plus a segmented control plus a list of times plus an inline wheel in half a screen is a settings panel wearing the wrong clothes. As a pushed route it also gets the native slide transition, the edge-swipe back and unlimited height. ★ Built from Settings' OWN `SettingsSection`/`SettingsRow` and page metrics — **not for code reuse, for CONTINUITY**: it is reached from Settings and belongs to it, so a bespoke layout would announce itself as somewhere else. Looking like the screen you came from is most of what "native" means here. The master switch and the schedule flag also collapse into ONE control on this screen (they stay separate in the model, so silencing still does not forget the times) — two toggles both labelled "reminders" is a puzzle |
| ↳ Reminders — the editor's content | ⏳ | 🔬 | 🔬 | "How many times a day" first (the question a patient has an answer to), then times pre-filled with waking-day anchors, all editable. Rows are named by **part of the day chosen from the TIME**, not the index, so a reminder moved to 07:30 reads "Morning" rather than "Reminder 2". Picker is `@react-native-community/datetimepicker` — the real iOS wheel and the real Android clock dialog; a hand-rolled picker is the fastest way to make a settings screen feel like a website. ⚠️ Android's dialog reports its own dismissal and iOS's inline wheel does not, so they are closed differently — treating them alike leaves the Android picker un-closable |
| ↳ Reminders — ⚠️ no push entitlement, on purpose | — | ✅ | ✅ | **The first 0.34.0 iOS build FAILED**: *"Provisioning profile doesn't support the Push Notifications capability / doesn't include the aps-environment entitlement."* `expo-notifications` adds `aps-environment` unconditionally **and is applied by AUTOLINKING** — deleting it from `app.json`'s `plugins` array does nothing (verified, not assumed: `expo config --type introspect` still showed it). The obvious fix — enable Push on the App ID and reissue the profile — is the WRONG one: this app schedules local reminders through `UNUserNotificationCenter`, which needs no entitlement, never calls `getExpoPushTokenAsync`, and never creates a device token. `plugins/withoutPushEntitlement.js` deletes the key after the library sets it (mods compose in registration order; it deletes rather than blanks, because the library's guard is `if (!…['aps-environment'])` and an empty string would be overwritten). ★ `expo config --type introspect` runs the full plugin pipeline **on Windows**, so this class of failure is now checkable without spending a 30-minute cloud build |
| ↳ Reminders — ★ the CONDITIONAL second ask | ⏳ | 🔬 | 🔬 | **v0.35.0.** Set 19:00 and, if nothing is in history by 20:00, the phone asks once more. The word doing the work is **if**: a patient who measured at 19:12 must not be nudged at 20:00 about the thing they already did. A reading up to 45 min EARLY counts too (`SATISFY_LEAD_MINUTES`) — 18:50 is the evening reading. ⚠️ **The two kinds are armed differently and have to be**: the primary is a repeating DAILY trigger (fires whether or not the app has run in a month — that guarantee IS the feature), the follow-up is DATED one-shots armed a week ahead, because it is conditional and nothing can evaluate "did they measure?" with the app closed. An occurrence whose window already holds a recording is never armed at all — cheaper than cancelling later, and it works for a reading taken on another device and synced here. Honest cost: follow-ups exist only as far ahead as they were armed; re-armed on every launch AND after every recording |
| ↳ Reminders — Snooze / Done on the notification | ⏳ | 🔬 | 🔬 | **v0.35.0, asked for as "until you snooze or deny manually".** Notification actions via `setNotificationCategoryAsync` — runtime calls into the module already in the binary, so this shipped as an OTA. Neither action opens the app: the whole point of "not now" is that it costs nothing. **Done** cancels that occurrence's second ask, matched on the slot AND the date it was due — the same slot has a follow-up armed for each of the next seven days, so cancelling by slot alone would silence the rest of the week. The response listener is at MODULE scope: a tap can arrive from the lock screen with no React tree alive, so a hook could not catch it, and the snoozed notification's words come from a cached copy for the same reason |
| ↳ Reminders — ⚠️⚠️ **v0.35.0 CRASHED THE APP; a persisted shape is untrusted input** | — | ✅ | ✅ | **Rolled back to the embedded bundle within minutes, fixed in v0.35.1.** v0.35.0 added `schedule.followUpMinutes`; every existing install had a schedule PERSISTED BY v0.34 with no such field, and `hydrate` was `{ ...state, ...payload }` — which replaces a nested object WHOLESALE, so the default never applied. Then four things that each look fine: it hydrated `undefined`; **`undefined !== null` passed the guard**; `new Date(NaN)` was built; and **an Invalid Date is TRUTHY**, so it survived `if (!followUpAt) continue` too. The OS scheduler threw inside a `void (async …)()` with no catch, and `useReminders` is mounted by the Tests TAB — so it fired on any navigation. Fixed in FOUR places because any one alone leaves the next version of this bug alive: `hydrate` merges nested objects + runs `normalizeSchedule`; `normalizeSchedule` validates types and ranges; the scheduler tests `Number.isFinite(getTime())` and wraps every OS call in `safely()`; every `void (async …)()` catches. ★ **tsc, both bundles and expo-doctor all passed on v0.35.0** — none of them can see a value that only exists on a device running an older build. Verified against the exact crashing blob plus 14 other malformed shapes |
| ↳ Reminders — ⚠️ concurrent applies had to be serialised | — | ✅ | ✅ | **v0.35.0, found by reading rather than reported.** `useReminders` is mounted in THREE places, and Settings + Reminders are on screen together whenever the editor is pushed. Every apply is cancel-then-set, so two concurrent ones could interleave their cancels and sets and leave duplicates or nothing. It would have presented as "sometimes I get two" — close to impossible to reproduce on purpose. All applies now go through a queue |
| ↳ Reminders — ★ "Check it works" + a one-minute test | ⏳ | 🔬 | 🔬 | **v0.36.0, after an hour was spent waiting for a follow-up that never arrived while the app could not say why.** The screen now reports what the **OPERATING SYSTEM** actually holds (`getAllScheduledNotificationsAsync`): daily set, follow-ups set. Every other reading there described INTENT — the switch, the times, the next one due — and intent was never in doubt. Plus "Send a test now": the REAL primary in 10 s and the REAL follow-up 70 s later, through the same content, category and actions, so a pass means plumbing works and only timing separates it from the scheduled one. ★ A feature whose shortest honest interval is thirty minutes is close to untestable without this, which is how the hour got spent. The likely original cause is instructive and was also undiscoverable: the crash rollback put the phone on 0.34.0 (no follow-up at all), and `normalizeSchedule` resets `followUpMinutes` to null on any pre-0.35.1 schedule — both give exactly "primary fires, second ask does not" |
| ↳ Reminders — the follow-up repeats, within an iOS budget | ⏳ | 🔬 | 🔬 | **v0.36.0.** Three times, ten minutes apart, all carrying the same `due` so ONE Done or one measurement cancels the chain rather than only the next. ⚠️ **iOS keeps at most 64 PENDING notifications and silently drops the rest** — 4 slots × 7 days × 3 repeats is 84, so occurrences are armed in TIME ORDER until a budget of 56 is spent: tonight matters, next Tuesday's third repeat does not. Android follow-ups get their own HIGH-importance channel (heads-up); the primary stays DEFAULT, because it is a routine nudge at a time the patient chose and nothing this app produces is urgent by construction. The follow-up differs by CONSENT: they switched on a thing whose job is to catch a miss |
| ↳ Reminders — ⏳ prominent on the iOS lock screen needs a rebuild | ⏳ | ⏳ | — n/a | Genuine prominence on iOS means the **Time Sensitive** interruption level, which requires `com.apple.developer.usernotifications.time-sensitive` — a native rebuild and a new capability on the App ID. Unlike the push entitlement stripped in 0.34.0 this one WOULD be honestly used, so it is a fair claim; it is simply not an over-the-air change. Android already has its equivalent (HIGH channel) as of v0.36.0 |
| ↳ Reminders — the second ask is ON by default | ⏳ | 🔬 | 🔬 | **v0.37.0.** Someone who set reminders at all has said they want to be caught when they forget; a reminder they slept through having no consequence is the case they were guarding against. One tap from Off, and a measurement silences it before it fires, so being wrong costs nothing. ★ The subtle part is telling **Off** from **not set**: `normalizeSchedule` keeps an explicit `null` (the patient chose Off) but takes the default for a MISSING field (written by a build that had no such field). Coercing the second to null is what silently left the follow-up off on every pre-existing install — and what had somebody waiting an hour for a notification that was never armed |
| ↳ Reminders — the editor fits on ONE screen | ⏳ | 🔬 | 🔬 | **v0.37.0, reported as "a whole page with scrolling just for a notification".** Four sections, three descriptions, a subtitle and a footnote became one card. Every cut followed one rule: **a control that explains itself needs no sentence under it.** `Off` became a SEGMENT of the follow-up control, which collapsed a switch, its description and their heading into nothing for identical expressive power; the two armed counts and the test button became ONE row whose VALUE is the count. 17 translation keys per language DELETED rather than orphaned. Kept: the permission warning (every control above it is a lie without it) and the armed count (fact, not intent) |
| ↳ Reminders — **the web port is outstanding** | ⏳ **pending** | — | — | The schedule type and all its maths (`defaultSlotTimes`, `resizeSchedule`, `nextOccurrence`) are already platform-neutral in `CYPHIX_SHARED`. Delivery is the part that cannot port: a browser can only nag a tab that is open, which is exactly why the schedule is defined apart from the thing that fires it. The web should read and EDIT the same schedule even if it cannot deliver it — a patient setting times on a laptop is a normal thing to want |
| Chat | ✅ | 🔬 | ✅ v0.92.0 | ★ **THE TAB IS A REQUEST FORM, NOT A CHAT (v0.92.0).** Asked for directly: *“אף אחד לא מצטאטט עם הרופא שלו כאילו זה ווצאף — זה צריך להיות ממש פתיחת פניה לreview של בדיקה”*. `ChatComposer` is DELETED; the tab is now Recording · Reason · Details · Send, plus a “Your requests” list. That is also what the SERVER ALREADY MODELS — `POST /patients/:id/messages/:mode` makes a message `kind: 'request'` the moment it carries a coded `reason`, takes `attachment: { recordingId, label }` and tracks `requestStatus` — so this is the app catching up with its own API, not a new idea. New: `FieldRow`, `ChoiceSheet`, `SendRequestButton`, `config/consultReasons.ts` (copied from web verbatim, SNOMED codes included). ⚠️ **DELIVERY IS STILL NOT WIRED and must not be faked.** No `messageApi` exists. Send runs the real sending state and then says in words that nothing was sent. A patient who believes they have asked a clinician to look at their heart, and has not, is the worst thing this screen can produce. **Web is `⏳ pending`** — it still has the old thread UI, and the request form has not been ported there yet |
| ↳ **Chat — the composer’s border beam (v0.90.0)** | ⏳ pending | 🔬 | ✅ verified on an emulator | Asked for as *“npm install border-beam”*, and **that package cannot be installed here**. Unlike `thinking-orbs` — same author, and split into a portable `engine` precisely so the RN port could share its maths — this one is CSS all the way down: 21 `@keyframes`, `conic-gradient`, `filter: blur()/hue-rotate()`, `matchMedia`. So `BorderBeam` is a PORT of the design read out of the published bundle (palettes, the sweep ramp, 360°/1.96 s), and the package is **not a dependency and not in `SOUP.md`**. ★ **It does not run all the time:** `active = focused || sending`, resting as a still gradient edge — `ThinkingOrb`’s header already says a Skia animation is “NOT fine as ambient chrome somewhere it could run for an hour”, and a composer is exactly that. Reduce Motion (live) and backgrounding stop it too. **Web is `pending`** — there the real npm package can simply be used, which is the opposite of the usual direction |
| ↳ ⚠️ **Chat — three builds of the beam drew NOTHING, and a bundle could not tell** | ➖ | 🔬 | ✅ | The CSS original is a **fixed** cloud of nine radial blobs with a conic **window** sweeping over it — which is why its light changes colour at each corner, and it is not what it looks like it is doing. Rebuilding that needs `ring ∩ window` as a mask, and in Skia: (1) a `DiffRect` carrying a shader child yields **no alpha** for a `<Mask>`, though the same `DiffRect` with `color="white"` masks perfectly; (2) a nested `<Mask>` over a rotating square did not draw either; (3) a gradient `transform` + `origin` rotates about (0,0), swinging the sweep’s centre off the box. `strength={4}` against all three changed nothing, which is what ruled out “too faint” and pointed at the shape. **Now one stroked `Path` with a `SweepGradient`, no masks at all.** The trade, stated: the beam carries its own blue→purple gradient instead of picking up a hue per corner. **Rule: if a Skia scene renders nothing, raise the strength ONCE to separate “invisible” from “absent” before changing any numbers** |
| ↳ ⚠️ **Chat — `Skia.Path.Make()` in a `useMemo` is a native object React does not own (v0.91.0)** | ➖ | 🔬 | ✅ fixed | **Reported as “the app crashed”.** v0.90.0 built the beam’s ring imperatively and keyed the memo on the MEASURED SIZE, so a new native `SkPath` was created on every layout pass while the old one was still referenced by the element tree. That is a use-after-free waiting for a measurement, and it takes the process down rather than throwing somewhere catchable. Replaced by a declarative `<RoundedRect style="stroke">`; no manual Skia object remains. ⚠️ **And `FailSoft` was never the protection it sounded like — it catches a REACT RENDER, not a native crash.** “It is wrapped in FailSoft” was true and irrelevant. **Rule: never hand a `<Path>` a `SkPath` built in a memo keyed on layout; if a primitive can draw the shape, use the primitive** |
| ↳ **Chat — the beam is driven by the TYPING, not by a clock (v0.91.0)** | ⏳ pending | 🔬 | ✅ | Reported as *“too bright, and it has nothing to do with how fast I type — it looks like fireworks”*. Every keystroke adds to an `energy` shared value that decays over 1.6 s, and that value moves BOTH brightness and speed (0.11 → ~0.5 turns/s). Adding to what is left rather than restarting is what makes FAST typing sit higher than slow typing instead of every keystroke producing the same flash. Peak brightness halved and the lit arc lengthened: a short bright arc reads as a flash going past, a long gentle one reads as light moving. **At rest it draws NOTHING** — v0.90.0 rested at 30 %, which is a coloured arc parked on an input nobody has touched (“יש פס קטן צבעוני על המלבן”) |
| ↳ **Chat — the box starts at one line and grows (v0.91.0)** | ✅ (a single-row input) | 🔬 | ✅ | *“המלבן גבוה מאוד על ההתחלה ולא תלוי בכמה טקסט כתבתי”.* The field carried a 44 pt minimum — the TAP-TARGET number — on something whose height is supposed to say how much has been written, and the whole box is the target anyway. `onContentSizeChange` now drives it from one line to a ceiling, and only on a real change (Android fires it on nearly every keystroke, and an identical height would re-render and re-measure the box for nothing). **Rule: a tap-target minimum is for a control, never for a field whose height carries meaning** |
| ↳ **Chat — tapping the thread dismisses the keyboard (v0.91.0)** | ➖ web has no keyboard to trap | 🔬 | ✅ | *“אי אפשר לצאת ממוד של הקלדה לנצח”*, and it was literally true: a multiline field makes Return a newline, and nothing else blurred it, so the only way out was to leave the tab. The thread area is now a `Pressable` calling `Keyboard.dismiss`, and sending dismisses it too. ⚠️ `accessible={false}` on that region — announced as a button it would put “double-tap to activate” in front of the empty-state sentence a screen reader is there to read. **Rule: any multiline field needs a stated way out of the keyboard; Return is not it** |
| ↳ ⚠️ **Chat — the composer was hidden behind the Android keyboard** | ➖ | 🔬 | ✅ fixed v0.90.0 | `KeyboardAvoidingView` with `behavior` on iOS only is the usual advice and is WRONG in this app: it is **edge-to-edge** (Android 15 makes that mandatory), so the window does not resize when the keyboard opens and `adjustResize` moves nothing. The box was typed into and completely invisible. `behavior="padding"` on both platforms. Passed `tsc`, both `expo export`s and `expo-doctor` without a word — only opening the tab on a device showed it. **Rule: any keyboard-avoiding layout in this app must be checked with the keyboard actually open, on Android** |
| ↳ ★ **Chat — naming the recording is the whole job (v0.92.0)** | ⏳ pending | 🔬 | ✅ verified on an emulator | The picker listed three studies from one afternoon as three IDENTICAL rows — “Jul 30 · 6 Limb Leads” × 3 — with no way for a patient to say which one they meant. The TIME is part of the label now. This is not cosmetic: the entire request is *about* that recording, so naming it is the one thing the form has to get right. And the chosen study keeps its `SIMULATION` badge ON THE FORM (`FieldRow.note`), not only inside the sheet it was picked from — a request to review synthetic data has to be recognisable as one where a clinician will read it (mobile CLAUDE.md §4). **Rule: a picker row that can repeat needs a disambiguator in the label, not in a tooltip** |
| ↳ ★ **Chat — the beam is on the SEND BUTTON, while sending (v0.92.0)** | ⏳ pending | 🔬 | ✅ verified on an emulator | Asked for: *“רק על כפתור השליחה, בזמן שליחה”*. It is also the only place on this screen where the animation MEANS something — work is in flight — instead of decorating a field somebody is trying to type into. ⚠️ It is mounted **above** the pill and **only while `sending`**, and the `only while` is what makes drawing over a control safe: the `Pressable` underneath is `disabled` for exactly as long as the canvas exists, so there is no tap for a native view to steal (mobile CLAUDE.md §1). `energy` is not passed — there is nobody typing to react to |
| ↳ ⚠️ **Chat — the beam RAN and could not be SEEN, for two compounding reasons (v0.92.0)** | ➖ | 🔬 | ✅ fixed | Both found only by pressing the button on an emulator; `tsc`, both `expo export`s and `expo-doctor` were clean throughout, and the code reads correctly. **(a) It was drawn UNDER the pill**, whose fill is opaque — so the crisp ring and the inner glow, which are the entire effect, were painted and then covered up, leaving a faint outer halo that reads as “the effect is broken” rather than “the effect is hidden”. **(b) Its strength had collapsed to ~6 % alpha**: `BorderBeam` multiplies brightness by `energy`, and with no energy source that defaulted to 0 — “nobody is typing, keep quiet”. But a caller passing no energy is not saying that; it is saying *work is in flight*, which is the opposite of quiet. Default is now 1 (`BorderBeam` v3.1.0). **Rule: a “no signal” default must be read as what the ABSENCE means to THAT caller — 0 was the right floor for a typing field and the wrong one for a spinner** |
| ↳ ⚠️ **Chat — a general FILE attachment is still missing (v0.92.0)** | ✅ web has attach | ⏳ pending | ⏳ pending | Reported as *“אין בכלל אופציה להוספת קבצים”* and only HALF answered here. The **recording** is the attachment the server’s request model takes, and it is now pickable; a photo or a document is not. Two separate blockers, both stated rather than worked around: (1) `expo-image-picker`’s `photosPermission`/`cameraPermission` strings in `app.json` currently promise the library is used **only** for a profile picture, so widening the use means changing them — an `app.json` change is a NATIVE REBUILD, not an OTA (§5A.1), and shipping a wider use under a narrower permission string is dishonest to the reader and an App Store review problem; (2) there is **no endpoint to receive a file** — `photoApi` is `PUT /patients/:id/photo`, the profile picture, and nothing else. `expo-document-picker` (already used by History) needs no permission change and is the OTA-safe half. **Next change** |
| ↳ ★ **Chat — the sending light FINISHES ITS LAP (v0.93.0)** | ⏳ pending | 🔬 | ✅ measured on an emulator | *“כשזה עושה sending האנימציה חייבת לסיים לפחות סיבוב שלם של האורות לשלמות וסיפוק”*. It was cut off wherever it had got to — about half a lap — which reads as an interruption. ★ **The fix is in the ANGLE, not in a timer:** `BorderBeam` counts turns MONOTONICALLY now, because a wrapped angle cannot tell “back where it started” from “never moved”, while on a running total every whole number IS home. On release it sets `landAt = max(1, ceil(turns))` — the next whole lap, and at least one whole lap however fast the work was — runs on to it, lands EXACTLY there (overshoot is up to 50 ms of travel, and stopping a few degrees short is visible on a ring this size), then fades. `onSettled` fires after the FADE, not at the boundary, or the caller’s unmount would cut off the ending the lap was completed to give. ⚠️ **Opt-in (`finishLap`), NOT the default** — an `energy`-driven beam slows to 0.11 turns/s when the hands stop, so finishing a lap there could hold a light on a blurred field for NINE SECONDS. ⚠️ **And the RESULT waits for it:** the button keeps saying “Sending…” and stays disabled until the light is home, because “Not sent” under a button still reading “Sending…” is two answers at once. The wait is bounded by one lap and never changes WHAT is reported, only when. **Measured from a screen recording:** button busy 1.10 s → **2.67 s**, and the beam crosses the top halo at 2.71 s and the bottom at 3.71 s = one complete revolution. (2.67 s rather than the 2.3 s the constants predict because the emulator renders below 60 fps and the per-frame `dt` is clamped at 50 ms.) **Rule: a “finish what you started” animation must be ended by its own ANGLE, not by a JS timer that will drift away from it** |
| ↳ **Chat — the title sits to the SIDE (v0.93.0)** | ✅ | 🔬 | ✅ | *“הכותרת ממורכזת אבל היא צריכה להיות בצד כמו שאר הכותרות”*. v0.92.0 centred it; every other title in the app is aligned to the side (`PageTitle` on Insights and History, and the plain ones on Settings, Personal details, Reminders), so next to those tabs it read as a different app. Aligned to the writing direction, so it sits right in Hebrew, and matched to `PageTitle`’s 30 / -0.4 typography rather than Settings’ 32 — Insights and History are the tabs either side of it. Not `PageTitle` itself: that fades on scroll to let a long list through, and this form is short and stationary |
| **Profile (medical card)** | ✅ | 🔬 | 🔬 | **v0.21.0: it is the signed-in patient's REAL record.** `usePatientCard` → `GET /patients/:id/card` for the account's own `linkedPatientId`: name, age, sex, MRN, phone, blood type, height/weight/BMI, conditions, allergies, medications, family history, emergency contact, care team — assembled and minimized SERVER-side, so the phone and the browser cannot disagree about what "age" or "BMI" means. Verified against the deployed API: every field populated for the demo patient. All sections were already ported with coded chips; only the source changed. **Not ported:** the ECG education guide |
| ↳ Card — the portrait | ✅ | 🔬 | 🔬 | v0.21.0: `GET /patients/:id/photo`, so a picture set in the browser appears on the phone (it lives inside the encrypted health profile, not in device storage). Initials when there is none — an equal alternative, not a failed image. **The avatar had NEVER rendered a photo before this, in any mode** |
| ↳ Card — setting the portrait FROM the phone | ✅ (profile picker) | 🔬 | 🔬 | **v0.22.0 — closed.** The avatar is a button with a camera badge (a tappable circle with no affordance is a circle nobody taps) opening take / choose / remove. Saved to the RECORD, not the device, so it appears in the browser too. **The picture is resized to 512 px on-device first** — `expo-image-manipulator`, the one new dependency, because the server caps the data-URL at 1.5 M characters and a square crop off a 12 MP sensor is several times that; the picker's `quality` alone lowers JPEG quality but never the pixel count. Verified against the deployed API: PUT → GET returns byte-identical → remove → the account is back as it was, and 1.6 M characters really is refused (400). **v0.25.3:** the badge was being CROPPED by the portrait's own circle — one view owned both the round `overflow: 'hidden'` mask and the Pressable the badge lived in, and a round mask crops the square's corners, which is exactly where the badge sits. The mask is its own inner view now and the badge is its sibling, painted on top of the circle. The wrapper stays 68×68 so the badge still lands inside its parent's bounds (Android does not reliably draw a child outside its parent) |
| ↳ ↳ …and the sign-up photo step | ⏳ (web has no photo step) | 🔬 | 🔬 | v0.22.0: the picture the wizard collects is uploaded once the account exists — it could not travel in the registration body, which has no patient id yet. **Best-effort by design:** the account and session are already created by then, so a failed upload must never report "sign-up failed" for a picture. The patient lands with initials and a working picker, which is why swallowing it is acceptable here and nowhere else |
| ↳ ↳ Portrait failures | ✅ | 🔬 | 🔬 | Three, kept apart because they need three different answers: the OS **refused** (a decision, not an error — the message says where to change it), the image could not be **encoded** (rare, but must not look saved), and the **upload** failed (the optimistic cache patch rolls back, so the old portrait returns rather than the new one appearing to have stuck). Audited as `patient:update` — a reference, never the image |
| ↳ Card — when it will not load | ✅ | 🔬 | 🔬 | **Deliberate, and the opposite of the obvious shortcut.** A real account whose card fails does NOT fall back to `DEMO_CARD` — printing a fictitious blood type and medication list under a real person's own name, on their medical record, is the worst failure this screen has. It shows a name-only card, an explicit "could not be loaded" notice (empty sections would otherwise read as "you have no conditions and no allergies"), and pull-to-refresh. `DEMO_CARD` is now the OFFLINE card only |
| ↳ Card — a clinician's Profile tab | ✅ (patient picker) | 🟡 | 🟡 | A clinician has no single "active patient", so `useActivePatientId` returns null rather than inventing one and they still see the demo card. A picker is the follow-up: ⏳ |
| ↳ ★ Card — LIST editing (allergies · medications · family history) | ✅ | 🔬 | 🔬 | **Shipped v0.39.0, never recorded here — this row is the correction.** `CardListEditor` bottom sheet per category, diff-only `PATCH /patients/:id/card` (only the edited category is sent), draft kept open on failure, demo card read-only by design. ⚠️ **v0.54.0 fixed a data-loss bug in it:** the editor's `CodedAnswer` shape has no dose, and the server replaces the whole medications array — so opening Medications and pressing Save wiped every dose. The dose is now rejoined by name on save. Dose *editing* is still ⏳ |
| ↳ ★ Card — PERSONAL DETAILS editing (height · weight · blood · emergency) | ✅ (web edits via its own card UI) | 🔬 | 🔬 | **v0.54.0.** `PersonalDetailsScreen`, pushed from the Details / Emergency section headers (sheet rejected — the Reminders precedent). Reuses the onboarding step bodies (`MeasureSlider`+`UnitToggle`, blood grid with first-class "I don't know", contact fields + relation chips) through `authPalette(dark)`. Diff-only patch; sliders track *touched* separately from *different* so a fallback value is never written; a half-typed emergency contact **blocks** saving (server requires name+phone+relation) instead of being dropped. **Identity fields (name/DOB/sex/phone) shown and named as clinic-changed — contractually excluded from `PatientCardPatch`, deliberately not editable on any platform** |
| ↳ Card — empty sections render (emergency contact · care team) | ✅ | 🔬 | 🔬 | **v0.54.0.** Both were conditionally rendered: a patient with no emergency contact saw NO section and therefore had no way to add one — contradicting the Section header's own comment. Always rendered now with honest empty sentences; Emergency carries the Edit affordance, Care team stays read-only (the clinic assigns itself) |
| Account popover (top-end avatar) | ✅ | — | — | **Deliberate divergence.** Mobile has no top bar to hang an avatar off — the dock is the whole navigation. Its contents (theme, background) live in Settings instead, reached from the bottom of Profile |
| **Settings** | ✅ | 🔬 | 🔬 | Ported page: Appearance · Notifications · Care connection · ECG Device · Privacy & Security · Account · About, with the same pastel illustrations. See the Settings table below for the rows that differ |

## Onboarding / auth (v0.19.0 — from the "CYPHIX Onboarding" design reference)

The signed-out experience. It stands in FRONT of the navigator (`AuthGate`), so
none of the tabs mount until there is an account. The web has the same flow in a
different shape (`AuthPage` + `RegisterWizard`); the mobile one follows the
design reference the user supplied, and the rows below say where the two part
company.

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| **Splash** | ✅ (its `AuthGate` restore splash) | 🔬 | 🔬 | **v0.34.2 — now MATCHES THE WEB: the `CyphixWordmark` on WHITE, plus a small busy ring.** The web has two branded loading surfaces and they are not interchangeable: `LoadingScreen` (blobs + orbiting spinner + full lockup, the showy one) and `AuthGate`'s restore splash (wordmark, page background, nothing else). This is the second, at the user's instruction — and the right one for the job: the screen exists because a disk read is in flight, which is a fraction of a second and is not an occasion. WORDMARK not `BrandLogo`, because the lockup adds the mark and "MEDICAL" — full identification, correct on a report where the issuer of a clinical document must be unambiguous, heavy on a screen up for under a second. ⚠️ **The status bar had to flip to DARK glyphs**: light ones were right on navy and are invisible on white, and typecheck, both bundles and doctor all pass with an unreadable clock. The floor went **1700 → 900 ms** — 1700 existed so the old ENTRANCE ANIMATION could land, and that animation is gone; the only job left is not flickering. The version line stays (with OTAs landing several times a day it is the fastest answer to "did my change reach the phone?") but moved `muted` → `label`, since #B3BCC9 at 75 % on white is a line that is in the render tree and not on the screen. ★ **This is the SECOND time white was tried**: v0.19.3 went white with a mark-only lockup and v0.19.4 reverted it. That revert is not evidence against white — the objection was the cropped MARK, not the background. **Superseded detail below, kept because the traps are still true:** NAVY screen, in both themes: the full `BrandLogo` (mark + CYPHIX + "MEDICAL") fading up, then the tagline. Its width is **90 % of the window capped at 520**, drawn with the new `crop` — a fixed point size is a guess that is right on exactly one screen. ⚠️ **TRAP (v0.19.5): an inherited viewBox is not necessarily tight around its artwork.** `BrandLogo`'s ink measures x 41.34 → 181.45 inside a box declared 34 → 209 — 7.3 units of air on the left against 27.6 on the right — so centring the BOX put the artwork ~18 pt left of centre and filled only 80 % of the width it claimed. It was invisible everywhere else because every other caller pins the logo to a corner, where trailing padding is just a gap. `crop` swaps in the measured ink box and is **opt-in**: fixing it globally would resize the logo on screens nobody has complained about. Real lockup width went 256 → 351 pt on a standard iPhone. ⚠️ **v0.19.4 REVERTED v0.19.3**, which had made this a WHITE screen carrying a mark-only `CyphixLogo`: three things changed when only the size was the complaint. `CyphixLogo` is deleted rather than left uncalled — three lockups with two callers is how the wrong one gets picked later (`git show 542a650`). The two that remain are still not interchangeable: `BrandLogo` is the full signature (shell, profile, reports, this screen) and `CyphixWordmark` is the word alone, for over a headline. **v0.19.1 removed the reference's pulsing ECG mark** — two rings around a stylised trace that drew itself — at the user's instruction: it is not the CYPHIX identity, and a mark that behaves like a logo without being one teaches people the wrong thing to recognise. `EcgSweepMark` is deleted, not left unused. Held for 1 700 ms **minimum**, with the session check running inside that window, and 4 000 ms **maximum**, after which the app opens signed out rather than staring at a logo. It carries the version, so "is my build actually on the phone?" is answered on the first screen anyone sees |
| **App icon** | ✅ (favicon) | 🔬 | 🔬 | **v0.23.0 — it was still Expo's blue placeholder**, construction guides and all. Now the CYPHIX mark on **white** (the user's choice), with the Android adaptive background/foreground/monochrome layers and the favicon regenerated together. `scripts/make-icons.js` renders them from **`BrandLogo`'s own path data, copied verbatim** — a hand-traced lookalike is a second unlinked copy of the brand that drifts the first time either changes. The counter-dot is punched through as **real transparency** where it matters (the Android themed icon is tinted by the system, so a filled dot would tint with it), while `icon.png` is fully opaque because iOS rejects an alpha channel. Sizing is per-platform on purpose: **60 %** on the square icon (iOS masks to a squircle and an organic mark needs air) but **42 %** on the Android foreground, whose outer third is croppable — only the inner 66 % is guaranteed visible on every launcher shape. Regenerate with `node scripts/make-icons.js`; the rasteriser is a scanline fill, so it is ~1.4 s, not the per-sample point-in-polygon that never finished |
| **Welcome** | ✅ (`AuthPage` choice mode) | 🔬 | 🔬 | A PHOTOGRAPH of the device in use (v0.19.1, replacing the reference's flat navy panel) over Create account / Sign in / two platform marks. **Divergence:** the web's is a centred wordmark card |
| ↳ Welcome — when the photo ARRIVES | — | 🔬 | 🔬 | **v0.22.0, from a real report: "the screen is blue and only then the picture comes up".** Not the file (1400 × 1066, 176 KB) — the *timing*: nothing asked for the image until this step mounted, so the fetch and decode happened while the patient was already looking at it. In **Expo Go** it is worse, and that is where it was seen: a `require`d asset is not in the client, it is pulled from the Metro dev server over Wi-Fi on first use. Two changes: `prefetchHero()` runs inside the 1.7 s splash the app already holds (dead time, and no new dependency — `resolveAssetSource` + `Image.prefetch` are React Native's own), and the photo now FADES IN over the navy in 260 ms instead of appearing between two frames. The layers are explicit now (image / scrim / copy) so only the picture animates — the wordmark and headline never wait for it. **v0.26.0 finishes the job — see the row below** |
| ↳ Every bundled photograph is warmed, and warmed early | — the browser has a real HTTP cache and no Metro between it and its own `/assets` | 🔬 | 🔬 | **v0.26.0, from a second report: the sign-in picture AND the START TEST guides arrive seconds late, "they should be part of the build".** They are — in a **Release** build; `expo export` embeds all four `.jpg`s. In **Expo Go / Debug** a `require`d asset is not in the app at all: `resolveAssetSource` returns `http://<dev-machine>:8081/assets/…` and RN fetches it over Wi‑Fi on first render, queued behind Metro serving a 5.7 MB JS bundle — so a 36 KB photograph takes seconds and the *app* looks slow. Two real defects behind the symptom: (a) v0.22.0 warmed the **hero only**, because that was the one reported, leaving the three measurement guides to be fetched at the exact tap that shows them; (b) it started from `AuthGate`'s effect, which mounts **behind `PreferencesGate`'s storage read**, so part of the splash the fetch is meant to hide inside was already spent. Now: one registry of every bundled photograph (`services/media/imagePreload.ts`), warmed **in parallel** — each independently, since one `try`/`catch` around a sequence of `await`s lets a single rejection abandon every image after it, and a local `file://` asset *can* reject in Release — started at **`App.tsx` module scope**, before the first render. `heroImage.ts` is now only the asset. ★ A new image asset belongs in that list or it is not preloaded |
| ↳ Welcome — the scrim | — | 🔬 | 🔬 | Structural, not decoration: the photo is warm and LIGHT, so a navy gradient sits between it and the type — `0.45` at the very top (light status-bar glyphs stay legible), clearing to `0.10` where the picture is the subject, then `0.72 → 0.97` under the words. The headline is always on navy whatever the image does underneath. ⚠️ The 34 pt rounding is on the WRAPPER: a native image view ignores a parent's `borderRadius` on Android unless the clip belongs to the view that owns `overflow` |
| ↳ Welcome — the lockup | ✅ full wordmark | 🔬 | 🔬 | v0.19.1: the **text-only** `CyphixWordmark` (no mark, no "MEDICAL"), as the reference has it — a second line of type directly over a headline argues with it. Path verbatim from the brand file; ⚠️ that file is an **A4 Inkscape page**, so the viewBox here is the glyphs' measured bounding box or the word renders as a speck on an empty sheet. `BrandLogo` stays the full lockup everywhere else |
| ↳ Apple / Google sign-in | ⏳ | ⏳ | ⏳ | **Drawn, not wired** — both land on the e-mail form. Neither can work until the server holds the client secrets and an OAuth redirect exists. Recorded rather than hidden, because the buttons ARE in the design |
| ↳ Terms / Privacy Notice | ✅ (routes exist) | 🟡 | 🟡 | The sentence names both documents and they are deliberately **not tappable**: no such document exists in this app yet, and a link that opens nothing is worse than a name. They become links the day there is something to open |
| **Sign in** | ✅ `LoginForm` | 🔬 | 🔬 | E-mail + password with Show/Hide, "Forgot password?", and one honest line per `AuthErrorCode`. Same enumeration-safe rule as the web: no-such-account and wrong-password share one message |
| ↳ Face ID / Touch ID / fingerprint | — (no web equivalent) | 🟡 offline only | 🟡 offline only | **Mobile-only**, `expo-local-authentication`. Drawn ONLY when the device has the hardware, has something enrolled, AND remembers an account — all three, or the offer could not be honoured. **v0.20.0: connected to a server it is NOT offered, deliberately.** The only thing a fingerprint could release there is the enclave's refresh token — and if that token is still valid, `restore()` already signed the patient in and this screen was never reached; if it is not, no gesture revives it. The button would be decorative, which is exactly what `biometrics.ts` refuses to draw. What this wants to become is an **app lock** — **and v0.40.0 built it; see the App lock row below.** This row's own verdict is unchanged: against a real server the *sign-in* button stays unoffered, because `restore()` has already opened the session by the time that screen could be reached |
| **Reset password** | ✅ | 🟡 | 🟡 | Sends nothing and says so honestly: "if that address is on an account, a link is on its way" — which is also exactly what a real server should answer. **Still true when connected: the server has no mail sender** (`AUTH_ROUTES_PLANNED` in `@cyphix/shared` names the route it will have). The screen is honest, the feature is ⏳ on the SERVER, not on mobile |
| **Register — credentials** | ✅ | 🔬 | 🔬 | Name · e-mail · password with a live strength meter (width AND colour animated over 300 ms). The score is `passwordStrength()` from `@cyphix/shared`, so "fair" means the same thing on both platforms. The address is checked for an existing account HERE, on the step that owns the field — not five screens later. v0.19.2: the name capitalises itself as it is typed ("elio" → "Elio") — `autoCapitalize` only suggests a shift state to the keyboard, so the VALUE is normalised in the reducer instead. It never lower-cases ("McDonald" survives) and does not treat apostrophes as boundaries |
| **Register — phone + code** | ⏳ | 🟡 | 🟡 | **Mobile-only step, from the reference.** The country code cycles on tap (Israel first — this app ships Hebrew); the number and the six-digit code are typed on an **in-page pad**, not the OS keyboard, which would cover the primary button and differs per Android OEM. There is no SMS gateway **on either side of the wire** — connecting to the server did not change that, so the step still SAYS it is a demo build and prints the code it will accept. The number itself is real and is stored on the server as the FHIR `telecom`; what is not verified is that the patient owns it: ⏳ |
| **Register — six health steps** | ✅ (`RegisterWizard`, fewer steps) | 🔬 | 🔬 | Sex · height · weight · blood type · emergency contact · photo, each with a progress rail, a Skip, and a sentence giving the CLINICAL reason it is asked. Height and weight are sliders with a CM/FT · KG/LB toggle; cm and kg are what get stored whichever unit is displayed |
| ↳ Emergency contact — **deliberate divergence** | ✅ typed | 🔬 | 🔬 | The reference opens on the phone's CONTACT LIST. Mobile types the number instead: reading the address book needs a permission and a native module, and a mis-tap in a list of real people silently writes a real person into a medical record. The native picker (`expo-contacts` / `presentContactPickerAsync`) is the follow-up — ⏳ |
| ↳ Profile photo | ⏳ (the web profile has no picker) | 🔬 | 🔬 | `expo-image-picker`, square-cropped, camera or library, with a **spoken refusal** when the OS says no. The six avatar tones with initials are an equal alternative, not a consolation — a photo on a medical record is a real thing to decline |
| **Review** | ⏳ | 🔬 | 🔬 | Every value with a way back to the step that set it, and the gaps NAMED ("you skipped blood type") rather than left blank. The account is created from THIS screen, so abandoning the flow earlier leaves nothing behind |
| **Profile created** | ⏳ | 🔬 | 🔬 | Pulse ring + filled check. Both buttons enter the app — the reference's own behaviour; "Pair my device" cannot mean more until the flow can hand the home screen an intent (⏳). ⚠️ It is reachable at all only because of the `justRegistered` latch in `authSlice` — see the trap below |
| **The reference's home screen** | — | — | — | **Deliberately NOT ported, at the user's instruction.** The app already has a Home; the reference's is a mock-up of one |
| Onboarding — animations | — | 🔬 | 🔬 | All four keyframes are ported rather than approximated: `scrIn` (16 px + fade, 320 ms, `cubic-bezier(.22,.7,.3,1)`) on every step, direction-aware so going back does not look like going on; `fadeUp` **with its `both` fill mode** (invisible during its delay, never flashed then animated); `pulseRing`; `sweep`. Reanimated, UI thread |
| Onboarding — **typography divergence** | — | 🔬 | 🔬 | The reference sets **IBM Plex Mono** on every label, digit and keycap. Mobile ships the SYSTEM font (root `CLAUDE.md` §3.1), so what carries over is the TREATMENT — 10.5 pt, caps, `.1em` tracking — plus `tabular-nums` wherever digits change in place (the 64 pt readout, the OTP cells, the pad), so a changing number never shifts the layout under a thumb |
| Onboarding — dark mode | — | 🔬 | 🔬 | The reference is light-only. `theme/authTheme.ts` carries a dark translation (navy stays navy; the white page becomes the app's dark surface) because the app has a theme switch and a flash of white at 2 a.m. is a real complaint |
| Onboarding — RTL | — | 🔬 | 🔬 | Hebrew throughout, with the back arrow, the rows and the text alignment following the reading direction. **The sliders and the numeric pad stay LTR:** a phone number and a measurement range are not reordered by the language around them |
| **Auth service (the swap point)** | ✅ `HttpAuthService` | 🔬 | 🔬 | **v0.20.0 — DONE. The two apps now share one account system.** `EXPO_PUBLIC_API_BASE_URL` set → `HttpAuthService` against CYPHIX_SERVER `/api/v1/auth/*`: an account made in the browser signs in on the phone and is the same `users` row, the same FHIR Patient, the same recordings. Empty → the device mock, so the offline demo is untouched. Both are typed `MobileAuthService`, so a method one of them forgets is a compile error rather than a dead screen. Verified against the live server + Neon DB (login · `/auth/me` · rotation · replay-kills-family · logout · 409/400 mapping · row scoping) — but the SCREENS are `🔬` until someone signs in on a handset |
| ↳ Token storage | ✅ memory + localStorage | 🔬 | 🔬 | Access token (15 min JWT) in **memory only**; the rotating refresh token in the **Keychain / Android Keystore** (`expo-secure-store`), never AsyncStorage (root §3.4). The web keeps its refresh token in localStorage — that is not drift, it is the strongest store each platform has |
| ↳ Refresh / 401 handling | ✅ | 🔬 | 🔬 | Behavioural twin of web's `httpBaseQuery`: bearer on every call, **one single-flight** refresh across parallel 401s, one transparent retry. Single-flight is correctness, not tuning — the server treats a replayed rotated-out token as theft and revokes the family, so two parallel refreshes would sign the patient out. **Divergence:** the refresh token travels in the request BODY, not a cookie; a native app has no cookie jar, and the server reads the body first |
| ↳ Session expiry → the door | ✅ `sessionExpired` | 🔬 | 🔬 | Same bridge as web: the service layer dispatches `sessionExpired`, the slice drops the principal, `AuthGate` shows onboarding. **v0.40.0 made "offline is not signed out" true rather than merely intended.** It was already written down here and in `tokenStore`'s comments — but `refreshSession()` returned `SessionUser | null`, which cannot express it, so both callers read `null` as signed-out and a lost signal ended a session. `RefreshOutcome` (`@cyphix/shared` `auth/session.ts`) now names three: only `rejected` — a server that **answered and refused** — clears the enclave and shows the door. A **5xx is `offline`, not `rejected`**, which matters specifically because a sleeping Render service answers with one while it wakes. ★ Revocation got *stronger*: `rejected` clears the enclave, where the old bounce left the refresh token sitting in it |
| ↳ ★ **THE RANDOM SIGN-OUT — four client causes closed (v0.71.0)** | ⚠️ web still has cause (a) | 🔬 | 🔬 | Reported as *"sometimes the app just logs me out and sends me back to the login screen — probably something on the server."* Half right: SIX causes across both sides, four here, two in **CYPHIX_SERVER v0.7.0** (which must be deployed for the most likely one to be fixed). **(a)** Only **401/403** end a session. "Any 4xx" also covered **429** — `/auth/refresh` inherits the server’s GLOBAL 300/min limiter keyed on `req.ip` with `trustProxy`, so clinic Wi-Fi and carrier CGNAT share one bucket — and **404** from a rollback or a base-URL typo, which would have signed out every user at once. **(b)** A failed enclave write is no longer fatal: `memoryRefreshToken` holds the newest token this process has seen and is preferred over the enclave, which becomes the COLD-START source. Before this, a swallowed SecureStore write left a REVOKED token on disk while `storeSession` reported success — the app ran fine for ~15 min, then met the server’s replay detection. **(c)** ONE revalidation at a time. `refreshSession` was single-flight but releases when one exchange settles, and `AuthGate` dispatches `revalidateSession` from three places that fire in the same second on a foreground — three probes returning 401 chained three SEQUENTIAL rotations. The lock moved up to `revalidate()` itself, because the probe is where that 401 comes from. **(d)** A 401 on a request sent with a NOW-STALE access token is retried, not refreshed (`prepareHeaders` reads the token at send time), so one tap fanning out to four queries no longer chains four rotations. **(e)** `RECOVERY_TIMEOUT_MS` 20 s → 60 s: the comment beside it already said a Render container takes ~50 s to wake and that a lower ceiling "is not a timeout, it is a guaranteed loss", then set one at 20 s. ⚠️ **`tsc`, both bundles and expo-doctor cannot see ANY of this** — every one is a timing race or an error path that fires only against a cold server, a locked screen or a lost packet. Stays 🔬 until the phone has gone a real stretch with no surprise sign-in; Settings → About names the last session event if it recurs. **Web is NOT at parity**: its `httpBaseQuery` is still on the old two-outcome contract and signs out on ANY refresh failure — mobile’s own pre-v1.2.0 bug, a violation of root CLAUDE.md §2.2, tracked here rather than left silent |
| ↳ **A session survives a cold start with no server** | ✅ (a browser tab holds a refresh cookie; nothing to port) | 🔬 | 🔬 | **v0.40.0, mobile-shaped by necessity.** The principal (`PersistedPrincipal`: id, display name, role, `refreshExpiresAt`) is written into the **secure enclave beside the refresh token**, by the same call, cleared by the same call — a principal with no credential behind it and a credential whose owner has been forgotten are the two worst states this layer can hold, and both are reachable the moment they live apart. Nothing clinical is in it. ★ **It grants nothing new:** the access token is memory-only, so it is gone after a cold start and every request 401s until a real refresh succeeds — the server stays the sole authority over data, and what opening early unlocks is the device cache that was already on the device. Bounded by the refresh token's lifetime, which the **server now states** (`refreshExpiresInSec`, CYPHIX_SERVER v0.4.0) instead of the client hard-coding 30 days and never learning we changed it |
| ↳ ⚠️ The persisted principal needed a MIGRATION | — | 🔬 | 🔬 | **v0.40.2, and REBUILT in v0.40.3 because the first attempt was the original bug wearing a different hat.** Before v0.40.0 the enclave held a refresh token and nothing else — the principal was never written down — so every phone already signed in when that update landed had a valid token, no principal, and `readPrincipal()` → null → the sign-in screen. It presented as "it STILL sometimes goes to login", and looked intermittent because it happened once per install and signing in repaired it. ⚠️ **v0.40.2 fixed it by awaiting the refresh INSIDE `restore()` — i.e. by making restore touch the network again, the precise thing v0.40.0 exists to have stopped.** `AuthGate`'s 4 s ceiling then raced it and beat it every time against a ~50 s cold start: ceiling fires, thunk still pending, door shown to somebody holding a valid credential. Reported as "I force-quit and it goes straight to login". ★ **The lesson is about the ceiling, not the timing:** 4 000 ms was chosen to bound a DISK READ, and putting a network call behind it is two different waits sharing one number. Now `restore()` is a pure disk read reporting `hasStoredSession`, the slice latches `recovering`, and the GATE holds the splash on its own `RECOVERY_TIMEOUT_MS` (20 s) — a wait that must be bounded belongs where the bound lives. Costs at most one launch per install |
| ↳ Settings › About — what the enclave holds | — | ✅ | ✅ | **Diagnostic, v0.40.3, and it exists because two rounds were spent guessing.** "It sent me to the sign-in screen" has four indistinguishable causes — no token, a token with no principal, an expired principal, a Keychain that will not answer — and from a Windows machine only the phone can say which. Prints `token + principal` / `token only — will recover on next launch` / `no stored session` / `token + EXPIRED principal` / `enclave unreadable`, **plus the last thing that happened to the session** (v0.40.5, `noteSessionEvent`) — kept in AsyncStorage deliberately so it SURVIVES the sign-out that clears the enclave, which is exactly when someone wants to read it: `no stored session · last: refresh refused by server (401) @ 14:02`. A fact about the device, never advice, and it names no secret: whether a token exists, not what it is. English like the build label and the glass material, so a bug report quotes a greppable string. Exactly the precedent `GLASS_MATERIAL` set in v0.24.1 |
| ↳ ⚠️ Keychain accessibility (`AFTER_FIRST_UNLOCK`) | — (a browser has no keychain) | ✅ | ✅ n/a (iOS-only attribute) | **v0.40.5, and the cause of a spontaneous MID-SESSION sign-out.** `expo-secure-store` defaults to `WHEN_UNLOCKED` and nothing here had ever set `keychainAccessible` — so the item is unreadable **and unwritable** while the screen is locked. With rotating refresh tokens that is a logout generator by two routes: ① a refresh while locked reads empty → `rejected` → the door; ② **worse**, the refresh SUCCEEDS, the server rotates the old token out, the write of the new one fails, `storeSession` swallowed it — so the enclave keeps a REVOKED token, the next refresh presents it, and the server's replay detection kills the whole family. One swallowed write, total logout minutes later, nothing on screen connecting them. ★ **Our own rotation policy is what turns a silent write failure into a total sign-out**, which is why an empty `catch` was never acceptable there: the write is retried and recorded now, and an empty token read beside a LIVE principal is `offline` rather than `rejected` (they are written and cleared together, so that pair is a failed read, never a revocation). ⚠️ **The first pass at this fix covered every keychain READ and missed the WRITE** — i.e. it fixed the mild cause and left the dangerous one untouched; caught on review, recorded because the write is both the one that matters and the one that looks least like a security setting. Nothing in a typecheck, a bundle or `expo-doctor` can see any of this |
| ↳ ★ **Rotation can tell a LOST reply from a STOLEN token** | ✅ (server-side — the fix lands for the web app too, unchanged) | 🔬 | 🔬 | **v0.45.0, and the cause that survived the two fixes above.** Reported a third time as *"sometimes I'm in the app and it suddenly switches to the login screen and disconnects me on its own"*. The Keychain fix (v0.40.5) and the offline-≠-signed-out fix (v0.40.0) were both real and both correct; **this one was never fixable from the app.** `rotateRefreshToken` retired the presented token and issued its successor in the same breath, so from that instant the old token was a trap: a reply that did not reach the enclave — dropped socket after the server committed, unreadable body through a captive portal, app suspended mid-write, iOS refusing the Keychain write while the screen is locked — left the phone holding a token the server had already killed. Presenting it read as **theft and revoked the entire family**: 401 on everything, no recovery, minutes after the event that caused it, with nothing on screen connecting the two. `refresh_tokens.replaced_by` (CYPHIX_SERVER v0.5.0, migration `0003_refresh_grace.sql`) records which token replaced which; a successor that has **never been presented**, within 60 s, proves the reply never landed ⇒ retransmission, not replay. ★ **The trade is stated, not hidden:** for 60 s after a rotation a replayed token no longer immediately fells the family — the "reuse interval" of OAuth 2.0's rotation guidance. Outside the window, and for any successor an attacker actually used, detection is unchanged; NULL `replaced_by` on every pre-existing row keeps the old path, so there is no backfill and nothing already issued changes behaviour. ⚠️ Also flipped the order to **issue-then-retire**: revoking first meant a failure mid-rotation left a retired token with no successor recorded — the exact orphan state the grace window cannot rescue, i.e. the bug was reachable from inside its own fix. Verified by a 17-case behaviour matrix over an in-memory stand-in for the driver (ordinary rotation · retransmission · reuse-after-successor-used · replay outside the window · pre-migration rows · unknown tokens). That exercises the branching, **not** the SQL against a real Postgres: there is no Docker on this machine and the only reachable database is production |
| ↳ ★ **The liveness probe stopped rotating the session** | — (the web has no foreground/backoff revalidation loop; its `refreshSession` fires only on a 401) | 🔬 | 🔬 | **v0.45.0.** `revalidate()` was `refreshSession()` and nothing else, so **every caller rotated the refresh token** — and `AuthGate` calls it on every return from the background *and* on a 4 s→60 s backoff for as long as the app believes it is offline. The app was spending its most fragile credential over and over, most eagerly on exactly the flaky network that loses a rotation's reply (the row above). Switch to Messages and back: a rotation. Walk through a tunnel: one every few seconds. It now asks with the access token it already holds (`GET /auth/me`), which proves the same two things the caller actually wants — the server is reachable, this session is still recognised — and rotates only when that cannot answer: no access token at all (a cold start) or a 401 because the ~15-minute access token aged out. **One rotation per ~15 min of use instead of one per foreground, and none at all while offline.** ★ Every unexpected reply degrades to the OLD behaviour rather than to a guess: a 403, a 404 and an unreadable 200 all fall through to the full refresh. And a 401 from the probe is explicitly **not** a rejection — it says a 15-minute access token expired, which is ordinary and says nothing about the session. Revocation is untouched: a server that answers and refuses still ends the session on the same path, immediately |
| ↳ **App lock — "Lock when unattended"** | ⏳ (no web equivalent — a browser has no enclave and no device unlock) | 🔬 | 🔬 | **v0.40.0, NARROWED in v0.40.2 after it was used on a handset.** ⚠️ It first gated every COLD START, and that was wrong: reported as "it asks for Face ID every time I close and open it", with **Dexcom** named as the counter-example — and Dexcom is right. A CGM showing live glucose does not biometric-gate each launch, MyChart does not by default, and nothing in HIPAA or the MDR asks for one on a patient's own phone. The reason is structural: **the OS lock screen already IS that check** — you unlocked the phone to reach the app, so a launch prompt re-asks what the device just answered, and a lock that fires every time is one people switch off. It now covers only the gap the OS cannot: an **already-unlocked phone handed over with the app resident**, 5 min after backgrounding (was 60 s — shorter than fetching a code from Messages, so it fired during ordinary use). Off by default; offered only where `isEnrolledAsync` says the OS can honour it. Allows the **device passcode** as a fallback where the sign-in button does not — this is the only way in, so a sensor that will not read must not be a dead end. Flag lives in the **enclave**, not `AsyncStorage`, and outlives any one session. ★ **It is a gate on RENDERING** and is described as one: anyone who can defeat the OS's own check reads the cache files directly and never meets it. **Honest cost, recorded rather than glossed:** a cold start on an unlocked phone somebody else is holding is not gated — `authSlice`'s `restoreSession.fulfilled` is where that line goes back. ⚠️ **"Face ID only after 30 days" was asked for and is NOT implementable:** after 30 days the refresh token is dead, and only the server can issue new ones, which needs the password. Biometrics can gate a session that still exists; they cannot resurrect one |
| ↳ **Connection strip** (`Connecting…` / `Offline · showing saved data`) | ⏳ pending | 🔬 | 🔬 | **v0.40.0, and reworked twice on device — v0.40.1 and v0.40.4.** It went coloured toast → monochrome glass capsule → **no container at all**, which is where it should have started: the capsule was drawing a box around two words to announce they were worth putting in a box, and once the text is legible the frame is decoration. The fade is opacity-only as a CONSEQUENCE of that — a container can settle by scaling a few per cent, bare text scaling reads as a zoom. The obligation created by letting the app open on a restored session: a patient reading a record is entitled to know whether it was just confirmed or is the phone's last copy — the same principle as the streaming watchdog refusing to draw a frozen trace as live. **The steady state draws nothing.** `pointerEvents="none"` at every level, because it floats over the landscape exam and a strip that can eat a touch can break a measurement. ★ Reads **both** `auth.sessionMode` and the sync engine's phase — `sessionMode` only ever moves *towards* live (a confirmed session stays confirmed), so on its own it could never report a phone that connected at boot and walked into a basement an hour later. ⚠️ **v0.40.1 — the first version shipped as a coloured toast and was reported as ugly and un-native, having passed typecheck, both bundles and doctor.** (a) The green **"Connected" state is gone**: reconnecting is not an achievement, and a badge that appears *after* everything is fine is an interruption caused by the absence of a problem. The notice dissolving IS the confirmation; `connLive` was deleted from both locales, not orphaned. (b) It is `GlassSurface` now — the **dock's own material**, with the dock's tint arithmetic copied rather than re-tuned, since two surfaces of one material drift the moment they are tuned apart — and **monochrome**, because `attention` and `danger` mean specific things here and neither is "the wifi". (c) It **settles** (spring on scale from 0.94, no translation) rather than sliding in like a notification banner, and stays mounted at zero opacity so `offline → connecting` swaps the words instead of the whole capsule re-entering. Pending on web, where the same question (is this tab talking to the server?) is just as real |
| ↳ **Reachability, in both directions** (`sessionConfirmed` / `serverReachable` / `serverUnreachable`) | ⏳ pending | 🔬 | 🔬 | **v0.40.0, completed in v0.40.2 after a real report: "I was offline, the internet came back, and it stayed offline until I closed and reopened the app."** Nothing was watching for the network RETURNING — the boot revalidation runs once per account and the sync engine refreshes on foreground, and neither fires when the radio reconnects under an app already open. Two halves, both needed: `httpBaseQuery` reports reachability from **every request** (the only layer that knows — `netinfo` is a NATIVE module and so cannot ship over the air per §5A.1, and "the radio has an IP address" is not "CYPHIX is reachable" anyway, as any captive portal shows), and `AuthGate` **knocks on a backoff** (4–60 s) while offline, because an app on a screen that already has its data makes no requests to report from. A 4xx counts as REACHED: a 403 is the server being present and telling us something true. ★ Reachability may never clear the principal — a 404 proves the server is up and proves nothing about the session; only `rejected` ends one. Web should adopt this with the strip |
| ↳ Boot restore vs. a COLD server | ✅ (web has no splash ceiling) | 🔬 | 🔬 | **v0.40.0 — FIXED, and this row had predicted it.** It read: *"on the first launch after a long gap the welcome screen can appear and then be replaced by the app when the refresh finally lands… the fix is a warm server, or a restore that says 'reconnecting' instead of showing the door."* That is exactly what was then reported from the phone, and it is the second half of it — **the restore no longer touches the network at all.** `HttpAuthService.restore()` reads the principal persisted beside the token in the enclave and resolves in milliseconds; `AuthGate` opens on that. Revalidation runs behind the rendered app and on every foreground, and only a `rejected` ends a session. A cold start is now the same length with the server up, asleep or absent, and the 4 000 ms ceiling guards **storage** again — which is what it was written for — rather than racing a 50 s cold start it could never win. The "reconnecting" half is the `ConnectionStrip` row below |
| ↳ RBAC principal | ✅ real | 🔬 | 🔬 | **v0.20.0: the deliberate disagreement is over — where there is a server.** Connected, `useCurrentUser` answers with the SIGNED-IN account (role + `linkedPatientId`), because the server enforces its own RBAC and row scoping anyway: a client claiming clinician while the server says patient just draws buttons whose requests come back 403. Offline it is still the demo CLINICIAN, so the showcase keeps History's tools. ⚠️ Consequence to expect on a real patient login: calipers / filters / annotate / compare are hidden, by design. "Preview as role" remains the follow-up for demoing the other side |
| ↳ Medical card ← registration | ✅ | 🔬 | 🔬 | **v0.21.0 — closed.** The health profile reaches the SERVER at sign-up (FHIR Patient + encrypted `health_profiles`), including the emergency contact's relation (server `auth.ts` v0.2.1, extended because the mobile flow is the only one that asks), **and the Profile tab now reads it back** — see the card rows below |
| ↳ `emailExists` on the sign-up step | ✅ (mock only) | 🔬 | 🔬 | **Deliberate divergence when connected.** The step still fails on the field that owns the problem — but the server refuses to be an account-enumeration oracle (its login answers identically for unknown-account and wrong-password), so there is nothing to ask. Connected, the check returns `false` and `POST /auth/register` → **409** is the real verdict, landing on the same step with the same message one screen later |
| **Medical card — EDITABLE (allergies · medicines · family history)** | ⏳ **pending** | 🔬 | 🔬 | **v0.39.0 — MOBILE FIRST; the web editor is this row's IOU under the Cross-Platform Rule.** The section header carries Edit; one `CardListEditor` sheet serves all three categories over the blurred card, because a pushed screen per category is four screens and four ways to get lost for a two-tap job. Picks come from `CYPHIX_SHARED/types/healthCatalogue.ts` (14 cardiac allergies · 23 medicines · 10 family conditions) so three systems agree on what was meant, and **"something else" is always present** — a list that cannot express the real answer teaches people to pick the nearest wrong one, which is then recorded as true. Server: `PATCH /patients/:id/card` (server v0.3.0), audited by **field name only**. ⚠️ Two client rules: **only the edited category is sent** (echoing the whole card reverts anything changed elsewhere since load — invisible on one device, inevitable with two), and a failed save **keeps the sheet open with the draft**. The DEMO card has no Edit control: there is nothing behind it to write to |
| ↳ ★ Card editor — it took three releases to become usable, and the last two causes were not in the sheet | ✅ (a browser scrolls a dialog and has no floating dock) | 🔬 | 🔬 | **v0.39.1 + v0.39.2.** (a) `BottomSheet` had **no scroll view**: past the 82 % ceiling with `overflow: hidden`, content is CLIPPED, not scrolled, so Save was never on screen → `scrollable` + Save in the sheet's `footer`. (b) That pinned it into the ~90 pt the **dock** owns. The dock is the navigator's `tabBar`, a **sibling of the screen**, so nothing a screen renders can paint above it — `zIndex` orders siblings of ONE parent → overlays now render at the **app root** (`OverlayPortal`), which also finally dims the dock and stops it being tappable through a modal. (c) The rise started in the same commit that mounted ~100 views, and views are created **on the UI thread** — the one the native-driver animation runs on → the panel is committed off screen and rises on its first `onLayout`. All three shipped green on tsc, both bundles and doctor |
| Sign out | ✅ | 🔬 | 🔬 | v0.19.1: on the **Profile tab**, last thing under the Settings card — where every app a patient already uses puts it. Settings › Account keeps its row too; two doors to one action is right here. Both confirm first, because on a device-local account this costs the password (or a face). Without it the signed-out flow could never be reached a second time on a device that has completed it |

## Platform capabilities

| Capability | Web | iOS | Android | Notes |
|---|---|---|---|---|
| BLE ECG link (frozen GATT contract) | ✅ Web Bluetooth | 🔬 Swift/CoreBluetooth | 🔬 Kotlin/BluetoothGatt | `modules/cyphix-ble` — parsing off the JS thread, 10 Hz batches. ⚠️ **The Android ✅ here was wrong and is corrected in v0.23.0.** The Kotlin module is `@SuppressLint("MissingPermission")` and its header says the UI must hold the runtime grants before `connect()` — and nothing ever requested them, so a scan returned nothing, silently. Verified against the firmware: it *does* `addServiceUUID`, so the native modules' service-UUID scan filter is correct (the web filters by `namePrefix` with the service only as `optionalServices`, which had suggested otherwise), and it sends 16 × 9 B int32 µV @ 20 Hz, MTU 185 — the stride-9 path both parsers implement. **v0.65.0 — a SECOND characteristic, `ECG_DATA3` (`beb5483f-…`, firmware v3+):** `[seq][count][flags][rsv]` + count × 13 B `{i32 Lead I, i32 Lead II-a, i32 Lead II-b, u8 LOD}`, 12 samples = 160 B per 37.5 ms. **The legacy characteristic is untouched and stays byte-identical on v3 firmware**, so every build ever shipped keeps working; a client subscribes to **one of the two** — the 3-channel one when the device has it, else legacy exactly as before. Both native halves mirror `parseEcgPacket3` 1:1 and it is **STRICT** (length must equal 4 + count × 13 or the packet is dropped — a parser that guesses a stride is how a format change becomes a plausible wrong trace). `onEcgBatch` gains `leadIIb` (**always present, EMPTY on a legacy device** — that is how JS tells), new event `onDeviceFlags` on change of the flags byte; stream state is reset per subscription. ⚠️ **NATIVE REBUILD REQUIRED — NOT an OTA:** `modules/**` changed, `app.json` 0.35.0 → **0.36.0**, so every future `eas update` targets runtime **0.36.0** and nothing published before the new binary is installed will reach it (§5A.2). ⚠️ **Neither half has been compiled** (Windows machine) and neither has met a v3 device **2026-09-18 — what 🔬 meant here:** iOS — the Swift half COMPILES (EAS build #9, 0.36.0; built, not submitted). Android — the Kotlin half had NEVER compiled: a `return@AsyncFunction` from v0.1.0 is a type error under Kotlin 2, found by running `gradlew :cyphix-ble:compileDebugKotlin` locally; fixed, now `BUILD SUCCESSFUL`. Both remain 🔬: compiled is not run, and neither has met a v3 device |
| ↳ **Reaching the real signal at all** | ✅ | 🔬 dev build | 🔬 dev build | **v0.23.0 — the answer to "there is only a demo signal".** Not a defect: `requireOptionalNativeModule('CyphixBle')` is null in **Expo Go**, which cannot contain this app's native code, so `bleClient` falls back to `EcgSimulator` exactly as mobile CLAUDE.md §4 designs. Hardware needs a **development build** → Xcode → a Mac. `IPHONE_SETUP.md` is that route end to end for an **Intel** MacBook + a **free** Apple ID, gated on macOS 14.5 / Xcode 16.1 (SDK 54 / RN 0.81) because Apple blocks a newer Xcode on older macOS and there is no workaround. Free signing expires every **7 days** — documented, not a bug. The EAS cloud escape hatch is named but flagged: device installs there need the **paid** account |
| ↳ ↳ …built HOW, now the Mac is out | ✅ (browser) | 🔬 EAS cloud | 🔬 EAS cloud | **v0.27.0.** The borrowed MacBook is Intel on macOS < 14.5 → cannot run Xcode 16.1 → cannot build SDK 54, and Apple blocks a newer Xcode on older macOS. Paid Apple Developer account bought, so `eas.json` (new) has Expo compile `modules/cyphix-ble` on **their** macOS runners, driven from Windows. ★ **Expo Go can never do this** — it is a prebuilt App Store binary holding only Expo's own native modules, so the handle is null there *by construction*; that is the whole reason only a demo signal was ever seen. What the $99 buys is **signing**, not building: EAS builds for free accounts too, but installing on a physical iPhone needs ad-hoc / App Store provisioning that Apple issues only to Program members. `production` → TestFlight (valid a **year**, vs the free tier's 7 days), `preview` → QR straight to the phone for fast hardware loops, `development` → dev client + Metro. `appVersionSource: remote` + `autoIncrement` because TestFlight refuses a build number it has seen, and finding that out after a 20-minute cloud build is expensive |
| ↳ Streaming-stale watchdog | ✅ (`FRAME_STALE_MS`, vision) | 🔬 | 🔬 | v0.23.0, closing root CLAUDE.md §3.2, which required it and had nothing. A link stays `connected` while delivering nothing — locked phone, backgrounded app, slipped device — and the last waveform sits there looking live. `STREAM_STALE_MS` (600 ms) is **derived** from the frozen cadence (16 samples/50 ms + 10 Hz flush ⇒ ~100 ms healthy gap; 600 ms = six missed), and lives in `CYPHIX_SHARED` so web and mobile cannot drift. Three details carry it: **AppState marks stale on the way OUT** (a suspended app's timers never run to notice later), **only a real batch clears it** (announcing "live" on foreground before a sample lands is the same lie), and **`isStreaming` now means samples are arriving**, so an in-flight capture is **discarded** — ten seconds of wall clock is not ten seconds of ECG, and a strip padded with silence reads as asystole |
| ↳ Android BLE runtime permissions | — (browser prompts) | — (CoreBluetooth prompts from `NSBluetoothAlwaysUsageDescription`) | 🔬 | v0.23.0. `PermissionsAndroid` in `bleClient.connect()` — RN's own API, no new native dependency — splitting on API 31: `BLUETOOTH_SCAN` + `BLUETOOTH_CONNECT` from Android 12, `ACCESS_FINE_LOCATION` below. A manifest entry is **not** a grant, and an unpermitted `startScan` returns no results and throws no error, which on screen is indistinguishable from "the device isn't here". The refusal message names the permission: "Bluetooth error" sends someone to check a battery for a problem that is in Settings |
| ↳ **Android requests the ATT MTU (v0.65.0)** | — (the browser negotiates it) | — (CoreBluetooth negotiates it; 185 on every supported iPhone) | 🔬 **never run** | **Found while adding the 160 B packet, and it predates it.** A GATT link opens at MTU 23 — a **20-byte** notification — and only the *client* may ask for more; the firmware's `BLEDevice::setMTU(185)` is the ceiling it will agree to, not a request. The Kotlin module never asked, so by the ATT rules every 146 B legacy packet arrives cut to 20 bytes and is dropped by the stride check: *connected, no signal*. Now: `connect → requestMtu(185) → onMtuChanged → discoverServices` (GATT operations must not overlap, so discovery waits for the answer), with a 2 s fallback if the stack never answers and a straight-to-discovery path if the request cannot be queued. ⚠️ **Reasoned from the spec, not observed** — this half has never been run against hardware, and this changes how *every* Android connection opens |
| ECG simulator fallback | ✅ | ✅ | ✅ | Auto-selected when the native module is absent (Expo Go); UI badges it `SIMULATED` |
| Live waveform rendering | ✅ Canvas | 🔬 Skia | ✅ Skia | Mobile redraws at 30 Hz from the ring buffer; worklet-driven path is the next step |
| ECG DSP (Einthoven, filters, Pan-Tompkins) | ✅ | ⏳ | ⏳ | **Must be numerically identical** when ported — same frozen coefficients, no per-platform tuning |
| Gemini Live narrator | ✅ | ⏳ | ⏳ | Protocol is frozen; port as-is or route through the server |
| Auth (bearer + refresh) | ✅ | 🟡 | 🟡 | `httpBaseQuery` twin done incl. 401→refresh→retry; `refreshSession` still a stub, tokens in SecureStore. As of v0.19.0 there IS a session to carry — see the Onboarding table |
| Audit logging | ✅ | ⏳ | ⏳ | Port `auditLogger` (references only, never PII) |
| RBAC route guards | ✅ | ⏳ | ⏳ | Lands with auth |
| i18n — strings (en/he) | ✅ | 🔬 | 🔬 | v0.13.0. Same four-file shape as the web (`config` registry · `I18nContext` · `I18nProvider` · `useTranslation`) and the same key names wherever the copy is the same sentence, so the Hebrew is copied verbatim from the web locale rather than re-invented. `he.ts` is typed `Record<TranslationKey, string>`, so a missing key is a **compile error**, never a blank label. **Divergence:** the choice is stored in the preferences slice, not its own key — `PreferencesGate` already hydrates that before the first paint, and a separate async read would open the app in English and repaint in Hebrew a frame later. Screens call `tr()` (`const { t: tr }`) because `t` is already bound to the theme tokens app-wide |
| ↳ i18n — RTL layout | ✅ (`<html dir>` mirrors everything) | 🟡 | 🟡 | **Partial, and deliberately so.** Rows, section headers, metric tiles, chips, the profile identity block and the forward chevron all reverse and re-align off `useTranslation().rtl`. What is NOT done is native mirroring: that is `I18nManager.forceRTL()`, which is process-wide and only applies **after a relaunch** — flipping it under a patient mid-session, with no `expo-updates` to reload cleanly, would be worse than the gap. When it lands it needs an explicit "restart to apply" flow. Deliberately **never** mirrored even then: the interval-bar axis (scaleMin→scaleMax is a number line) and the ECG paper itself (time runs left→right on every ECG on earth) |
| ↳ i18n — what is NOT translated | — | ✅ | ✅ | On purpose, and each has a comment at the site: clinical `display` values on coded chips (ICD-10 / SNOMED data, not copy) · lead names (I, II, aVR…) · unit symbols (BPM, ms, Hz, mV, %) · the 25 mm/s · 10 mm/mV scale · `APP_BUILD_LABEL` (a developer identifier a bug report should quote verbatim) · the brand name · the platform's own BLE error text, which is shown untranslated rather than replaced by a generic sentence that says less |
| ↳ i18n — dock order under RTL | ✅ mirrors | — | — | Deliberately fixed. Home is the centre anchor with two items either side, so mirroring moves nothing meaningful — while the sliding pill's offset is `state.index * step`, indexed off `state.routes`. Reversing one and not the other lights the wrong tab |
| Theme (light/dark) | ✅ | 🔬 | 🔬 | Tokens ported 1:1 from `tokens.css`. Resolution order is the Settings choice, then the OS (`useIsDark`) — every surface must use that hook, never `useColorScheme()` directly, or the app goes half dark |
| Report geometry (mm grid + path) | ✅ own copy | 🔬 | 🔬 | **Now shared** in `CYPHIX_SHARED/src/ecg/ecgPath.ts` + `ecgGrid.ts`, consumed by mobile. An interval measured off the web sheet and off the phone must land on one ruler. ⚠️ The web still imports its own copies under `src/services/ecg/` and imports NOTHING from `@cyphix/shared` (verified) — until it migrates, edit both |
| Preference persistence | ✅ (`localStorage`) | 🔬 | 🔬 | `preferencesSlice` + AsyncStorage, hydrated before first paint. **Tokens stay in SecureStore** — non-secret settings must not be joined to them |

## Offline-first data layer (v0.29.0)

The one row that governs this whole table: **the sync CONTRACT is shared and
the server implements it once, so nothing here is a mobile-only protocol.**
What is mobile-only is the device-side storage — and that is the deliberate
divergence, spelled out row by row below.

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| Sync contract (delta envelope, cursor rules, 304 convention) | ✅ shared | ✅ shared | ✅ shared | `CYPHIX_SHARED/src/api/sync.ts`. Lives there and not in a client because "what does *unchanged* look like" is a protocol question — a server answering `304` and a client treating it as an error would produce an app that shows a blank profile **only when everything works** |
| `GET /recordings/sync` (changed + tombstones, paged) | ✅ server | ✅ server | ✅ server | Server-side, so all three platforms can use it. Cursor is the **DB clock**, never the app server's or the device's. Migration `0002` adds `recordings.updated_at`, backfilled from `COALESCE(deleted_at, created_at)` so the first delta after deploy is not "everything changed" |
| ETag → 304 on card + portrait | ✅ server (browser HTTP cache uses it for free) | ✅ server | ✅ server | Portrait's validator comes from `health_profiles.updated_at`, not a hash of the payload — hashing would mean decrypting 1.5 MB to discover nothing changed |
| Device mirror of Scan History (metadata index) | ⏳ **pending** | 🔬 | 🔬 | **The divergence.** Web has no equivalent and does not need one yet: a browser tab is not in a tunnel, and the ETag work above already saves it the payloads. An IndexedDB mirror for web is a separate, tracked item — not "we forgot" |
| Waveform files, fetched lazily, kept forever | ⏳ pending | 🔬 | 🔬 | Immutability is the licence: a trace does not change after it was measured. Only fetched when a study is actually **opened** — pre-fetching an account's whole history would be the app's biggest download on behalf of a screen nobody opened |
| Cache-first reads (`offlineBaseQuery`) | ⏳ pending | 🔬 | 🔬 | Wraps `httpBaseQuery`; caching is **opt-in per route**, listed in one table in that file. The alternative (cache all, exempt the dangerous ones) fails open, and failing open about a medical record is not a trade worth making |
| Last-known-good when the network is gone | ⏳ pending | 🔬 | 🔬 | Only for status `0` / `503` / `504`. A `403`, `404` or `500` is the server saying something true, and papering over it with old data hides a real fault behind a screen that looks fine |
| Sync triggers: sign-in · foreground · pull-to-refresh | ⏳ pending | 🔬 | 🔬 | **No polling and no timer, on purpose.** A phone in a pocket has nothing to learn; the moment the screen returns is the moment the answer matters, and that is the foreground event for free. Throttled to 60 s for automatic runs; manual refresh bypasses it |
| Write-through on mutations | ⏳ pending | 🔬 | 🔬 | Every recording mutation answers with the whole updated record, so it lands on disk before the request finishes settling instead of waiting for the next sync to discover a change this device made |
| Cache wiped on account change, kept on sign-out | — | 🔬 | 🔬 | `claimCacheFor`, run **inside the boot splash before the app renders**. Documents, mirror and cursors are wiped *together* — a cursor that outlives its data claims the device is up to date about records it no longer has. Sign-out keeps everything: same person, same device, and the tokens are cleared regardless |
| **Offline write queue (outbox)** | ⏳ pending | ⏳ **pending** | ⏳ **pending** | **Deliberately NOT in v0.29.0.** A capture saved with no signal still fails, exactly as before. It is the obvious next step — a BLE scan in a basement is a real scenario — but it needs conflict rules, a retry policy and a visible "not yet saved" state, and shipping half of it would be worse than shipping none. Named here so it cannot be mistaken for an oversight |
| Sync of a patient's card the clinician is *viewing* | ⏳ pending | ⏳ pending | ⏳ pending | The engine revalidates the **signed-in patient's own** card and portrait. Another patient's card is cached on first read and revalidated only on explicit refresh. Fine while the app is patient-facing; must be closed before the clinician flows ship |
| PHI at rest on the device: encryption | ✅ server-side (per-patient keys) | ⏳ **pending** | ⏳ **pending** | The mirror stores decrypted metadata and waveforms in the app sandbox. iOS file protection and the Android sandbox are the only guards today. `expo-secure-store` is for small secrets, not megabytes — this wants a key in the enclave and payload encryption around `deviceCache` |
| PHI at rest: excluded from iCloud / Google backup | — | ⏳ **pending** | ⏳ pending | Files live in the documents directory (so the OS cannot sweep them), which on iOS **is** backed up. `expo-file-system`'s current API exposes no backup-exclusion flag; needs a config plugin or a native call |
| Local DB engine | — | 🔬 AsyncStorage + files | 🔬 AsyncStorage + files | **Not SQLite, and the reason is delivery, not preference.** `expo-sqlite` is a native module and cannot reach an installed build over the air — it needs a fresh EAS build first (root §5; the v0.27.x channel trap). This had to be an OTA. The access pattern is get-by-key plus one index, which is what a KV store is for. The day History needs date ranges or note search, that flips, and `deviceCache`'s API is shaped so SQLite replaces it without anything above moving |

## Settings rows (where mobile differs from the web page, and why)

| Row | Web | iOS | Android | Notes |
|---|---|---|---|---|
| Appearance › Theme | ✅ dark-mode switch | 🔬 | 🔬 | Three-way System / Light / Dark. The web's binary switch has no "follow the OS" option; a phone needs one |
| Appearance › Background | ✅ | 🔬 | 🔬 | Named swatches, never colour alone |
| Appearance › Text size | ✅ 4-step scale | — | — | **Deliberate divergence.** The web scales its own root font because a browser page has one. iOS and Android already own text size system-wide (Dynamic Type / Font size) and every screen here respects it; a second app-only scale would fight the phone's own setting and confuse exactly the patients it is for. The row explains this instead |
| Appearance › Language (en/he) | ✅ `<select>` | 🔬 | 🔬 | v0.13.0. **Deliberate UI divergence:** a row of 44 pt pills, not a dropdown — RN has no non-modal `<select>`, and a patient who opened the app in a language they cannot read must not have to open a menu whose label is in that language to escape it. Registry-driven from `LANG_META`, so it wraps rather than redesigns when a third language is added. Placed **first** in Appearance for the same reason. Each language is named in its own script (`English`, `עברית`), never translated |
| Notifications (3 toggles) | ✅ | 🔬 | 🔬 | **Row updated v0.55.0 — it predated Reminders.** Test reminders is REAL: the switch gates the schedule and the row opens the Reminders screen (see the Reminders rows above, v0.43.0+). Results-ready and doctor-messages remain stored flags a future push service reads, exactly as on the web |
| Care connection | ✅ | 🔬 | 🔬 | Ported |
| ECG Device | ✅ | 🔬 | 🔬 | Status / device name / connect / disconnect. **Not ported:** the "Live Scan" debug entry — no scan engine on mobile yet |
| Privacy › On-device processing | ✅ | 🔬 | 🔬 | Ported. **v0.55.0: the copy was FALSE and is rewritten** — it still said "Your ECG never leaves this device. There is no server today", which stopped being true when the backend and sync engine shipped. It now says: analysed on the phone, synced encrypted to the account. A privacy sentence describing an app that no longer exists is a false statement on the one screen that must never make one |
| Privacy › AI voice-guide key | ✅ | ⏳ | ⏳ | Gemini Live is web-only today; a key field here would configure nothing |
| Privacy › Export my data | ✅ Coming soon | 🔬 | 🔬 | Same "Coming soon" chip as the web |
| Account › Name / Role | ✅ | 🔬 | 🔬 | v0.19.0: the NAME is the signed-in account's. The role chip still reads Patient — see the RBAC row in the Onboarding table |
| Account › Sign out | ✅ | 🔬 | 🔬 | v0.19.0: live, behind a `ConfirmDialog` that says what it costs (the password, or a face). It is also the only route back into the onboarding flow |
| Account › Preview as role | ✅ (demo control) | 🔬 | 🔬 | **Row corrected v0.55.0 — it was stale:** shipped in v0.28.0 (see the dedicated ★ row in the History table), this table was never updated. Grants nothing; the server still authorises against the real role |
| Account › Lock when unattended | — (web has no device lock) | 🔬 | 🔬 | **Row added v0.55.0 — shipped earlier with no ledger row.** Offered only where the OS can honour it (`canUseAppLock`); a security switch that silently does nothing is worse than no switch |
| Clinic & Server (admin) | ✅ | ⏳ | ⏳ | Admin-gated on the web; there is no role to check against yet |
| About | ✅ | 🔬 | 🔬 | Version + build label + compliance. **Not ported:** the "AI model" row — no ONNX artifact ships on mobile yet |
| ★ Settings layout — wide controls STACK (v0.55.0) | ✅ (web rows wrap in CSS) | 🔬 | 🔬 | **Bug fix, reported as "texts climb on top of the tiles".** Root cause: the inline control slot is capped at 52 % of the row, but Yoga's default `flexShrink` is 0 and RN views default `overflow: visible` — so a 3-segment control (~200 pt intrinsic) kept its width, pinned to the row's end, and painted LEFTWARD over its own label; under Hebrew the unflipped `alignItems` made it spill toward the card edge instead. Fix: `SettingsRow layout="stack"` gives wide controls the whole row under the label (Theme, Care connection, role chips, long About values); the segmented track may now shrink as a last resort; `SettingsChip` is a View around a Text so a stadium radius stops clipping glyphs; the swatch row wraps; section art centres against its heading |

## Phone-scale departures from the web layout (v0.9.0)

A landscape phone is ~390 pt tall; the web's exam CSS was measured on a
viewport several times that. These are the places the sizes had to change so
the layout would still *mean* the same thing. Structure, order and copy are
untouched, and **above a 500 pt stage every one of these reverts to the web's
own value**.

| Element | Web | Phone (< 500 pt stage) | Why |
|---|---|---|---|
| Prep confirm button | 18 px pad · 21 px text | 13 px pad · 16 px text | Was taller than the photograph's remaining slot; still over the 44 pt tap-target floor |
| Prep headline | `clamp(23px, 5vw, 34px)` | driven by stage HEIGHT (~20 px) | `5vw` assumes width is the SHORT edge; in landscape it is the long one, so the web formula pins the headline at its ceiling |
| Prep headline width | `max-width: 640px` | full stage width | At ~20 px the longest step title then fits on ONE line, worth ~30 pt of photograph |
| Prep gaps / dots | 16–28 px · 9 px | 10 px · 7 px | Pure chrome |
| Capture countdown | 132 px ring in the foot | **the same ring at 40 pt, in the bar** beside the BPM, labelled `SEC LEFT` | The foot is then empty during a capture and is not rendered, so the traces take the whole screen. **User-requested.** 40 pt is ≤ the bar's existing content height (the Exit pill sets it at ~41), so the ring costs the traces nothing — bar is 57 pt with it and without. The 132 px foot ring is still used above 500 pt |
| Bar subtitle | `limbHowTo` idle · `limbRecordingNow` recording | not shown at all | Idle: the guide circle carries it in larger type. Recording: the draining ring already says a capture is running, and a line of prose is read once then occupies the traces' height for ten seconds. Dropped, never ellipsised — a clinical instruction cut mid-sentence is not a shorter version of it |
| Prep step-2 title | "Rest that hand on your left thigh — the back of the watch touching your leg" (74 chars) | "Rest that hand on your left thigh" | The long line wrapped to a second row and stole ~25 pt from step 2's OWN photograph, so the two steps showed different-sized pictures. The dropped detail — which way round the watch sits — is exactly what the photograph shows |
| Prep title height | auto | FIXED at one line | The picture's slot is the flex remainder, so anything above it that changes size between steps changes the picture's size too. `adjustsFontSizeToFit` shrinks a longer string rather than truncating it |
| Auto-arm hint | always | hidden while the guide circle is up | The circle's caption already says the recording starts on its own |
| Simulation badge | `SIMULATION — not a real signal` | `SIMULATION` | Full sentence kept as the accessibility label. Shortened only so it cannot push Exit off the bar; it still says the one thing that matters |
| Lead-card padding | 8 px | `floor(cardH × 0.06)`, clamped 3–8 | A fixed 8 px is a quarter of a short card's trace. **Scaled, not stepped** — a threshold would make traces shrink as the layout grew. Rounds DOWN: it is dead margin, so the spare point goes to the trace |
| Grid gap between cards | 10 px | 6 px on grids under 400 pt | A phone spent 20 of ~300 pt of grid on two seams; a seam carries no signal |
| Bottom safe-area inset | n/a | home indicator NOT reserved; Android 3-button nav bar is | The pill is a thin overlay and the grid beneath it has nothing to tap. Same `> 40` test as `dockMetrics.dockBottomOffset` |

Resulting geometry (computed from the formulas, worst case, iPhone 15 Pro /
SE 3rd gen / Pixel 7 / Pixel with 3-button nav): prep photograph **393 × 221 –
462 × 260** (was ~259 × 146); trace **72–82 pt** waiting and **83–94 pt**
recording (was ~52 pt at v0.8.0 — **+71 %**), 311–425 pt wide.

## Traps this port has already paid for twice — do not reintroduce

| Trap | What happens | Rule |
|---|---|---|
| `StyleSheet.absoluteFill` on an `<Image>` with `resizeMode="contain"` inside an `overflow: hidden` parent | The Image takes its box from four zero insets with no intrinsic size, `contain` resolves against the wrong frame, and the parent crops the photo to a corner — the patient sees one finger. Broke `LimbPrep` in v4.0.0 and again in v0.10.0 | Pass explicit point `width`/`height`. Use `position: 'absolute'` for stacking only |
| Deriving a visual from "what the real thing looks like" | v0.10.0 painted the report strips on pink clinical ECG paper. CYPHIX's sheet is white with a blue grid (`--ecg-*` in `report.css`) | The web is the reference for anything on a brand surface. Read the token, do not reason about the domain |
| Two writers of the iOS orientation API | Three rotations per navigation (v0.8.0 post-mortem in `RootNavigator`) | react-native-screens only — route `orientation` or `navigation.setOptions`. `lockAsync` is banned |
| ★ Reading "the feature doesn't work" as "the feature is broken" | "There is only a demo signal, never the real hardware" sounds like a defect in the ECG path. Every part of that path was correct — both native modules, the frozen contract, the raw buffer, the display filter, the analysis on raw — and so was the firmware. The cause was the **environment**: Expo Go contains no `cyphix-ble`, so `requireOptionalNativeModule` returns null and the documented simulator fallback engages. Hours can be spent debugging a pipeline that was never wrong | Before debugging a feature, establish that the code under suspicion is **running at all**. An optional native module is null in Expo Go by design; a fallback doing its job looks exactly like a failure. Ask "which build is this?" first |
| A permission declared in the manifest and never requested | `BLUETOOTH_SCAN`/`BLUETOOTH_CONNECT` sat in `app.json` since the module was written, so it read as done. Nothing called for them at runtime, and Android's `startScan` then returns **no results and no error** — identical on screen to the device being absent or off. The PARITY row said Android ✅ | A manifest entry is a request for permission to ask, not a grant. Any Android capability behind a dangerous permission needs a runtime request at the call site — and a silent-empty API is worse than a throwing one, so treat "found nothing" as a permission suspect first |
| Rasterising a logo by eye instead of from its path | An icon hand-traced to look like `BrandLogo` is a second, unlinked copy of the brand: the day either changes, they diverge, and nobody notices because both look right in isolation | Render from the real path data (`scripts/make-icons.js` copies `BrandLogo`'s verbatim). Regenerating is a command, not a redraw |
| Stepping a size on a threshold | An 89 pt lead card drew a TALLER trace than a 92 pt one, so traces shrank as the layout grew | Scale continuously, and round dead margin DOWN |
| A `<Text>` in a `flexDirection: 'row'` | Does not wrap — it overflows its parent and prints through the border. Word-valued metrics ("Slightly variable") escaped their tiles in v0.11.0 | `flexShrink: 1` on anything that can be long, `flexShrink: 0` on the unit beside it |
| Porting a **print** layout element-for-element | The report's two A4 sheets became a repeated letterhead and six 51 pt slivers on a phone. Every element was faithful; the result was a fax (v0.11.0) | Root §3.3 is two halves: brand identity is identical, **layout follows the platform**. Ask what the layout is solving for — a printer is not a thumb |
| One `<Svg>` wider than the GPU's texture limit | react-native-svg gives each `<Svg>` a single native texture. Past ~4 096 px it draws **nothing** — a blank lead that looks like missing data, not like a rendering bug. A zoomed 10 s sheet is 6 600 px on a 3× phone | Tile the paper (`EcgReviewStrip`), and keep `TILE_MM` a multiple of the 5 mm major grid or every seam shows as mismatched squares |
| Porting a **hover** interaction to a finger | The web caliper is hover → click → click. A fingertip is ~9 mm — 360 ms of paper at 25 mm/s, wider than a QRS — so an absolute-positioned touch tool measures whatever is under the hand and therefore invisible | Persistent handles, dragged by **delta** not to absolute position, so the thing being positioned stays beside the finger. Precision comes from zoom |
| **A `PanResponder` inside a `ScrollView` that claims on MOVE** | It never gets the gesture. A ScrollView that has begun panning OWNS the responder and a child asking afterwards is ignored — so in v0.15.0 grabbing a caliper crosshair scrolled the paper underneath it, and **not one draggable thing in the History viewer worked**. Every green check passed | Claim on **touch-down** (`onStartShouldSetPanResponder` + capture) and set `scrollEnabled={false}` from `onPanResponderGrant`, so the scroll never begins |
| ★ **A `Pressable` never re-targets — so "follows the finger" only ever meant "on touch-down"** | v0.24.0 moved the dock's highlight to the tab being pressed and called it following the finger. It was not: a press belongs to the view it started in, so the only tab a finger could reach was the one it **landed on**. Sliding towards a neighbour did nothing — reported from the phone as "it only works for the Chat icon", which was simply the tab being tapped. The behaviour looked correct in every read of the code, because each item's handler is correct; what is wrong is that no item can see the others | A gesture that must **re-target between siblings** has to live on the parent that can see all of them — one `Pan` on the bar, not five handlers on five tabs. Keep the per-child pressables underneath for taps and accessibility, and let the pan take over after a small travel threshold |
| **`options.tabBarButton` on a navigator with a custom `tabBar`** | Standard advice for animating tab items is a custom `tabBarButton`. It is consumed **only by React Navigation's own `BottomTabBar`**. `RootNavigator` passes `tabBar={}`, which replaces the whole bar, so setting `tabBarButton` here reads as configuration and does exactly nothing — including in a code review, which is the dangerous part | Before taking a navigator recipe, check which layer it plugs into. With `tabBar={}` the equivalent of a custom button is a child of your own bar (`DockItem`), and the `options` route is closed |
| ★ **An indicator defined relative to the surface under it** | The dock's selection pill was a translucent white. It was never visible in its own right — it was visible *because it was brighter than the bar*. One release later the same change set made the bar glassier to look more like the system's, the pill lost the only thing it contrasted against, and **the current tab stopped being marked at all**. The dock's own dressing destroyed the one thing the dock exists to show, and typecheck, both bundles and `expo-doctor` all passed while it did — none of them can see contrast | Anything that says "you are here" must be legible **on its own terms**, because the surface beneath it is the part most likely to be restyled. Make it solid, and where two values are *required* to match (here: the pill and the cut-outs in the icon standing on it) make them literally **one constant** — "similar" drifts, one value cannot |
| **A prop named for one branch of a two-branch component** | `GlassSurface` picks Liquid Glass on iOS 26+ and a tinted BlurView everywhere else. The colour prop was called `fallbackTint` and was passed only to the second branch, "because Liquid Glass tints itself". It does not: untinted `regular` glass over a LIGHT page is nearly clear, so on an iPhone every sheet, the dock, the report bar and the confirm dialog rendered with **no background at all**, while the identical code on a Galaxy looked exactly right. Found by holding the two phones side by side | Name a prop for what it MEANS, not for the branch that happened to need it first (`tint`, not `fallbackTint`). And when a component resolves to per-platform implementations, check that every input reaches every one of them |
| **A gate that opens the instant the state it watches flips** | Registration writes the account, so `auth.user` exists ONE SCREEN BEFORE the flow is over — and `AuthGate` swapped the whole flow for the app on that frame. "Profile created" was written, styled, animated and **structurally unreachable**; the patient would have gone from the review screen straight to the tabs. `tsc`, both bundles and `expo-doctor` were green | A gate needs the state AND permission to act on it. `authSlice.justRegistered` is that permission, and `welcomeAcknowledged` is the patient handing it back. Ask of any gate: is there a screen that lives on the far side of the condition? |
| A percentage grid with `gap` | `gap` is not part of the percentage, so three 33 % keys plus two 10 pt gaps overflow the row — and `flexWrap` answers by silently giving a **two-column dialler**. Same arithmetic would have put the eighth blood-type tile on its own line | Size the cells UNDER the fraction (30 % for thirds, 20 % for quarters) and let `flexGrow` hand the slack back |
| `paddingEnd` / `end` in an app that does not switch `I18nManager` | They resolve against the NATIVE layout direction, not the app's chosen language. This app draws RTL itself (see `i18n/I18nProvider`), so `end` stays the right edge in Hebrew while the control it was clearing moves left — and the text runs underneath it | Explicit `left`/`right`, chosen from the `rtl` flag the translation hook already gives every component |
| A handle that must also be TAPPABLE | Owning touch-down swallows any `Pressable` inside it — a marker that drags perfectly and cannot be opened. Claiming on move instead trades one broken half for the other | Own the gesture, and tell tap from drag **on release, by travel** (`< TAP_SLOP` = tap). One target, both behaviours |
| ★ **A `PanResponder` rebuilt MID-GESTURE** | Its handlers close over the running totals (`last`, `travelled`) that convert "distance since touch-down" into "distance since the last event". `useMemo(…, [onTap, onStep])` rebuilds it between move events, because callback props are fresh arrows every parent render and every drag re-renders the parent. Result: each move applies the FULL distance (the thing runs away from the finger) and release reads travel 0 as a TAP. On a reference line that meant drag → lift → **it deleted itself** (v0.16.0) | Build the responder ONCE (`useMemo(…, [])`) and read everything live through a ref — `useDragHandle` in `EcgReviewSheet`. **Never put a callback prop in a responder's dependency array** |
| A landscape screen that ignores the safe area | The notch / Dynamic Island is on a SIDE in landscape, not the top. A full-bleed sheet put the first ~50 pt of every ECG lead under it — a cut-off clinical trace, not a cosmetic gap | Inset all FOUR edges on any landscape route, and keep floating chrome out of the content's own box |
| An invisible drag surface | Compare's ghost was moved by dragging anywhere on the sheet, with nothing on screen saying so. It was reported as "compare does not work" — which, as a feature, it did not | If a gesture is the only way to use something, draw the handle. A labelled affordance is the feature; the gesture is just how it is operated |
| Giving a tool's chrome the same weight as the trace | v0.15.0's labelled 44 pt chips + headline + meta + tabs + hint took ~a third of the screen from a waveform. Every element was justifiable on its own | On a phone, chrome competes with the SUBJECT, not with other chrome. Icons where the pictogram is honest, a labelled sheet where it is not, a FIXED-height status slot that tools share rather than add to, and a landscape full-screen for when the content's aspect is not the phone's |
| ★ **"In tree" is not the same as "in the screen's subtree"** | `OverlayLayer` fixed the Modal trap below by rendering overlays inside the screen that opened them. Correct for the blur, and it put every sheet **underneath the floating dock** — the dock is the tab navigator's `tabBar`, a SIBLING of the screen, painted after it, and `zIndex` orders siblings within ONE parent. So a bottom-anchored sheet's pinned Save button was unreachable behind the bar, the scrim did not dim it, and it stayed **tappable through a modal**. Fixing the clipping in v0.39.1 only moved the button from off-screen to behind-the-bar, and the second report read identically to the first | What a material needs is the same **window**, not the same subtree. Portal overlays to the app root (`components/atoms/OverlayPortal.tsx`) — same window, different parent — and mount the host AFTER the navigator. When something must sit above chrome the navigator owns, no value set inside a screen can achieve it |
| ★ **Starting an entrance animation in the commit that mounts its content** | A sheet with two dozen rows is ~100 native views, and views are created and laid out **on the UI thread** — the same thread a `useNativeDriver: true` animation runs on. So the first frames of the rise competed with the mount and the panel arrived in visible steps: reported twice as "it comes up in frames, it isn't smooth". The native driver is not a defence here; it is what puts the animation on the busy thread | Commit the panel **off screen**, and start the animation on the content's first `onLayout` — by then the views exist and the thread is idle. One frame of delay buys every frame after it. Same family as the SVG-paths-on-the-open-frame lesson already recorded in `OverlayLayer`: for a smoothness complaint, find what else is running on that frame |
| ★ **Presenting an overlay in a `Modal`** | A `Modal` is a SEPARATE WINDOW — its own `UIViewController` on iOS, a `Dialog` with its own `Window` on Android. Two failures follow, and both shipped. (a) `UIVisualEffectView` samples **its own window's** layer tree and dimezis' `BlurView` snapshots **its own decor view**; inside a Modal that is empty, so every "glass" sheet in v0.17.0 rendered as a flat grey rectangle — the exact thing it was written to replace. (b) `Modal` defaults to `supportedOrientations={['portrait']}`, so presenting one while the app is landscape makes UIKit raise `UIApplicationInvalidInterfaceOrientation` — an uncaught ObjC exception, so the **process dies**. Tapping MARK in full screen killed the app | Present overlays **in tree** (`components/atoms/OverlayLayer.tsx`). A material needs something behind it, and a Modal is defined by having nothing behind it. `supportedOrientations` only silences (b) and leaves (a) — it is not the fix. Whatever leaves `Modal` must take over its back button, its keyboard handling and its mount lifecycle |
| An overlay hosted inside a **padded** container | Absolutely-positioned children are laid out against the parent's CONTENT box, so a full-screen scrim inside a safe-area-padded root stops at the inset and leaves four unblurred strips | Put the safe-area padding on an inner view and keep the overlay a sibling of it, at the unpadded root |
| ★ **An inline object prop passed to a `memo`-wrapped component** | `palette` was rebuilt on every render of `StudyViewerScreen` and handed to six `memo`-wrapped `EcgReviewStrip`s. The memo never held **once**, so every re-render — a sheet opening, a caliper nudge, one frame of a drag — re-ran `buildEcgPath` over four tiles × six leads, doubled with a ghost. Reported as "it flickers", "it isn't smooth", "feels dated". Two separate rounds of animation tuning chased it and neither helped, because the animation was never the cost | `memo` is a contract with the CALLER. When wrapping a component in it, audit every prop for referential stability and memoise the objects and arrays at the call site — an unstable prop does not weaken the memo, it deletes it. Chase a smoothness complaint by asking what runs on the same frame, not by shortening the animation |
| Re-deriving a signal when the change was a **transform** | Dragging the ghost re-entered `useOverlayRecording` per touch event, allocating six shifted `Float32Array`s and, in warp mode, re-running `alignByFiducials` on all six leads — the most expensive routine in the module, at touch frequency | A translation of an already-computed curve is a transform, not a recomputation. Apply it at DRAW time (`<G translate>` in `EcgReviewStrip`, in the viewBox's millimetres). ⚠️ Tiled paper only draws the samples on each tile, so a translate opens a seam gap unless each tile over-draws by the clamp limit — hence `GHOST_NUDGE_LIMIT_MM`, which is a drawing constraint and not a preference |
| Gating an invisible touch surface on **mode alone** | Clearing the comparison left `mode` on `'ghost'`, so the drag capsule stayed on screen with no ghost — and the full-sheet invisible drag surface under it stayed too, swallowing every touch and freezing the scroll. The visible symptom was a stray capsule; the real one was a dead sheet | Gate on the DATA the mode acts on, not just the mode (`mode === 'ghost' && ghost`), and reset the mode when its subject disappears. Do both: either alone is luck. Anything that covers the screen invisibly must be impossible to leave mounted |
| Stacking two blurs, and animating the outer one's opacity | Reported as "slow, and it flickers a bit when it opens". A `UIVisualEffectView` **re-computes its effect whenever its opacity changes**, so fading a full-screen blur in re-renders it every frame; and Android's `dimezisBlurView` is experimental and snapshots a view tree per frame, so two of them stack that cost. The panel's own glass was sampling a scrim that was itself sampling the page | **One** blur per overlay, on the panel, and never the thing whose opacity animates. Dim the scrim with a plain colour — free on the native driver. This is also what the platform does: an iOS sheet dims its backdrop and reserves the material for the sheet |
| Replacing a **direct-manipulation** gesture with buttons | v0.18.0 answered "compare is not intuitive" by moving the ghost with 40 ms arrow steps in a sheet. Reported immediately as wanting the on-screen drag back. Aligning two heartbeats is judged continuously **by eye**, and no amount of discoverability survives making the reader look away from the trace to press a button beside a list | Buttons cannot substitute for a gesture whose value is the closed loop between eye and hand. When such a gesture is undiscoverable, **draw the affordance over it** — do not replace it. Show the resulting number on screen, so nothing has to be reopened to read it |
| Explaining a feature by listing its **settings** | Compare shipped as filter/alignment rows before anything had said what the grey trace was. Reported twice as "still not clear how to use it" — v0.17.0 answered the wrong half by making the *drag* visible, when what was missing was the *feature* | Lead with what the thing IS (a sentence and a legend), then what it acts on, then how to operate it. And give any gesture-only control a tap equivalent: a drag is the best way to operate something and the worst way to discover it exists |
| Running `npx expo install` from the wrong directory | The legacy global `expo-cli` resolved `latest` into `CYPHIX_SHARED`, pulling expo 57 + RN 0.86 into a package that has no dependencies at all, and would have broken the Expo Go SDK 54 pin (`CLAUDE.md` §1) had it landed in the app | `Set-Location` to the project explicitly, and check the installed version against `expo@54`'s `bundledNativeModules.json`, never against npm `latest` |

## v0.59.0 — this build measures, it does not decode (+ the Values redesign)

At the user's instruction: *"I don't want the FINDINGS tab — my app doesn't
decode anything, it only shows measurements."* Nothing was deleted; one flag
in `config/featureFlags.ts` (`INTERPRETATION_ENABLED`, currently `false`)
turns four surfaces off together, and turning it back on restores all four.

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| **Findings tab hidden in the study viewer** | ✅ still shown | 🔬 hidden | 🔬 hidden | **Deliberate divergence from web, and the reason is in the flag's doc comment.** The segment, its pane and `EcgScreeningSheet` are untouched and unreachable; `useScreening` is handed `undefined` so `screenLimbEcg` does not run at all — 43 rules over six leads is real JS-thread work for a pane nobody can open. The viewer is two segments now: **ECG · Values**, and a *patient* lands on **Values** (it used to be Findings; a clinician still lands on the trace) |
| ↳ **Verdict pill gone from the History list** | ✅ still shown | 🔬 hidden | 🔬 hidden | Same flag. It came from the same screening engine, so a build that "only shows measurements" could not go on printing *Clear / Attention / Urgent* beside every study. `undefined` is the shape `StudyCard` already had for a study whose digest is not computed yet — no new state was introduced |
| ↳ **Digest backfill no longer screens** | — | ✅ | ✅ | `digestFromRecording` skips `screenLimbEcg` entirely rather than computing a level into a cache nothing reads. This is a real saving: it ran per study, for every study in the history, on the JS thread, the first time the list was opened |
| ↳ **PDF: no interpretation pages** | ✅ still printed | 🔬 | 🔬 | **The one that mattered most.** A PDF leaves the phone, gets emailed and filed, and is read by someone with no way to ask what the app was claiming that day — a document carrying a verdict the app no longer offers would be the last copy of that claim in circulation. The report falls into the path a **simulated** recording has always taken (never screened since v1.0.0): no interpretation page, and the identification grid printed on the statistics page instead. Page arithmetic unchanged — `interpPages` was already `0` on that path |
| **Dock slot 2: "My Tests" → "Insights"** | ✅ sidebar has no equivalent | 🔬 | 🔬 | Same glyph (`TestsNavIcon`), new word and new destination. It opens the **ECG ID**, which used to live behind a sub-tab inside History. `TestsScreen` (the test-choice carousel) is **kept in the tree and deliberately unrouted** — a patient starts a test from the HOME button, which is the control that has always started one. Web has no bottom dock, so there is nothing to port back |
| ↳ **History is one list again** | ✅ two-column reading view | 🔬 | 🔬 | The `Studies | Insights` segmented control is gone with Insights, and so is the hide-don't-unmount pane machinery that kept both alive (v1.7.0) and the header's tab-row height term. A tab screen is unmounted by the navigator when you leave it, so `EcgIdentityPanel.active` no longer has anything to defend against and is left at its default |
| **VALUES tab redesigned** | ✅ web keeps the report sheet | 🔬 | 🔬 | From the design handoff *"מסך Values מעוצב מחדש"*. Reported as *"very old-fashioned"*, which it was — it was `EcgAnalysisSheet`, the web's printed measurement form ported line for line, being read on a phone by a patient. Now: the rate as a hero card with the study's own lead II under it, sections told apart by colour on translucent cards over a fixed glow field, reference bands as one flat tint, and **every value tappable** for one sentence saying what the quantity is. `EcgAnalysisSheet` is **not deleted** — it is still what the report preview and the PDF lay their measurements out from, where a printed form is the right answer |
| ↳ Values — colour sections, it never grades | — | ✅ decided | ✅ decided | **The rule that survived the repaint, and the easiest one to break by accident.** The rhythm tile is amber whether the rhythm is regular or not; the steadiness ring is mint at 12 % and at 98 %; the reference band is one tint at any value. Nothing on the screen takes its colour from whether a measurement is inside or outside a range — that would be an interpretation, which `ecgAnalysis.ts` is forbidden from making. Argued in `valuesPalette.ts`, `ValueQualityCard` and `ValueIntervalRow` where each would have to be changed to break it |
| ↳ Values — cards are gradients, not blur views | ✅ `backdrop-filter` | ✅ | ✅ | **Deliberate divergence from the handoff.** Eight `BlurView`s in one scroller is the most expensive thing a phone can composite while a list moves (Android's `dimezisBlurView` re-renders what is behind it every frame). What the blur is *for* is that the card takes a hint of the colour behind it — and behind it is a smooth radial field, which a 26 px blur barely changes. `GlassSurface` is kept where it earns itself: the header bar, sheets, the dock |
| ↳ Values — the hero number is flat, not a gradient | ✅ `background-clip: text` | 🔬 | 🔬 | **Deliberate divergence, recorded rather than hidden.** RN has no `background-clip: text`. The two ways to get it are a masked view — a **native dependency**, which would turn this release from a 1-minute OTA into a 40-minute rebuild — or SVG text, which gives up `tabular-nums` and the metrics that keep "82" and "BPM" on one baseline. The handoff's gradient is ~90 % crimson at the end anyway, so the number takes the crimson and the card keeps the gradient |
| ↳ Values — amplitude table fits the phone | ✅ | 🔬 | 🔬 | The ported table put seven columns in a **horizontally scrolling** view. Defensible on a clinician's report, poor here: a table you have to drag sideways to finish reading hides half its own content. Now the numbers take one row and the QRS peak-to-peak **profile** — what a reader actually scans this table for — gets a full-width bar under each row. Still p-p only; six columns of competing bars would bury the profile |
| ↳ Values — `AxisDial` is skinned, not forked | ✅ | 🔬 | 🔬 | The dial takes an optional `palette` and `readout={false}` instead of a second copy existing with different colours. Same geometry, same ECG angle convention (positive is **down**, toward the feet) — and a forked copy is how that convention eventually gets flipped in one of them and not the other |
| ↳ Values — "Export Report" CTA | ✅ web has a print button | 🔬 | 🔬 | Named at the user's request (the handoff said "View Report"). It makes the **same** choice the ⋯ menu makes: preview when this binary carries the WebView, direct share when it does not, because a button must never dead-end on a build that received this over the air. Hidden entirely when the reader lacks `exportPdf` |
| ↳ Values — the trace is the real lead II | — handoff draws a decorative path | 🔬 | 🔬 | The handoff repeats a hand-written ECG path. That is right for a mock-up and the one thing that must not be copied: a decorative waveform on the same card as this patient's measured rate is a picture of somebody else's heart under their number. Reduced by **peak-preserving decimation**, not averaging — averaging flattens the R wave, which is the one feature that makes a 56 pt trace legible |

## v0.65.0 — Lead II, measured twice (firmware v3)

> ⚠️ **Native rebuild, not an OTA** — `modules/cyphix-ble` changed in both halves, `app.json` 0.35.0 → 0.36.0. The wire-level half of this release (the second characteristic, the strict parser, the Android MTU request) is in **Platform capabilities → BLE ECG link**, above. Nothing in this section has been run on a device: the native halves were not even compiled (Windows machine), and no v3 unit has met this build.

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| **Dual Lead II capture + fusion in reports** | ✅ v1.51.0 | 🔬 needs-iOS-verify | 🔬 typechecks + bundles only — never run on a device | **v0.65.0, firmware v3+.** The device measures Lead II twice (copy A = LL#1−RA, what every device measures and still `leadII` everywhere; copy B = LL#2−RA, `leadIIb`). **Recorded, never drawn:** `SixLeadMonitor`, `EcgWave` and `useHeartbeatGate` read `leadI`/`leadII` exactly as before — fusion needs the whole recording, so there is nothing honest to draw live. `bleClient` grows a third ring that exists on the `EcgBufferView` **only while the device streams a second copy** (undefined on legacy devices and the simulator); `useLimbRecorder` keeps it for a recording only if it was there from the first captured sample to the last (else that recording is a normal two-channel one — never mismatched lengths). **Stored RAW** (`channels.leadIIb`, optional `rawLeadIIb` on `POST /recordings`): the fused trace is an opinion and the viewer can switch it off. **One function decides fuse-or-not for every reader** — end-of-exam report, viewer, PDF, History digest all go through the shared `limbLeadsFromRaw` (stored records via `services/ecg/storedLimbLeads.ts`, which adds the **limb-only** gate: in the chest protocol the probe moves and copy B does not follow). No frequency filtering of the ECG anywhere in it (0.05–150 Hz ischaemia band). **PDF + digest PIN fusion ON** (as they pin the filters); the viewer has a **"Lead II fusion"** row in the Filters sheet, default on, **drawn only for a study that has a second copy**. **It says what it did:** one quiet line in the viewer header and under the end-of-exam summary (`fused · noise a → b µV` / declined **with the reason** / off), a second provenance paragraph on the PDF — and **nothing at all** on a recording with one copy, so every older study renders exactly as before. CSV: the shared builder already emits a raw trailing `II_b` column. Local store: ~51 kB per 3-channel capture vs ~34 kB, cap **stays 40** (~2.1 MB of ~6 MB; parity with web). `verify-pdf` gained three dual-Lead-II cases (fused 30 → 7 µV on synthetic noise; a disconnected second electrode correctly declined). ⚠️ No v3 device has been near this build |
| ↳ **ECG ID templates stay on Lead II copy A** | ❓ not checked from this change set (web v1.51.0 is being written in parallel — whoever lands second confirms the two agree) | ✅ decided | ✅ decided | **Deliberate divergence from every other reader of a stored recording, v0.65.0.** `features/insights/recordingTemplate.ts` reads `channels.leadII` alone and does **not** fuse. An identity is a baseline across months of recordings, and every one made before the second electrode existed has only copy A; fusing the new ones would make them systematically quieter than the old ones, and the ECG ID would report that as a change in the patient's **heart** on the day they changed **device**. Copy A is the one signal every recording has, so it is the only one baselines can be compared on. Stated at the site, so the next person to "finish the migration to `limbLeadsFromRaw`" reads why it was left |
| ↳ **RLD fault / second leg electrode off — live warning** | ✅ v1.51.0 | 🔬 needs-iOS-verify | 🔬 never run | **v0.65.0 — the ONE user-visible live change of the dual-Lead-II work, asked for.** Limb exam, in the rail note's own slot and style (`styles.railNote`): **reference electrode off** (`ECG3_FLAG_RLD_FAULT` — every channel is unreferenced, nothing on screen can be trusted) and **second leg electrode off** (`LOD_LL2` — the patient loses nothing they can see; "the recording will use a single copy"). Both **gated on the 3-channel stream being ACTIVE**, not on the bit: LOD bit 3 is ADS1293 input IN4, which on older hardware is not an electrode at all. Neither blocks the capture, for the rail note's own reason. Path = the rail's path: native event → `bleClient.onElectrodeFaults` (on change only) → `bleSlice.electrodes` → `useBle().rldFault / .secondLegOff`. ⚠️ `onLeadOff` had been emitted by both native halves since v0.1.0 and **nothing in JS had ever listened**; this is its first consumer. ⚠️ The "single copy" promise is kept by `fuseLeadII`'s own fallback (a copy with no heart in it fails the same-lead check) — the recorder still stores the raw second channel, because raw is what gets stored. Never seen on a device: needs a v3 unit with an electrode lifted |
| ↳ **⚠️ TEMPORARY: Lead debug screen** (Lead I / II-a / II-b live) | ➖ deliberately not built | 🔬 needs-iOS-verify | 🔬 never run | **v0.66.0 — mobile-only ON PURPOSE, and meant to be deleted.** Asked for the day firmware v3 went on the board: *"how can I see that it records two leads? … only for this debug, and then we switch it off as if it had never been."* Settings → ECG Device → **Lead debug** (DEBUG chip) → connect → three stacked Skia traces on **one shared scale** (II-b in its own ink), Filtered (the exam's display chain) or Raw; the stream kind (3-channel fw v3 / 2-channel legacy / simulator), the **measured** sample rate, lost packets, RLD / LL#2 / rail states; and the II-a↔II-b **correlation + RMS difference** — small-but-not-zero is the proof of a second electrode, and an exactly-zero difference is called out in words as a duplicated channel. No recording, no timer, no gate, no save; it reads the same `bleClient` ring every other screen reads. **Off switch:** `LEAD_DEBUG_SCREEN_ENABLED` in `config/featureFlags.ts` (outside the mirrored table, like the wordmark switch) — false = route not registered + row not rendered; `LeadDebugScreen`, `LeadDebugMonitor`, `useLeadDebug` become unreachable. JS-only ⇒ OTA both ways. English-only and outside i18n deliberately (developer readout; no orphan keys on removal). **Why not web:** the web's Live Scan tab is already its debug surface, the request was for the phone, and a screen with a planned deletion date is not worth a second implementation; the hardware-side equivalent is `HW_PLAYGROUND/firmware_production_v3/tools/ble_check.py`. **This is the ONE sanctioned exception to "recorded, never drawn" in the row above** — behind a flag, off the patient's path |

## v0.64.0 — the fingerprint keeps its name and loses the lecture

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| **Signature title opens an info sheet** | ⏳ pending | 🔬 | 🔬 | The title stays (v0.62.0's argument holds: nothing about one clean ECG trace says it is an AVERAGE of many recordings). The sentence under it does not. A TITLE is navigation — read every visit, one glance; an EXPLANATION is read once, ever, and putting it under the title charges every future visit for a question answered on the first one. Same `BottomSheet` pattern as every Values tile, interval row and finding |
| ↳ The explanation got LONGER, not shorter | — | 🔬 | 🔬 | A sheet is opened deliberately, so it can be complete: what it is · why averaging is the point (what repeats is the heart; a shaky hand and a loose electrode cancel out) · that the "usually" figures are what YOUR baseline calls normal, not what is normal for people in general. That last point was never on the screen in any form and it is what makes the panel below legible |
| ↳ Tap target is the width of the words | — | 🔬 | 🔬 | ⚠️ Not the column. A full-width invisible button directly above the ECG would swallow the start of a horizontal drag on the sheet below it — the gesture the caliper lives on. `alignSelf: flex-start`, mirrored under RTL |
| ↳ First-screen budget improves | — | 🔬 | 🔬 | The heading drops from ~3 wrapped lines + title to one row, which gives back more than v0.62.0 borrowed. Still arithmetic, not a measurement — the row asking for a look on a small handset stays open |

## v0.63.0 — the app can say an update is waiting

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| **App-update row (Settings → About)** | ➖ n/a | 🔬 | 🔬 | **Deliberate divergence: web has no equivalent and needs none.** A web app reloads into the newest build on the next navigation; there is no downloaded-and-waiting state to report because there is no install. This row exists only because `expo-updates` applies on the NEXT cold launch |
| ↳ Root cause | — | — | — | `expo-updates` was installed, configured and delivering, and **nothing in the app ever called it** — so its defaults ran unassisted (`ON_LOAD` check, `fallbackToCacheTimeout: 0`, apply on the next cold launch). Two cold launches per update, and from the phone "downloading", "downloaded and waiting" and "never published" are indistinguishable. That last part is the defect |
| ↳ Checks on RESUME, not only cold launch | — | 🔬 | 🔬 | `expo-updates` checks on a cold launch only, so a phone that is never fully killed can sit for days on a stale bundle |
| ↳ **Never auto-reloads** | — | ✅ decided | ✅ decided | ★ `reloadAsync()` tears down the JS context. On a device that may be holding — or streaming — a recording, an automatic reload is data loss with a friendly name, fired whenever the CDN answers. The reload is a TAP, on the one screen where nothing is being recorded. The library's passive apply-on-next-launch is unchanged |
| ↳ The states that matter are unreachable here | — | 🔬 | 🔬 | ⚠️ "ready" and the resume check cannot be produced on this machine: they need a real installed build with an update behind it. Typecheck and bundle prove neither |

## v0.62.0 — Insights says what it is showing

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| **V1–V6 hidden in the coverage grid** | ⏳ pending | 🔬 | 🔬 | Behind `PRECORDIAL_LEADS_ENABLED` (off). The grid was built to print all twelve leads with the six un-measured ones drawn empty — a clinician's argument about the SHAPE of the record. On a patient's screen six permanently grey cells read as six broken things, on a device that will never fill them. A FLAG and not a deletion: the 12-lead seam is real, and `hideEmpty` filters on "has no studies" rather than on "is precordial" so the lead set is not defined in two places |
| **The signature curve has a title** | ⏳ pending | 🔬 | 🔬 | "Your heart's fingerprint" + one line: *"One beat, averaged from all your recordings — every new study is compared against it."* **Not a reversal of v0.44.0** — that removed a TUTORIAL (a ring, three figures, a three-line explainer, a legend, every paragraph); what was missing was the figure's NAME. Nothing about one clean ECG trace says it is the AVERAGE of many recordings rather than the last one, and every number below it means something different depending on which the reader believes |
| **The lead row has a caption** | ⏳ pending | 🔬 | 🔬 | "Leads · how many recordings back each one". Six cells reading "I 12 · II 12" are a picker AND a per-lead evidence count, and a bare figure under a lead name could just as easily be a measurement |
| ↳ First-screen budget | — | 🔬 | 🔬 | ⚠️ Everything from the trace to the plain reading is sized to exactly ONE viewport (`firstScreen`). Two lines were added and one row of cells removed, which should net out — but that is arithmetic, not a measurement. **Needs a look on a small handset**: if the plain reading falls below the fold the trade is a bad one |

## v0.61.0 — inspiration, not a copy

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| **Six-lead sheet is page 1 again** | ✅ web prints the browser's page | 🔬 | 🔬 | Reverses v0.60.0 at the user's instruction. Every number on the measurements page is a claim ABOUT the signal made by this app's own delineator; the trace is the only page a second reader can check independently. Measurements now at page 3, statistics 3→"Clinical tables & variability", reference 4 |
| **Masthead has no band** | — | 🔬 | 🔬 | The 44 mm plum-to-navy card is deleted. The rate is set on the paper in the wordmark's navy over a hairline; the trace under it is drawn in the SAME navy as the six-lead sheets. `background-clip: text` and its `@supports` guard are gone with it — that guard existed only because light rose type on plum needed it, and it is the one declaration in this stylesheet that renders a number INVISIBLE when unsupported |
| **Axis needle 1.1 mm → 0.45 mm** | — | 🔬 | 🔬 | Measurably wrong, not merely heavy: 1 viewBox unit is 1 mm in that figure, so the needle was thicker than the sector boundary it is read against, on a figure whose job is to report an angle. Head 1.5 → 0.85 mm, hub 1.1 → 0.7 mm. Applies to BOTH dials (measurements page and statistics page) |
| **Chrome returns to CYPHIX** | ✅ unchanged | 🔬 | 🔬 | ★ THE RULE: the measurements page keeps the redesign's SECTIONING (a hue per family, tinted tiles, reference bands, axis and quality cards); everything that is CHROME — letterhead, section rules, table headers, figure panels, footers — goes back to white stock, the wordmark's navy and the report's blue. v0.60.0's lighter WEIGHTS are kept; only the hues came home. Violet now appears in one place only, the axis card, where it means something |
| **Processing provenance on page 4** | ⏳ pending | 🔬 | 🔬 | NEW SECTION, and it fixes my own v0.60.0 regression (removing the empty blind-spots list freed 41 mm and gave it to nothing, so the last page ended two-fifths down). States the filter chain and that four of six leads are DERIVED from two recorded channels. Provenance, never a finding. Deliberately does NOT restate the filter's window lengths — they are local constants in `reportFilter.ts`, and a printed document is the worst place for a number that can go stale silently. **Web has no equivalent section: pending** |
| **★ Fifth clipping bug (pre-existing)** | — | 🔬 | 🔬 | A four-line device subtitle printed OUTSIDE the letterhead, grey on white, across the blue keyline and into the first section heading. `.lh` was the one block in the report without `overflow: hidden` — the full-bleed, negative-margined one. Leading 1.55 → 1.3 so the longest real subtitle fits rather than merely gets cut |
| ↳ Harness stub matches shipped copy length | — | ✅ | ✅ | `procBody` in `verify-pdf.ts` is deliberately as long as the string in `en.ts`. A short stub renders a comfortable block here and an overflowing one on the phone |

## v0.60.0 — the printed report takes the design language

| Feature | Web | iOS | Android | Notes |
|---|---|---|---|---|
| **PDF measurements page** | ✅ web prints the browser's own page | 🔬 | 🔬 | From the *Clinical data export to PDF* handoff. Opens the report; the ECG sheets moved to page 2, because every clinical document opens with a summary and this one opened with two pages of trace. The band's trace is the study's own lead II and carries **no grid, no calibration, no axis** — it is scaled to fit, and those are what invite a ruler. Pages 2's sheets are unchanged at 25 mm/s · 10 mm/mV |
| ↳ **"within range" call-outs NOT built** | — | ✅ decided | ✅ decided | **Deliberate divergence from the handoff, at the user's instruction.** The design puts *"within range"* / *"2 ms below range"* beside each interval and a green *"Normal axis"* pill on the dial. Both are statements ABOUT a measurement — the thing v0.59.0 took out of this document. The shaded band and the marker stay; the axis pill is the section's violet at every classification |
| ↳ **System fonts, not Google Fonts** | — | ✅ | ✅ | The handoff sets Space Grotesk / IBM Plex Mono / Source Sans 3 from fonts.googleapis.com. This PDF is built ON THE PHONE at export time: a `<link>` there is a network request inside an export that must work offline, and it hands a third party the user's IP every time a medical report is printed. Offline it falls back silently to a different typeface than the one approved |
| ↳ **oklch converted to hex offline** | — | ✅ | ✅ | The handoff is written entirely in `oklch()`, which landed in Safari 15.4 / Chrome 111. `expo-print` hands the HTML to whatever WebView the phone has, and a colour function an engine cannot parse does not degrade — it drops the declaration. The letterhead would have printed white on exactly the oldest devices. The oklch original is kept beside each hex in `reportPalette.ts` |
| **★ Four clipping bugs fixed (pre-existing)** | — | 🔬 | 🔬 | Found by RENDERING the report for the first time. The amplitude table was printing **three of six leads** — aVR, the lead that catches swapped arm electrodes, did not print at all. Also cut: the last row of both measurement tables, the fifth interval bar, the identification grid's second row, the sixth median beat, and the signal-quality table's last row. One cause: an `<svg>` is inline and reserves descender space, and a ruled row's height is a line box (font-size × 1.2) — both make a block taller than its arithmetic, and `assertFits` validates what the BUILDER declares, not what the browser did inside it |
| ↳ Statistics-page amplitude table removed | ✅ web keeps it | 🔬 | 🔬 | Not repaired — replaced. The measurements page does the same job properly (six leads, every number, a chart each), and two tables of one dataset with one truncated is worse than one that is complete |
| ↳ `verify-pdf.ts` writes its HTML out | — | ✅ | ✅ | `PDF_OUT=<dir> npx tsx scripts/verify-pdf.ts`. The structural assertions passed for months on a report losing three leads, because they check declared heights. The only thing that catches that is looking |
| **Report no longer claims to screen** | — | 🔬 | 🔬 | Two v0.59.0 leftovers: the disclaimer opened *"This is a screening result…"* on a report with no screening result, and the reference page printed "WHAT THIS TEST CANNOT SEE" over 34 mm of white space (the blind-spot list comes from the engine that is switched off). A section promising blind spots and listing none is worse than no section |

## Open verification debt

- Everything marked 🔬 was built on Windows and has **never run on an
  iPhone**. Per root `CLAUDE.md` §5, do not claim iOS works until it has run
  via Expo Go (UI only) or an EAS dev build (native BLE included).
- ★ **v0.61.0 was rendered in two of the nine harness cases** (normal, and
  the long-device-label-plus-note case that exposed the letterhead bug). The
  same limits below still apply in full.
- ⚠️ **`reportFilterLeads` auto-rescales amplitude and nothing says so.**
  Noticed while writing the provenance copy, NOT acted on — it is
  `CYPHIX_SHARED` and therefore three apps. When lead II's 99th-percentile
  amplitude is above 2.0 mV or below 0.2 mV, every lead is divided by
  `maxAmp / 1.2` before `analyseLimbEcg` measures it. The printed strip stays
  self-consistent (its calibration pulse scales too), but the **millivolt
  amplitudes on the measurements page would be the rescaled ones**, which is a
  measurement that silently means something other than what it says. Affects
  low-voltage and saturated recordings only. Needs a decision, not a patch.
- ★ **v0.60.0's report was RENDERED, which is more than usual, and is still
  not verified.** Four A4 pages were printed to PDF and read — that is how the
  four clipping bugs above surfaced — but by desktop Chrome, from the harness's
  synthetic recordings. Not checked: the sheet on a real **printer**; the page
  as rendered by **WKWebView / Android WebView**, which is what `expo-print`
  actually uses and is where the `@supports` guard on the gradient headline and
  the hex-not-oklch decision either pay off or do not; and the report in
  **Hebrew**, which it has never been checked in — the document sets no `dir`,
  so RTL copy in the letterhead, the tiles and the captions is unproven.
- ★ **v0.59.0's Values screen is unrun.** It typechecks and bundles, which per
  root §6.4 means it is well-formed and nothing more. The five things only a
  phone can answer: (1) do the eight tiles' tap targets all fire, and does each
  open the *right* explainer sheet; (2) does the glow field sit still while the
  cards scroll over it, or does it scroll with them; (3) does the hero trace
  draw at all — it is built against an `onLayout` width, and a width of zero
  is a vertical line at x = 0; (4) does the amplitude table's seven columns
  actually fit a 390 pt screen without any of them wrapping, in **Hebrew** as
  well as English; (5) is the CTA's gradient legible in light mode, where its
  white text sits on a lighter palette.
- ★ **v0.65.0's dual Lead II is unrun end to end, and its native half is
  uncompiled.** `tsc` is clean, Android bundles, and `verify-pdf` builds three
  dual-Lead-II reports — which per root §6.4 proves the TypeScript is
  well-formed and that the fusion runs in Node, and nothing else. What only a
  v3 device and a built binary can answer: (1) do the Swift and Kotlin halves
  **compile** — neither was, on this machine; (2) does each actually pick
  `ECG_DATA3` when it exists and fall back to the legacy characteristic when it
  does not (test against a v2 device too: that path must be unchanged); (3) on
  **Android**, does the new `requestMtu → onMtuChanged → discoverServices`
  sequence connect at all, on more than one OEM — it changes how every
  connection opens; (4) does a 160 B notification arrive whole (a truncated one
  is dropped by the strict parser, which reads as "connected, no signal");
  (5) lift the second leg electrode, then the reference electrode — do the two
  notes appear, clear, and leave the traces' height sane on a 390 pt-tall
  landscape phone (each note takes height from the six traces, exactly as the
  rail note does); (6) is a saved dual recording's `leadIIb` on the server and
  back in the viewer, with the fusion line in the header and the switch in the
  Filters sheet; (7) the viewer header grows one line for such a study — does
  the first frame jump (`HEADER_H_GUESS` does not know about it); (8) the
  fusion runs on the JS thread when a study opens — how long on a real phone.
- ★ **The whole native BLE path is unrun.** `CyphixBleModule.swift` has never
  executed once — not against hardware, not at all — because until v0.23.0
  there was no way to get a build containing it onto a phone. `IPHONE_SETUP.md`
  §6.3 lists the four checks that distinguish a real trace from a convincing
  one: take the fingers off the electrodes (a demo keeps beating), light
  exertion, a held breath, and backgrounding the app (the trace must **stop**
  and mark itself not-live — that is the v0.23.0 watchdog). Until someone has
  done those, "the hardware works on iOS" is unproven.
- **The v0.29.0 offline layer has never run against a deployed server.** It
  typechecks on all three projects and bundles, which per root §6.4 proves it
  is well-formed and nothing more. `/recordings/sync` and the two ETag routes
  need `CYPHIX_SERVER` deployed (migration `0002` applies on boot) before the
  phone benefits at all — until then the sync reports an error and every read
  falls through to the network, which is the old behaviour, not a break.
  The five things a device has to demonstrate, none of them provable here:
  1. **Second launch is instant and offline.** Aeroplane mode, cold start —
     History lists, a previously opened study draws its trace, the portrait is
     there. Anything that spins is a route that was not actually cached.
  2. **The delta is really empty.** Foreground twice in a row with nothing
     changed and watch the server log: the second one must be one small
     request, not a page of recordings and not a 1.5 MB portrait.
  3. **A note written in the browser reaches the phone** on the next
     foreground — that is the annotation routes touching the *parent*
     recording's `updated_at`, which is the easiest thing in this change to
     get wrong and the hardest to notice.
  4. **A study deleted on the web disappears from the phone** — the tombstone
     path. Absence is the one thing a delta cannot express implicitly.
  5. **Signing in as a different account shows nothing of the first**, not
     even for one frame, on a device that already holds a full mirror.
- The staleness watchdog is verified only by construction. A real screen lock,
  a real walk out of BLE range and a real ESP32 brown-out have not been tried,
  and those are the events it exists for.
- The new icons have not been seen through iOS's own squircle mask or an
  Android launcher's adaptive crop, which is where icon sizing actually fails.
- The Swift and Kotlin packet parsers duplicate `parseEcgPacket()` by hand.
  If the packet format ever changes, all three must change together —
  consider a shared test vector fixture before clinical use.

- **The v0.19.2 glass fix is inferred, not observed.** The symptom (invisible
  on iOS 26, correct on Android) and the API both point one way, but this
  machine cannot run iOS. It wants one look at the sign-out dialog and one at a
  History sheet on the iPhone.

- **The v0.24.0 dock interaction is still unverified on any handset.** One of
  its three judgement calls has since been settled *by* a handset —
  `isInteractive` on the whole bar was reported as "no glass effect at all" and
  is out (v0.24.1). Two remain:
  1. The **swell against `overflow: hidden`** is computed, not seen: at
     `HOLD_SWELL` the pill grows to 60.0 pt inside a 65.1 pt inner box, and
     horizontally to ~4.5 pt short of the rounded cap on the outermost tabs.
     It should press against the container without being cut by it.
  2. Whether the highlight **travelling on touch-down** reads as responsive or
     as jumpy.
  3. New in v0.24.3: whether **6 pt** is the right threshold before the drag
     takes the touch off the tap — too low and a tap with a roll in it becomes
     a drag, too high and a slide feels stuck before it starts. Also whether a
     selection tick per tab is right, or one tab too many on a fast sweep.

- ✅ **Settled by the device (v0.24.3):** Settings › About reads `Apple Liquid
  Glass (iOS 26+)`. The material is live and real on the test iPhone, so
  neither "no iOS 26" nor "no `expo-glass-effect` in this client" is in play —
  anything still wrong with how the bar feels is in this app.

- **The dock cannot look like Apple Music's bar while nothing passes under it.**
  A material refracts what is behind it; Apple Music's tab bar reads as glass
  because artwork and lists scroll *under* it. This dock floats over a soft flat
  backdrop, so however the tint is tuned it can only render as a translucent
  plate. Fixing that properly means letting screen content run beneath the dock
  rather than stopping at `dockFootprint()` — a layout change to every patient
  screen, not a dock change, and not attempted here.

- **The welcome scrim (v0.19.1) was judged against a thumbnail.** Whether
  white type is comfortable over that photograph at those four gradient stops
  is a daylight-and-a-real-screen question, not a code question.

- **The whole onboarding flow (v0.19.0) is unverified on any device.** It
  typechecks, both platforms bundle and `expo-doctor` passes — which, per root
  `CLAUDE.md` §6.4 and the Scan History history below, proves only that it is
  well-formed. Specifically unproven: the four ported animations at real
  frame rates, the numeric pad and the OTP boxes on a SHORT phone (both steps
  scroll for exactly that reason, and nobody has watched them do it), the
  slider under a real thumb, `expo-image-picker` returning a usable square,
  and — untestable on Windows at all — Face ID.

- **Scan History has been through three rounds of device feedback** (v0.16.0,
  v0.17.0, v0.18.0) and sixteen findings. Every round passed `tsc`, both
  `expo export`s and `expo-doctor` before the phone found them — including a
  hard native crash and a blur that was never going to render. That is the
  clearest evidence in this repo for why §6.4 says what it says.
- v0.17.0's claim that the sheets rise on a blurred material was **wrong**: they
  were in a `Modal`, so they could not. Corrected in v0.18.0's entry.
- v0.18.0's own fixes are **partly confirmed on device**: the sheets now look
  right and full screen no longer crashes. Its blurred scrim was **too slow**
  and its arrow-pad answer to compare was **rejected** — both corrected in
  v0.18.1.
- **The viewer was measurably slow and nobody could see it from Windows.** Three
  device rounds reported "flickers", "not smooth", "feels dated" before the cause
  was found: one un-memoised object prop. Treat every future smoothness report as
  a question about what runs on the frame, not about the animation.
- Still **untested**: whether the memoised strips and the transform-based ghost
  drag are now smooth in the hand, whether the tiled ghost shows any seam at the
  clamp limit, whether one blur is now smooth enough on a real Android
  build (`expo-blur` behaves differently per OEM), whether the sheet clears the
  keyboard now that `OverlayLayer` and not the OS is lifting it, and whether
  Android's back button closes a sheet rather than the screen.
- Still untested from v0.17.0: the once-built responder, and the tap-vs-drag
  threshold (6 pt — possibly too tight for a shaky hand).
- The portrait toolbar is now **seven** 44 pt tools. It computes to 364 pt,
  which fits a 375 pt screen with 11 pt to spare; that margin has never been
  seen on a small device.
- The **tiled sheet's seams** at every zoom level, and whether the caliper
  crosshair lands where the finger expects, remain untested at the extremes.
- The **PDF has never been printed**. It bundles and the geometry comes from
  the shared builders, but page breaks, margins and how a printer renders
  0.1 mm grid strokes are only knowable on paper.

<!-- v0.10.5 — The splash lockup was genuinely off-centre: BrandLogo's inherited
     viewBox is padded 7.3 units left vs 27.6 right. New trap recorded, opt-in
     `crop` prop, and the splash goes to 90 % of the window. -->
<!-- v0.10.4 — The splash reverts to navy + the full BrandLogo; only v0.10.3's
     window-relative sizing survives. `CyphixLogo` deleted, not left uncalled. -->
<!-- v0.10.3 — The splash: white, the mark+CYPHIX lockup, sized from the
     window instead of a fixed point size. (Reverted by v0.10.4.) -->
<!-- v0.10.0 — The judder had a cause: an inline `palette` prop deleted every
     EcgReviewStrip memo. Three traps recorded — unstable props to a memo,
     re-deriving a signal for what was a transform, and an invisible touch
     surface gated on mode alone. -->
<!-- v0.10.2 — The glass row and a new trap: a tint that reached only one of
     two per-platform materials made every sheet invisible on iOS 26 while
     Android was correct. Dialogs stop being glass entirely. -->
<!-- v0.10.1 — Four from the first look at the flow: sign out moves onto the
     Profile tab, the welcome hero takes the text-only wordmark and a
     photograph, and the reference's pulsing ECG mark is deleted (it is not
     the CYPHIX identity). -->
<!-- v0.10.0 — The signed-out flow: splash, sign-in, biometric unlock, reset,
     and a 13-step registration built from the CYPHIX Onboarding reference.
     Three deliberate divergences recorded (system font instead of IBM Plex
     Mono, typed emergency contact instead of the address book, and the RBAC
     principal staying the demo clinician), plus three new traps. -->
<!-- v0.9.0 — One blur per overlay (two, with the outer one fading, was the
     flicker), and the ghost goes back to being dragged on the paper. Two more
     traps: stacked animated blurs, and replacing a direct-manipulation gesture
     with buttons. -->
<!-- v0.8.0 — Overlays leave `Modal` (the grey rectangle and the landscape crash
     were one import), and comparison becomes its own explaining sheet with tap
     controls. Three new traps recorded; the sheets row corrected — v0.17.0
     claimed a material it could not have rendered. -->
<!-- v0.7.0 — Scan History: the list + study viewer, the on-device store, the
     tiled sheet, touch calipers/marks/cursors/compare, and export by share
     sheet — plus the four traps that block cost. -->
